

ARQUIVO: src\app\contato\page.tsx

'use client'

import { motion } from 'framer-motion'
import { ArrowRight, Mail, Phone, MapPin, Send, MessageCircle } from 'lucide-react'
import Link from 'next/link'
import { SpotlightCard } from '@/components/ui/SpotlightCard'

export default function ContatoPage() {
  return (
    <div className="flex flex-col gap-32 pb-32">
      
      {/* BACKGROUND MESH EFFECT */}
      <div className="fixed inset-0 -z-50 pointer-events-none overflow-hidden flex justify-center items-center opacity-30">
        <div className="absolute w-[800px] h-[800px] bg-brand-primary/20 blur-[120px] rounded-full mix-blend-screen animate-pulse-slow translate-x-1/2 " />
      </div>

      {/* HERO SECTION */}
      <section className="pt-32 container mx-auto px-6 relative z-10">
        <motion.div
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8, ease: "easeOut" }}
          className="max-w-4xl"
        >
          <span className="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-brand-primary/30 bg-brand-primary/5 text-brand-primary font-bold tracking-wider uppercase mb-6 text-sm shadow-[0_0_20px_rgba(255,215,0,0.2)]">
            <MessageCircle className="w-4 h-4" />
            Connect & Deploy
          </span>
          <h1 className="text-5xl md:text-7xl lg:text-8xl font-black tracking-tighter mb-8 text-white leading-[1.05]">
             Inicie um <br />
            <span className="text-transparent bg-clip-text bg-gradient-to-r from-brand-primary to-amber-200">DiagnÃ³stico Executivo.</span>
          </h1>
          <p className="text-xl md:text-2xl text-foreground/70 max-w-3xl leading-relaxed font-light">
            Entre em contato direto para alinhar o mapeamento e arquitetura da sua operaÃ§Ã£o. Atendimento voltado para lideranÃ§as e operaÃ§Ãµes estruturadas B2B.
          </p>
        </motion.div>
      </section>

      {/* CONTACT MODULE */}
      <section className="container mx-auto px-6 mt-12">
        <div className="grid grid-cols-1 lg:grid-cols-12 gap-8">
          
          {/* FORMULARIO */}
          <SpotlightCard className="lg:col-span-7 p-8 md:p-12 border-white/10" spotlightColor="rgba(255, 215, 0, 0.1)">
             <h2 className="text-3xl font-black text-white mb-8">FormulÃ¡rio de Triagem</h2>
             <form className="flex flex-col gap-6" onSubmit={(e) => e.preventDefault()}>
                <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                  <div className="flex flex-col gap-2">
                    <label className="text-xs uppercase font-bold tracking-widest text-white/50">Nome / Empresa</label>
                    <input 
                      type="text" 
                      placeholder="IdentificaÃ§Ã£o..." 
                      className="bg-black/50 border border-white/10 rounded-xl px-4 py-4 text-white placeholder:text-white/20 focus:outline-none focus:border-brand-primary/50 focus:ring-1 focus:ring-brand-primary/50 transition-all font-light"
                    />
                  </div>
                  <div className="flex flex-col gap-2">
                    <label className="text-xs uppercase font-bold tracking-widest text-white/50">E-mail Corporativo</label>
                    <input 
                      type="email" 
                      placeholder="email@empresa.com.br" 
                      className="bg-black/50 border border-white/10 rounded-xl px-4 py-4 text-white placeholder:text-white/20 focus:outline-none focus:border-brand-primary/50 focus:ring-1 focus:ring-brand-primary/50 transition-all font-light"
                    />
                  </div>
                </div>

                <div className="flex flex-col gap-2">
                   <label className="text-xs uppercase font-bold tracking-widest text-white/50">Contexto Operacional (Onde dÃ³i?)</label>
                   <textarea 
                     rows={4}
                     placeholder="Descreva brevemente o gargalo tecnolÃ³gico ou logÃ­stico da sua operaÃ§Ã£o..." 
                     className="bg-black/50 border border-white/10 rounded-xl px-4 py-4 text-white placeholder:text-white/20 focus:outline-none focus:border-brand-primary/50 focus:ring-1 focus:ring-brand-primary/50 transition-all font-light resize-none"
                   />
                </div>

                <div className="pt-4 flex justify-end">
                   <button className="inline-flex items-center justify-center gap-3 bg-brand-primary text-black px-10 py-4 rounded-xl font-bold text-lg hover:shadow-[0_0_20px_rgba(255,215,0,0.4)] hover:scale-[1.02] transition-all">
                     <Send className="w-5 h-5" /> Submeter DiagnÃ³stico
                   </button>
                </div>
             </form>
          </SpotlightCard>

          {/* SIDE INFO */}
          <div className="lg:col-span-5 flex flex-col gap-6">
             
             {/* WhatsApp Card */}
             <SpotlightCard className="p-8 border-brand-success/30 h-full flex flex-col justify-center group" spotlightColor="rgba(0, 217, 142, 0.15)">
                <div className="w-16 h-16 rounded-2xl bg-brand-success/10 border border-brand-success/30 flex items-center justify-center mb-6 shadow-[0_0_15px_rgba(0,217,142,0.2)]">
                  <MessageCircle className="w-8 h-8 text-brand-success drop-shadow-[0_0_8px_rgba(0,217,142,0.8)]" />
                </div>
                <h3 className="text-2xl font-black text-white mb-2">Linha WhatsApp Executiva</h3>
                <p className="text-foreground/60 leading-relaxed font-light mb-6">Resposta rÃ¡pida para alinhamentos urgentes e prospecÃ§Ã£o de projetos.</p>
                <Link href="#" className="inline-flex items-center gap-2 text-brand-success font-bold uppercase tracking-widest text-sm hover:translate-x-2 transition-transform">
                  Chamar no WhatsApp <ArrowRight className="w-4 h-4" />
                </Link>
             </SpotlightCard>

             {/* Outras Infos Card */}
             <SpotlightCard className="p-8 border-brand-blue/30 flex flex-col justify-center" spotlightColor="rgba(0, 112, 243, 0.15)">
                <ul className="space-y-6">
                  <li className="flex gap-4 items-center">
                    <div className="p-3 bg-brand-blue/10 rounded-xl border border-brand-blue/20">
                      <Mail className="w-5 h-5 text-brand-blue" />
                    </div>
                    <div>
                      <p className="text-xs uppercase font-bold tracking-widest text-white/50">E-mail Direto</p>
                      <p className="font-bold text-white text-lg">contato@eduardolourenco.com</p>
                    </div>
                  </li>
                  <li className="flex gap-4 items-center mt-6 pt-6 border-t border-white/5">
                    <div className="p-3 bg-white/5 rounded-xl border border-white/10">
                      <MapPin className="w-5 h-5 text-white/80" />
                    </div>
                    <div>
                      <p className="text-xs uppercase font-bold tracking-widest text-white/50">Base</p>
                      <p className="font-bold text-white text-lg">Brasil / OperaÃ§Ãµes Remotas</p>
                    </div>
                  </li>
                </ul>
             </SpotlightCard>

          </div>

        </div>
      </section>

    </div>
  )
}


ARQUIVO: src\app\dados\page.tsx

'use client'

import { motion } from 'framer-motion'
import { ExecutiveDashboard } from '@/components/interactive/ExecutiveDashboard'

export default function DadosPage() {
  return (
    <div className="flex flex-col gap-24 pb-32">
      
      {/* HERO SECTION */}
      <section className="pt-32 container mx-auto px-6">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          className="max-w-4xl"
        >
          <span className="text-accent-gold font-bold tracking-wider uppercase mb-4 block">InteligÃªncia de Mercado</span>
          <h1 className="text-4xl md:text-6xl lg:text-7xl font-bold tracking-tighter mb-8 text-white leading-[1.1]">
            GestÃ£o de Dados e Performance Sazonal.
          </h1>
          <p className="text-xl text-foreground/80 max-w-2xl leading-relaxed">
            Planejamento tÃ¡tico com controle rigoroso de ACOS, ROAS e Taxa de ConversÃ£o por canal, construÃ­do para momentos de alta demanda (Black Friday, Dia das MÃ£es).
          </p>
        </motion.div>
      </section>

      {/* DASHBOARD COMPONENT SECTION */}
      <section className="container mx-auto px-6">
        <div className="mb-12">
          <h2 className="text-3xl font-bold text-white mb-4">Dashboards Executivos de DecisÃ£o</h2>
          <p className="text-foreground/80 max-w-3xl leading-relaxed">
            DecisÃµes comerciais nÃ£o se tomam por intuiÃ§Ã£o. Tomam-se por leitura de margem em tempo real. Altere os filtros sazonais abaixo para visualizar o impacto das nossas estratÃ©gias no faturamento cruzando Marketplaces:
          </p>
        </div>
        
        <ExecutiveDashboard />
      </section>

    </div>
  )
}


ARQUIVO: src\app\ecommerce\page.tsx

'use client'

import { useState } from 'react'
import { motion, AnimatePresence } from 'framer-motion'
import { BarChart3, Layers, GitBranch, Scale, AlertCircle, ChevronDown, ChevronRight, Target, Cpu } from 'lucide-react'
import { SpotlightCard } from '@/components/ui/SpotlightCard'
import { MarketplaceDataGrid } from '@/components/interactive/MarketplaceDataGrid'

// â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
// CONFIRMED REAL DATA ONLY â€” from Marketplace_Ecosystem_meu.xlsx
// Channels: Mercado Livre, Amazon, Magalu
// â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const CHANNELS = [
  {
    id: 'ml',
    name: 'Mercado Livre',
    role: 'Canal Principal â€” 60%+ do GMV',
    acosReal: 4.4,
    roasReal: 22.7,
    // Revenue omitted â€” awaiting owner confirmation
    revenue: null,
    badge: 'Meli+',
    badgeColor: 'brand-primary',
    detail: 'EstratÃ©gia full-funnel: trÃ¡fego orgÃ¢nico via SEO de TÃ­tulo/DescriÃ§Ã£o + ADS Patrocinados. ReputaÃ§Ã£o Platinum mantida por controle rÃ­gido de SLA de envio e mediaÃ§Ã£o proativa. OperaÃ§Ã£o de Full MLB para SKUs de alta rotaÃ§Ã£o.',
    architecture: [
      'Hub: Bling ERP como maestro de estoque e pedidos',
      'Sync automÃ¡tico de preÃ§os via API Bling â†’ ML (regra de margem mÃ­nima ativa)',
      'Etiquetas Full geradas em lote D-1 via integraÃ§Ã£o nativa',
      'ADS gerenciados via ACOS â‰¤5% como gatilho de pausa automÃ¡tica',
    ]
  },
  {
    id: 'amazon',
    name: 'Amazon Brasil',
    role: 'Canal de ExpansÃ£o',
    acosReal: null,   // Awaiting owner confirmation
    roasReal: null,
    revenue: null,
    badge: 'FBA/FBM',
    badgeColor: 'brand-blue',
    detail: 'EstratÃ©gia de expansÃ£o com foco inicial em ASINs de maior margem. CatÃ¡logo curado para evitar guerra de preÃ§o contra marcas prÃ³prias da Amazon. Foco em Buy Box via competitive pricing + rating de seller.',
    architecture: [
      'IntegraÃ§Ã£o via Bling â†’ Amazon Seller Central (plugin terceiro)',
      'PrecificaÃ§Ã£o dinÃ¢mica com repricer baseado em Buy Box data',
      'AvaliaÃ§Ãµes gerenciadas via programa Early Reviewer + follow-up pÃ³s-compra',
    ]
  },
  {
    id: 'magalu',
    name: 'Magazine Luiza',
    role: 'Canal de Volume Complementar',
    acosReal: null,
    roasReal: null,
    revenue: null,
    badge: 'Magalog',
    badgeColor: 'brand-success',
    detail: 'Posicionamento em categorias de menor competiÃ§Ã£o no ML. Aproveitamento da logÃ­stica da Magalog para regiÃµes com cobertura inferior dos Correios, especialmente interior de SP e regiÃµes Norte/Nordeste.',
    architecture: [
      'CatÃ¡logo sincronizado via Bling ERP (subset de SKUs selecionados)',
      'SLA de despacho especÃ­fico configurado conforme modal de entrega Magalog',
      'PreÃ§o estrategicamente acima do ML para nÃ£o canibalizar canal principal',
    ]
  },
]

// â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
// STRATEGIC INSIGHT BLOCKS â€” Engineering Case Study Format
// â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const INSIGHTS = [
  {
    icon: Target,
    color: 'brand-primary',
    title: 'Metodologia de Metas Semanais (ATG%)',
    desc: 'A meta semanal nÃ£o Ã© um nÃºmero arbitrÃ¡rio â€” Ã© calculada a partir do GMV mensal comprometido, decomposto em pesos por semana (levando em conta sazonalidade, datas comerciais e estoque projetado). O ATG% (Atingimento de Meta) Ã© o Ãºnico KPI de aceleraÃ§Ã£o que importa: positivo = aumentar investimento em ADS; negativo = auditar funil de conversÃ£o antes de qualquer gasto adicional.',
  },
  {
    icon: Scale,
    color: 'brand-blue',
    title: 'ACOS como RÃ©gua de EficiÃªncia, nÃ£o Meta',
    desc: 'ACOS (Advertising Cost of Sales) nunca Ã© gerenciado de forma isolada. O alvo Ã© ACOS â‰¤5% (benchmarked no ML: 4.4%), mas o ROAS Ã© quem confirma a qualidade do gasto. Um ACOS de 4% com ROAS de 10x em produto de ticket baixo Ã© pior do que ACOS de 6% com ROAS de 22x em produto de ticket alto. A anÃ¡lise sempre considera os dois em conjunto com o Ticket MÃ©dio.',
  },
  {
    icon: Layers,
    color: 'brand-success',
    title: 'ProteÃ§Ã£o de ReputaÃ§Ã£o como Ativo EstratÃ©gico',
    desc: 'No Mercado Livre, a reputaÃ§Ã£o (termÃ´metro) Ã© o ativo mais crÃ­tico â€” ela afeta diretamente o ranking de busca orgÃ¢nica, o custo de ADS e a elegibilidade para programas como Meli+. O processo operacional inteiro Ã© projetado em torno de dois KPIs de reputaÃ§Ã£o: SLA de Envio >95% e MediaÃ§Ãµes <2%. Qualquer degradaÃ§Ã£o aciona protocolo de correÃ§Ã£o imediata antes de qualquer anÃ¡lise de receita.',
  },
  {
    icon: GitBranch,
    color: 'brand-primary',
    title: 'Arquitetura Hub-and-Spoke: Bling como Camada Central',
    desc: 'A integraÃ§Ã£o multi-canal Ã© viabilizada pelo Bling ERP funcionando como hub Ãºnico de dados: todos os canais (ML, Amazon, Magalu) alimentam o Bling via API. O estoque Ã© decrementado em tempo real (reserva imediata no momento do pedido, confirmaÃ§Ã£o no despacho). Isso elimina o risco de overselling â€” o maior killer de reputaÃ§Ã£o em operaÃ§Ãµes multi-canal.',
  },
]

export default function EcommercePage() {
  const [openChannel, setOpenChannel] = useState<string | null>('ml')

  return (
    <div className="flex flex-col gap-28 pb-32">

      {/* BACKGROUND */}
      <div className="fixed inset-0 -z-50 pointer-events-none overflow-hidden opacity-20">
        <div className="absolute w-[900px] h-[900px] bg-brand-primary/15 blur-[150px] rounded-full -top-1/4 right-0" />
        <div className="absolute w-[600px] h-[600px] bg-brand-blue/10 blur-[120px] rounded-full bottom-0 left-0" />
      </div>

      {/* â”€â”€ HERO â€” CASE STUDY HEADER â”€â”€ */}
      <section className="pt-32 container mx-auto px-6">
        <motion.div
          initial={{ opacity: 0, y: 24 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.7 }}
          className="max-w-5xl"
        >
          <div className="flex flex-wrap items-center gap-3 mb-6">
            <span className="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-brand-primary/30 bg-brand-primary/5 text-brand-primary font-bold tracking-wider uppercase text-xs">
              <BarChart3 className="w-3.5 h-3.5" />
              Case Study â€” E-Commerce Operations
            </span>
            <span className="text-white/20 text-xs font-mono">â†’ GestÃ£o Multi-Canal / Performance Marketing / Arquitetura de Dados</span>
          </div>

          <h1 className="text-5xl md:text-7xl font-black tracking-tighter mb-6 text-white leading-[1.05]">
            Escalabilidade em{' '}
            <span className="text-transparent bg-clip-text bg-gradient-to-r from-brand-primary to-yellow-200">
              Marketplace<br />como Engenharia.
            </span>
          </h1>

          <p className="text-xl text-white/60 max-w-3xl leading-relaxed font-light">
            NÃ£o existe &ldquo;grow hacking&rdquo; sustentÃ¡vel em marketplace. Existe <strong className="text-white">arquitetura operacional</strong>: integraÃ§Ã£o de dados, controle de SLA, gestÃ£o de ADS baseada em indicadores â€” e um processo de decisÃ£o semanal disciplinado, documentado e reproduzÃ­vel.
          </p>
        </motion.div>
      </section>

      {/* â”€â”€ SEÃ‡ÃƒO 1: CONTEXTO â€” O PROBLEMA â”€â”€ */}
      <section className="container mx-auto px-6">
        <div className="grid grid-cols-1 md:grid-cols-2 gap-12 items-start">
          <div>
            <p className="text-xs font-bold uppercase tracking-widest text-brand-primary mb-4">01 â€” Contexto</p>
            <h2 className="text-3xl md:text-4xl font-black mb-6 text-white tracking-tight leading-tight">
              O Problema Estrutural das OperaÃ§Ãµes Multi-Canal
            </h2>
            <div className="space-y-4 text-white/60 leading-relaxed">
              <p>
                OperaÃ§Ãµes de e-commerce que crescem organicamente acumulam um conjunto de problemas invisÃ­veis: estoque gerenciado em planilhas desconectadas dos canais, ADS rodando sem correlaÃ§Ã£o com posiÃ§Ã£o de estoque real, e decisÃµes de precificaÃ§Ã£o tomadas sem dados de margem consolidados.
              </p>
              <p>
                O resultado Ã© previsÃ­vel: overselling que destrÃ³i reputaÃ§Ã£o, investimento em ADS para produtos sem estoque, e um ciclo de apagar incÃªndios que impede a visÃ£o estratÃ©gica.
              </p>
              <p className="text-white/80 font-medium border-l-2 border-brand-primary pl-4">
                A minha abordagem parte de um princÃ­pio de engenharia: <strong className="text-white">vocÃª nÃ£o pode gerenciar o que nÃ£o mede, e nÃ£o pode escalar o que nÃ£o estÃ¡ integrado.</strong>
              </p>
            </div>
          </div>

          <div className="grid grid-cols-2 gap-4">
            {[
              { metric: 'ACOS Real ML', value: '4.4%', note: 'Benchmarked â€” Mercado Livre', ok: true },
              { metric: 'ROAS Real ML', value: '22.7x', note: 'Confirmado â€” Marketplace_Ecosystem', ok: true },
              { metric: 'ACOS Benchmark', value: 'â‰¤ 5%', note: 'Gatilho operacional interno', ok: true },
              { metric: 'Canais Gerenciados', value: '3+', note: 'ML, Amazon, Magalu via Bling', ok: true },
            ].map((m) => (
              <SpotlightCard key={m.metric} className="p-5" spotlightColor="rgba(255,215,0,0.08)">
                <p className="text-[10px] uppercase tracking-widest text-white/30 mb-1">{m.metric}</p>
                <p className="text-2xl font-black text-white mb-1">{m.value}</p>
                <p className="text-xs text-white/40 leading-tight">{m.note}</p>
                {!m.ok && (
                  <span className="mt-2 inline-flex items-center gap-1 text-[10px] text-brand-primary">
                    <AlertCircle className="w-3 h-3" /> pendente
                  </span>
                )}
              </SpotlightCard>
            ))}
          </div>
        </div>
      </section>

      {/* â”€â”€ SEÃ‡ÃƒO 2: VISÃƒO ESTRATÃ‰GICA â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <p className="text-xs font-bold uppercase tracking-widest text-brand-blue mb-4">02 â€” VisÃ£o EstratÃ©gica</p>
        <h2 className="text-3xl md:text-4xl font-black mb-4 text-white tracking-tight">
          Como Analiso uma OperaÃ§Ã£o de Marketplace
        </h2>
        <p className="text-white/50 max-w-2xl mb-12 leading-relaxed">
          Cada canal tem uma lÃ³gica prÃ³pria de rentabilidade. A gestÃ£o integrada exige uma camada de abstraÃ§Ã£o que consolida os dados em indicadores comparÃ¡veis â€” sem esconder as diferenÃ§as estruturais entre canais.
        </p>

        <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
          {INSIGHTS.map((ins) => {
            const Icon = ins.icon
            return (
              <SpotlightCard key={ins.title} className="p-6 md:p-8" spotlightColor={`rgba(255,215,0,0.06)`}>
                <div className={`w-10 h-10 rounded-xl bg-${ins.color}/10 border border-${ins.color}/20 flex items-center justify-center mb-5`}>
                  <Icon className={`w-5 h-5 text-${ins.color}`} />
                </div>
                <h3 className="text-white font-bold text-base mb-3 leading-tight">{ins.title}</h3>
                <p className="text-white/55 text-sm leading-relaxed">{ins.desc}</p>
              </SpotlightCard>
            )
          })}
        </div>
      </section>

      {/* â”€â”€ SEÃ‡ÃƒO 3: ARQUITETURA â€” CANAIS â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <p className="text-xs font-bold uppercase tracking-widest text-brand-success mb-4">03 â€” Arquitetura de Canais</p>
        <h2 className="text-3xl md:text-4xl font-black mb-4 text-white tracking-tight">
          DecisÃ£o por Canal: LÃ³gica de AlocaÃ§Ã£o
        </h2>
        <p className="text-white/50 max-w-2xl mb-12 leading-relaxed">
          Cada canal recebe um conjunto de SKUs, estratÃ©gia de preÃ§o e polÃ­tica de ADS distintos. A decisÃ£o de alocaÃ§Ã£o Ã© baseada em dados de margem por canal, velocidade histÃ³rica de giro e capacidade logÃ­stica disponÃ­vel.
        </p>

        <div className="space-y-3">
          {CHANNELS.map((ch) => {
            const isOpen = openChannel === ch.id
            return (
              <div key={ch.id}
                className={`rounded-2xl border transition-all duration-300 overflow-hidden
                  ${isOpen ? 'border-white/10 bg-white/[0.03]' : 'border-white/5 bg-white/[0.015]'}`}
              >
                {/* Channel Header */}
                <button
                  className="w-full flex items-center justify-between p-6 text-left gap-4"
                  onClick={() => setOpenChannel(isOpen ? null : ch.id)}
                >
                  <div className="flex items-center gap-4 min-w-0">
                    <div>
                      <div className="flex items-center gap-2 mb-0.5">
                        <h3 className="text-white font-bold">{ch.name}</h3>
                        <span className={`text-[10px] font-mono font-bold px-2 py-0.5 rounded-full border text-${ch.badgeColor} border-${ch.badgeColor}/30 bg-${ch.badgeColor}/10`}>
                          {ch.badge}
                        </span>
                      </div>
                      <p className="text-white/40 text-xs">{ch.role}</p>
                    </div>
                  </div>
                  <div className="flex items-center gap-6 shrink-0">
                    {ch.acosReal !== null ? (
                      <div className="text-center hidden sm:block">
                        <p className="text-[10px] text-white/30 uppercase tracking-widest">ACOS</p>
                        <p className="text-brand-success font-black font-mono text-lg">{ch.acosReal}%</p>
                      </div>
                    ) : (
                      <div className="text-center hidden sm:block">
                        <p className="text-[10px] text-white/20 uppercase tracking-widest">ACOS</p>
                        <p className="text-white/20 font-mono text-lg">â€”â€”</p>
                      </div>
                    )}
                    {ch.roasReal !== null ? (
                      <div className="text-center hidden sm:block">
                        <p className="text-[10px] text-white/30 uppercase tracking-widest">ROAS</p>
                        <p className="text-brand-primary font-black font-mono text-lg">{ch.roasReal}x</p>
                      </div>
                    ) : (
                      <div className="text-center hidden sm:block">
                        <p className="text-[10px] text-white/20 uppercase tracking-widest">ROAS</p>
                        <p className="text-white/20 font-mono text-lg">â€”â€”</p>
                      </div>
                    )}
                    {isOpen ? <ChevronDown className="w-4 h-4 text-white/40" /> : <ChevronRight className="w-4 h-4 text-white/40" />}
                  </div>
                </button>

                {/* Channel Detail */}
                <AnimatePresence>
                  {isOpen && (
                    <motion.div
                      initial={{ height: 0, opacity: 0 }}
                      animate={{ height: 'auto', opacity: 1 }}
                      exit={{ height: 0, opacity: 0 }}
                      transition={{ duration: 0.25 }}
                      className="overflow-hidden"
                    >
                      <div className="px-6 pb-6 pt-0 border-t border-white/5">
                        <div className="grid grid-cols-1 md:grid-cols-2 gap-8 pt-6">
                          <div>
                            <p className="text-xs font-bold uppercase tracking-widest text-white/30 mb-3">EstratÃ©gia</p>
                            <p className="text-white/60 text-sm leading-relaxed">{ch.detail}</p>
                          </div>
                          <div>
                            <p className="text-xs font-bold uppercase tracking-widest text-white/30 mb-3">Arquitetura TÃ©cnica</p>
                            <ul className="space-y-2">
                              {ch.architecture.map((item, i) => (
                                <li key={i} className="flex items-start gap-2 text-sm text-white/55">
                                  <span className="text-brand-primary font-mono text-[10px] mt-1 shrink-0">â†’</span>
                                  {item}
                                </li>
                              ))}
                            </ul>
                          </div>
                        </div>
                        {ch.revenue === null && (
                          <div className="mt-4 flex items-center gap-2 text-[11px] text-white/30 border-t border-white/5 pt-4">
                            <AlertCircle className="w-3.5 h-3.5 text-brand-primary/60" />
                            Dados de receita por canal pendentes de confirmaÃ§Ã£o do proprietÃ¡rio.
                          </div>
                        )}
                      </div>
                    </motion.div>
                  )}
                </AnimatePresence>
              </div>
            )
          })}
        </div>
      </section>

      {/* â”€â”€ SEÃ‡ÃƒO 4: COMPONENTE INTERATIVO â€” DATA MATRIX â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <p className="text-xs font-bold uppercase tracking-widest text-brand-primary mb-4">04 â€” A Prova: Matriz de Dados Semanal</p>
        <h2 className="text-3xl md:text-4xl font-black mb-4 text-white tracking-tight">
          O Sistema de DecisÃ£o Semanal
        </h2>
        <p className="text-white/55 max-w-3xl mb-4 leading-relaxed text-sm">
          Esta Ã© a planilha que governa a operaÃ§Ã£o toda semana. Cada linha Ã© uma decisÃ£o: se o <strong className="text-white">ATG%</strong> estÃ¡ positivo, aumenta-se investimento em ADS. Se o <strong className="text-white">ACOS</strong> ultrapassou 5%, pausa-se as campanhas menos eficientes antes de qualquer outra aÃ§Ã£o. Se o <strong className="text-white">ROAS</strong> caiu, audita-se o mix de SKUs patrocinados.
        </p>
        <p className="text-white/35 text-xs mb-10 font-mono">
          {`// Aguardando: Dados reais das semanas da aba SEMANAL do Marketplace_Ecosystem_meu.xlsx`}
        </p>
        <MarketplaceDataGrid />
      </section>

    </div>
  )
}


ARQUIVO: src\app\engenharia\page.tsx

'use client'

import { motion } from 'framer-motion'
import {
  Server, Database, GitMerge, Zap, Code2, Shield,
  AlertTriangle, CheckCircle2, ArrowRight, Cpu,
  Lock, FileText, RefreshCw, GitBranch, Layers,
  Clock, Terminal, Package
} from 'lucide-react'
import { ErpBridge } from '@/components/interactive/ErpBridge'
import { KanbanSimulator } from '@/components/interactive/KanbanSimulator'
import { SpotlightCard } from '@/components/ui/SpotlightCard'

// â”€â”€â”€ TRAVA FISCAL â€” Business Logic Breakdown â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const travaFiscalSteps = [
  {
    step: '01',
    title: 'TÃ©cnico Finaliza OS',
    icon: CheckCircle2,
    color: 'brand-primary',
    desc: 'O tÃ©cnico marca a Ordem de ServiÃ§o como "ConcluÃ­da" no sistema. Neste ponto, o sistema verifica se existe uma Nota Fiscal emitida e vinculada.',
    code: '// Trigger: status === "CONCLUIDA"\nawait checkXmlValidation(osId)'
  },
  {
    step: '02',
    title: 'ValidaÃ§Ã£o de XML Ativo',
    icon: FileText,
    color: 'brand-blue',
    desc: 'O sistema consulta a tabela de NFs vinculadas Ã  OS. Se nÃ£o houver XML serializado e validado pela SEFAZ, a OS Ã© bloqueada automaticamente.',
    code: 'if (!nota?.xmlSefaz) {\n  throw new TravaFiscalException(\n    "OS bloqueada: XML nÃ£o validado"\n  )\n}'
  },
  {
    step: '03',
    title: 'Bloqueio com Auditoria',
    icon: Lock,
    color: 'red-400',
    desc: 'O fechamento Ã© recusado e um registro de auditoria Ã© criado com timestamp, usuÃ¡rio que tentou fechar e ID da OS. Isso alimenta o relatÃ³rio mensal de conformidade.',
    code: 'await auditLog.create({\n  action: "TRAVA_FISCAL_TRIGGERED",\n  osId, userId, timestamp: new Date()\n})'
  },
  {
    step: '04',
    title: 'EmissÃ£o ForÃ§ada NF',
    icon: RefreshCw,
    color: 'brand-success',
    desc: 'O operador Ã© direcionado ao mÃ³dulo de emissÃ£o de NF integrado ao Bling. ApÃ³s emissÃ£o e retorno do XML autorizado, a trava Ã© liberada automaticamente e a OS Ã© fechada.',
    code: '// Webhook Bling â†’ Sistema OS\nonNfAutorizada(nfId) {\n  await liberarTrava(osId)\n}'
  },
]

// â”€â”€â”€ BLING â†” DYNAMICS ARCHITECTURE â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const blingDynamicsFlow = [
  {
    from: 'ERP Dynamics',
    fromDetail: 'ERP Corporativo Industrial',
    to: 'Camada de Sync',
    toDetail: 'Middleware Node.js',
    event: 'Pedido de Compra PO com peso > 40kg',
    trigger: 'Cron D+0 04:00',
    color: 'brand-blue',
  },
  {
    from: 'Middleware',
    fromDetail: 'ValidaÃ§Ã£o e TransformaÃ§Ã£o',
    to: 'Bling ERP',
    toDetail: 'Hub Marketplace',
    event: 'CriaÃ§Ã£o de Pedido de Venda + NF de entrada',
    trigger: 'REST API Bling v3',
    color: 'brand-primary',
  },
  {
    from: 'Bling ERP',
    fromDetail: 'Fulfillment & Estoque',
    to: 'Transportadora TMS',
    toDetail: 'Jadlog / Correios API',
    event: 'GeraÃ§Ã£o de etiqueta logÃ­stica + despacho',
    trigger: 'Webhook pÃ³s-NF',
    color: 'brand-success',
  },
  {
    from: 'TMS / Carrier',
    fromDetail: 'Rastreamento em tempo real',
    to: 'ERP Dynamics + ML',
    toDetail: 'Sync de status bidirecional',
    event: 'AtualizaÃ§Ã£o de status: Coletado â†’ Em TrÃ¢nsito â†’ Entregue',
    trigger: 'Polling 2h + Webhook on-event',
    color: 'brand-indigo',
  },
]

// â”€â”€â”€ SYSTEM ARCHITECTURE â€” OS (Memo) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const osArchitecture = [
  {
    layer: 'ApresentaÃ§Ã£o',
    tech: 'React + Next.js',
    desc: 'Interface de abertura e gestÃ£o de OS. Dashboard de tÃ©cnicos com visÃ£o Kanban e painel de aprovaÃ§Ãµes.',
    color: 'brand-primary'
  },
  {
    layer: 'API / Business Logic',
    tech: 'NestJS + TypeScript',
    desc: 'Controllers REST, Guards de autenticaÃ§Ã£o JWT, interceptors de auditoria e mÃ³dulo de Trava Fiscal. ValidaÃ§Ã£o de DTOs via class-validator.',
    color: 'brand-blue'
  },
  {
    layer: 'ORM / Banco de Dados',
    tech: 'Prisma + PostgreSQL',
    desc: 'Schema relacional com entidades: OS, OrdemMÃ£e, OrdensFilhas, NotaFiscal, AuditLog. Migrations versionadas. Transactions ACID para operaÃ§Ãµes crÃ­ticas.',
    color: 'brand-success'
  },
  {
    layer: 'IntegraÃ§Ãµes Externas',
    tech: 'Bling API v3 + SEFAZ',
    desc: 'EmissÃ£o de NF via Bling, validaÃ§Ã£o de XML via WebService SEFAZ, sincronizaÃ§Ã£o de estoque e precificaÃ§Ã£o nos canais de venda.',
    color: 'brand-indigo'
  },
]

// â”€â”€â”€ TECHNICAL DECISIONS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const decisions = [
  {
    icon: Shield,
    color: 'brand-primary',
    title: 'Por que NestJS e nÃ£o Express?',
    desc: 'O sistema precisava de uma arquitetura modular escalÃ¡vel desde o inÃ­cio. O NestJS fornece injeÃ§Ã£o de dependÃªncia nativa (IoC), decorators para Guards/Interceptors e organizaÃ§Ã£o em mÃ³dulos â€” essencial para implementar a Trava Fiscal como um mÃ³dulo isolado e testÃ¡vel independentemente do restante da lÃ³gica de OS.'
  },
  {
    icon: Database,
    color: 'brand-blue',
    title: 'Por que Prisma ORM?',
    desc: 'A alternativa seria TypeORM ou SQL raw. Prisma foi escolhido pela type-safety garantida na camada de acesso ao banco â€” crÃ­tica quando uma transaÃ§Ã£o envolve OS + NF + AuditLog simultÃ¢neos. O Prisma Client gerado automaticamente elimina queries erradas em runtime antes do deploy.'
  },
  {
    icon: Zap,
    color: 'brand-success',
    title: 'Webhook vs Polling para Bling?',
    desc: 'A integraÃ§Ã£o Bling usa Webhook para eventos crÃ­ticos (NF autorizada, pedido criado) e Polling para sincronizaÃ§Ã£o de estoque (a cada 30min). O Webhook garante reaÃ§Ã£o imediata Ã  liberaÃ§Ã£o da Trava Fiscal; o Polling evita sobrecarregar a API em momentos de baixo trÃ¡fego com mÃºltiplos subscribe events.'
  },
  {
    icon: Layers,
    color: 'brand-indigo',
    title: 'PadrÃ£o Mother/Daughter Order',
    desc: 'Uma OS mÃ£e pode gerar N ordens filhas (por tipo de serviÃ§o: elÃ©trico, mecÃ¢nico, hidrÃ¡ulico). Cada filha tem status independente mas a OS mÃ£e sÃ³ fecha quando TODAS as filhas estÃ£o concluÃ­das E com NFe validada. Esse padrÃ£o evita o fechamento parcial que gerava inconsistÃªncia nos relatÃ³rios financeiros.'
  },
]

export default function EngenhariaPage() {
  return (
    <div className="flex flex-col gap-28 pb-32">

      {/* BACKGROUND */}
      <div className="fixed inset-0 -z-50 pointer-events-none overflow-hidden opacity-25">
        <div className="absolute w-[800px] h-[800px] bg-brand-primary/15 blur-[130px] rounded-full top-0 right-0 animate-pulse-slow" />
        <div className="absolute w-[600px] h-[600px] bg-brand-blue/10 blur-[110px] rounded-full bottom-0 left-0 animate-pulse-slow" style={{ animationDelay: '2s' }} />
      </div>

      {/* â”€â”€ HERO â”€â”€ */}
      <section className="pt-32 container mx-auto px-6">
        <motion.div
          initial={{ opacity: 0, y: 28 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8 }}
          className="max-w-5xl"
        >
          <div className="flex flex-wrap gap-3 mb-8">
            <span className="inline-flex items-center gap-2 px-3 py-1.5 rounded-full border border-brand-primary/30 bg-brand-primary/5 text-brand-primary font-bold tracking-wider uppercase text-xs">
              <Code2 className="w-3.5 h-3.5" />
              Case Study â€” Engenharia de Software
            </span>
            <span className="text-white/20 text-xs font-mono self-center">NestJS Â· Prisma Â· Bling API Â· Microsoft Dynamics</span>
          </div>

          <h1 className="text-5xl md:text-7xl font-black tracking-tighter mb-6 text-white leading-[1.02]">
            Sistemas que<br />
            <span className="text-transparent bg-clip-text bg-gradient-to-r from-brand-primary to-amber-200">
              protegem a operaÃ§Ã£o.
            </span>
          </h1>

          <p className="text-xl text-white/55 max-w-3xl leading-relaxed font-light">
            Desenvolvimento de software industrial, nÃ£o de startups de demo. Cada decisÃ£o arquitetural aqui tem um motivo de negÃ³cio claro: evitar evasÃ£o fiscal, eliminar Double Data Entry e garantir que nenhuma OS feche sem nota fiscal autorizada pela SEFAZ.
          </p>
        </motion.div>
      </section>

      {/* â”€â”€ CASE 1: SISTEMA OS + TRAVA FISCAL â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <p className="text-xs font-bold uppercase tracking-widest text-brand-primary mb-4">01 â€” Sistema OS (Projeto Memo)</p>
        <h2 className="text-3xl md:text-4xl font-black mb-4 text-white tracking-tight">
          A Trava Fiscal â€” PrevenÃ§Ã£o de EvasÃ£o na Fonte
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-8 mb-12">
          <div>
            <p className="text-white/60 leading-relaxed text-sm mb-4">
              <strong className="text-white">O problema:</strong> TÃ©cnicos fechavam Ordens de ServiÃ§o sem emitir Nota Fiscal â€” seja por pressa, por erro ou intencionalmente. O resultado: receita prestada sem competÃªncia fiscal, inconsistÃªncia no faturamento e risco de autuaÃ§Ã£o tributÃ¡ria.
            </p>
            <p className="text-white/60 leading-relaxed text-sm mb-4">
              <strong className="text-white">A soluÃ§Ã£o arquitetural:</strong> Implementei um mÃ³dulo de Trava Fiscal no NestJS que intercepta qualquer tentativa de mudanÃ§a de status para "CONCLUÃDA" e verifica na base de dados se existe um XML de NF autorizado pela SEFAZ vinculado Ã quela OS. Se nÃ£o existir â€” a operaÃ§Ã£o Ã© rejeitada em nÃ­vel de API, nÃ£o na interface. Isso garante que a trava funcione independentemente do frontend.
            </p>
            <p className="text-white/60 leading-relaxed text-sm">
              <strong className="text-white">O impacto:</strong> 100% das OS fechadas apÃ³s a implementaÃ§Ã£o tÃªm NFe correspondente. O relatÃ³rio de auditoria automÃ¡tico mostrou as tentativas de fechamento indevido e permitiu treinamento direcionado da equipe.
            </p>
          </div>
          <div className="bg-black/60 rounded-2xl border border-white/5 p-5 font-mono text-xs">
            <p className="text-white/30 mb-3 text-[10px] uppercase tracking-widest">// trava-fiscal.guard.ts</p>
            <div className="space-y-1 text-white/70">
              <p><span className="text-brand-blue">@Injectable()</span></p>
              <p><span className="text-brand-primary">export class</span> <span className="text-yellow-300">TravaFiscalGuard</span> <span className="text-brand-primary">implements</span> CanActivate {'{'}</p>
              <p className="pl-4"><span className="text-brand-primary">async</span> canActivate(ctx): <span className="text-brand-success">Promise{'<boolean>'}</span> {'{'}</p>
              <p className="pl-8 text-white/40">{'// Extrai osId do body'}</p>
              <p className="pl-8"><span className="text-brand-primary">const</span> os = <span className="text-brand-primary">await</span> this.osService.<span className="text-yellow-300">findWithNota</span>(osId)</p>
              <p className="pl-8"><span className="text-brand-primary">if</span> (!os.notaFiscal?.xmlSefaz) {'{'}</p>
              <p className="pl-12"><span className="text-red-400">throw new</span> <span className="text-yellow-300">ForbiddenException</span>(</p>
              <p className="pl-16 text-brand-success">&apos;Trava Fiscal: XML SEFAZ ausente&apos;</p>
              <p className="pl-12">)</p>
              <p className="pl-8">{'}'}</p>
              <p className="pl-8"><span className="text-brand-primary">return</span> <span className="text-brand-success">true</span></p>
              <p className="pl-4">{'}'}</p>
              <p>{'}'}</p>
            </div>
          </div>
        </div>

        {/* Trava Fiscal Flow */}
        <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-4 gap-4 mb-12">
          {travaFiscalSteps.map((step, i) => {
            const Icon = step.icon
            return (
              <motion.div
                key={step.step}
                initial={{ opacity: 0, y: 20 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ delay: i * 0.1 }}
                className="relative"
              >
                <div className={`p-5 rounded-2xl border h-full
                  ${step.color === 'red-400'
                    ? 'border-red-400/20 bg-red-400/5'
                    : `border-${step.color}/15 bg-${step.color}/5`
                  }`}>
                  <div className="flex items-center gap-2 mb-4">
                    <span className={`text-[10px] font-mono font-bold
                      ${step.color === 'red-400' ? 'text-red-400' : `text-${step.color}`}`}>{step.step}</span>
                    <div className={`w-7 h-7 rounded-lg flex items-center justify-center
                      ${step.color === 'red-400' ? 'bg-red-400/10' : `bg-${step.color}/10`}`}>
                      <Icon className={`w-3.5 h-3.5 ${step.color === 'red-400' ? 'text-red-400' : `text-${step.color}`}`} />
                    </div>
                  </div>
                  <p className="text-white font-bold text-sm mb-2">{step.title}</p>
                  <p className="text-white/50 text-xs leading-relaxed mb-4">{step.desc}</p>
                  <div className="bg-black/60 rounded-lg p-3 font-mono text-[10px] text-white/40 leading-relaxed overflow-x-auto">
                    <pre>{step.code}</pre>
                  </div>
                </div>
                {i < travaFiscalSteps.length - 1 && (
                  <div className="hidden xl:flex absolute -right-2 top-1/2 -translate-y-1/2 z-10">
                    <ArrowRight className="w-4 h-4 text-white/20" />
                  </div>
                )}
              </motion.div>
            )
          })}
        </div>

        {/* Architecture Stack */}
        <div>
          <p className="text-xs font-bold uppercase tracking-widest text-white/30 mb-6">Stack do Sistema OS</p>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
            {osArchitecture.map((layer) => (
              <div key={layer.layer} className="flex gap-4 p-5 rounded-2xl border border-white/5 bg-white/[0.02]">
                <div className={`w-2 rounded-full bg-${layer.color} shrink-0 self-stretch`} />
                <div>
                  <div className="flex items-center gap-3 mb-1">
                    <span className="text-white font-bold text-sm">{layer.layer}</span>
                    <span className={`text-[10px] font-mono text-${layer.color} border border-${layer.color}/30 bg-${layer.color}/10 px-2 py-0.5 rounded-full`}>{layer.tech}</span>
                  </div>
                  <p className="text-white/50 text-xs leading-relaxed">{layer.desc}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* â”€â”€ INTERACTIVE: KANBAN SIMULATOR â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <p className="text-xs font-bold uppercase tracking-widest text-brand-primary mb-4">Componente Interativo â€” OS Simulator</p>
        <h2 className="text-3xl md:text-4xl font-black mb-4 text-white tracking-tight">
          Ciclo de Vida da Ordem de ServiÃ§o
        </h2>
        <p className="text-white/55 max-w-3xl mb-10 leading-relaxed text-sm">
          SimulaÃ§Ã£o do fluxo real: OS MÃ£e desdobrando em Ordens Filhas por tipo de serviÃ§o. Cada filha percorre seu prÃ³prio pipeline. A OS MÃ£e sÃ³ avanÃ§a quando todas as filhas estÃ£o resolvidas. A Trava Fiscal bloqueia o fechamento final atÃ© a NFe ser validada.
        </p>
        <KanbanSimulator />
      </section>

      {/* â”€â”€ CASE 2: BLING â†” DYNAMICS â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <p className="text-xs font-bold uppercase tracking-widest text-brand-blue mb-4">02 â€” IntegraÃ§Ã£o ERP Bidirecional</p>
        <h2 className="text-3xl md:text-4xl font-black mb-4 text-white tracking-tight">
          Bling â†” Microsoft Dynamics: Eliminando o Double Data Entry
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-8 mb-12">
          <div>
            <p className="text-white/60 leading-relaxed text-sm mb-4">
              <strong className="text-white">O problema:</strong> A empresa operava com dois ERPs distintos sem comunicaÃ§Ã£o: o Microsoft Dynamics para a parte industrial (pedidos de compra, logÃ­stica pesada acima de 40kg) e o Bling para a operaÃ§Ã£o de e-commerce e emissÃ£o de NF. O resultado era Double Data Entry manual â€” os mesmos pedidos sendo digitados duas vezes por pessoas diferentes.
            </p>
            <p className="text-white/60 leading-relaxed text-sm mb-4">
              <strong className="text-white">A arquitetura:</strong> Desenvolvi um middleware em Node.js que atua como mediador assÃ­ncrono entre os dois sistemas. O Dynamics dispara eventos via webhook quando um Pedido de Compra Ã© aprovado. O middleware valida, transforma o payload e cria o pedido correspondente no Bling via REST API v3. O processo contrÃ¡rio (atualizaÃ§Ã£o de status de entrega) ocorre via polling + webhook do carrier.
            </p>
            <p className="text-white/60 leading-relaxed text-sm">
              <strong className="text-white">ResiliÃªncia:</strong> Todas as chamadas sÃ£o persistidas em uma fila de retry com backoff exponencial. Falhas na API do Bling sÃ£o registradas e reprocessadas automaticamente atÃ© 3 vezes antes de gerar um alerta para o operador.
            </p>
          </div>
          <div>
            <p className="text-xs font-bold uppercase tracking-widest text-white/30 mb-4">Fluxo de Dados â€” Pedido LogÃ­stico {'>'} 40kg</p>
            <div className="space-y-3">
              {blingDynamicsFlow.map((flow, i) => (
                <motion.div
                  key={i}
                  initial={{ opacity: 0, x: 20 }}
                  whileInView={{ opacity: 1, x: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: i * 0.1 }}
                  className="p-4 rounded-xl border border-white/5 bg-white/[0.02]"
                >
                  <div className="flex items-center gap-2 mb-2">
                    <span className={`text-[10px] font-bold font-mono text-${flow.color}`}>
                      {flow.from} â†’ {flow.to}
                    </span>
                    <span className={`text-[10px] border border-${flow.color}/30 bg-${flow.color}/10 text-${flow.color} px-2 py-0.5 rounded-full font-mono`}>
                      {flow.trigger}
                    </span>
                  </div>
                  <p className="text-white/55 text-xs">{flow.event}</p>
                  <div className="flex gap-2 mt-2 text-[10px] text-white/25 font-mono">
                    <span>{flow.fromDetail}</span>
                    <span>â†’</span>
                    <span>{flow.toDetail}</span>
                  </div>
                </motion.div>
              ))}
            </div>
          </div>
        </div>
        <ErpBridge />
      </section>

      {/* â”€â”€ DECISÃ•ES TÃ‰CNICAS â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <p className="text-xs font-bold uppercase tracking-widest text-brand-success mb-4">Anatomia das DecisÃµes</p>
        <h2 className="text-3xl md:text-4xl font-black mb-4 text-white tracking-tight">
          Por que cada tecnologia foi escolhida?
        </h2>
        <p className="text-white/50 max-w-2xl mb-10 text-sm leading-relaxed">
          Cada escolha tÃ©cnica existe porque resolveu um problema de negÃ³cio especÃ­fico. Sem cargo de cult â€” sem &ldquo;a comunidade usa&rdquo;. Aqui estÃ¡ o raciocÃ­nio por trÃ¡s de cada decisÃ£o arquitetural.
        </p>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-5">
          {decisions.map((d) => {
            const Icon = d.icon
            return (
              <SpotlightCard key={d.title} className="p-6" spotlightColor="rgba(255,215,0,0.05)">
                <div className={`w-10 h-10 rounded-xl bg-${d.color}/10 border border-${d.color}/20 flex items-center justify-center mb-4`}>
                  <Icon className={`w-5 h-5 text-${d.color}`} />
                </div>
                <h3 className="text-white font-bold text-sm mb-2">{d.title}</h3>
                <p className="text-white/55 text-sm leading-relaxed">{d.desc}</p>
              </SpotlightCard>
            )
          })}
        </div>
      </section>

    </div>
  )
}


ARQUIVO: src\app\formacao\page.tsx

'use client'

import { motion } from 'framer-motion'
import { GraduationCap, Award, BookOpen, Layers } from 'lucide-react'
import { SpotlightCard } from '@/components/ui/SpotlightCard'

export default function FormacaoPage() {
  const experiences = [
    { year: "2024 - 2026", title: "PÃ³s-GraduaÃ§Ã£o LideranÃ§a e InovaÃ§Ã£o", org: "PUC-RS", desc: "GestÃ£o estratÃ©gica, lideranÃ§a matricial e cultura de inovaÃ§Ã£o corporativa.", color: "primary", icon: <Layers className="w-6 h-6" /> },
    { year: "2018 - 2022", title: "Ensino Superior (Engenharia de ComputaÃ§Ã£o)", org: "Facens", desc: "Fundamentos de algoritmos, arquitetura de sistemas e infraestrutura distribuÃ­da.", color: "blue", icon: <GraduationCap className="w-6 h-6" /> },
    { year: "2016 - 2017", title: "TÃ©cnico Administrativo", org: "Etec", desc: "GestÃ£o de negÃ³cios, noÃ§Ãµes financeiras e administraÃ§Ã£o empresarial base.", color: "success", icon: <BookOpen className="w-6 h-6" /> },
  ]

  const certs = [
    { id: "CERT-01", title: "Consultor Certificado Mercado Livre Platinum", tag: "E-Commerce", bg: "from-[#FFE600]/20 to-[#FFE600]/5", color: "#FFE600" },
    { id: "CERT-02", title: "AWS Cloud Explorer / Foundation", tag: "Infraestrutura", bg: "from-[#FF9900]/20 to-[#FF9900]/5", color: "#FF9900" },
    { id: "CERT-03", title: "Scrum Fundamentals Certified", tag: "Metodologia Ãgil", bg: "from-[#005B9F]/20 to-[#005B9F]/5", color: "#005B9F" },
    { id: "CERT-04", title: "Meta Blueprint Professional", tag: "Performance Ads", bg: "from-[#0668E1]/20 to-[#0668E1]/5", color: "#0668E1" }
  ]

  return (
    <div className="flex flex-col gap-32 pb-32">
      
      {/* BACKGROUND MESH EFFECT */}
      <div className="fixed inset-0 -z-50 pointer-events-none overflow-hidden flex justify-center items-center opacity-30">
        <div className="absolute w-[900px] h-[900px] bg-brand-success/10 blur-[130px] rounded-full mix-blend-screen animate-pulse-slow translate-x-1/2 -translate-y-1/2" />
        <div className="absolute w-[600px] h-[600px] bg-brand-primary/10 blur-[90px] rounded-full mix-blend-screen animate-pulse-slow translate-y-1/2 -translate-x-1/4" style={{ animationDelay: '3s' }} />
      </div>

      {/* HERO SECTION */}
      <section className="pt-32 container mx-auto px-6 relative z-10">
        <motion.div
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8, ease: "easeOut" }}
          className="max-w-4xl"
        >
          <span className="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-brand-primary/30 bg-brand-primary/5 text-brand-primary font-bold tracking-wider uppercase mb-6 text-sm shadow-[0_0_20px_rgba(255,215,0,0.2)]">
            <Award className="w-4 h-4" />
            Track Record
          </span>
          <h1 className="text-5xl md:text-7xl lg:text-8xl font-black tracking-tighter mb-8 text-white leading-[1.05]">
            FormaÃ§Ãµes &<br />
            <span className="text-transparent bg-clip-text bg-gradient-to-r from-brand-success to-emerald-200">CertificaÃ§Ãµes.</span>
          </h1>
          <p className="text-xl md:text-2xl text-foreground/70 max-w-3xl leading-relaxed font-light">
            A fundaÃ§Ã£o tÃ©cnica atrelada Ã  validaÃ§Ã£o de parceiros globais. Base acadÃªmica rigorosa aplicada em cenÃ¡rios de alta competitividade B2B.
          </p>
        </motion.div>
      </section>

      {/* TIMELINE DE FORMAÃ‡ÃƒO */}
      <section className="container mx-auto px-6 mt-12">
        <div className="mb-16">
          <h2 className="text-4xl md:text-5xl font-black mb-6 text-transparent bg-clip-text bg-gradient-to-br from-white to-white/40 tracking-tighter">
            EvoluÃ§Ã£o AcadÃªmica
          </h2>
        </div>

        <div className="relative pl-8 md:pl-0">
          {/* Vertical line timeline */}
          <div className="hidden md:block absolute left-1/2 top-4 bottom-4 w-1 bg-white/5 rounded-full -translate-x-1/2" />
          
          <div className="space-y-12">
            {experiences.map((exp, idx) => (
              <div key={idx} className={`relative flex flex-col md:flex-row items-center justify-between group cursor-default ${idx % 2 !== 0 ? 'md:flex-row-reverse' : ''}`}>
                
                {/* Center Node dot */}
                <div className="hidden md:flex absolute left-1/2 -translate-x-1/2 w-8 h-8 rounded-full border-4 border-[#0A0E27] shadow-[0_0_15px_rgba(var(--brand-primary-rgb),0.5)] z-20 transition-all duration-300 group-hover:scale-125" style={{ backgroundColor: `var(--color-brand-${exp.color})` }}>
                  {/* Ping effect on hover */}
                  <div className="absolute inset-0 rounded-full animate-ping opacity-0 group-hover:opacity-60" style={{ backgroundColor: `var(--color-brand-${exp.color})` }} />
                </div>

                {/* Content Box */}
                <div className="w-full md:w-[45%]">
                  <SpotlightCard className={`p-8 border-brand-${exp.color}/30`} spotlightColor={`rgba(var(--brand-${exp.color}-rgb), 0.15)`}>
                    <div className="flex justify-between items-center mb-6">
                      <span className={`text-xs font-bold uppercase tracking-widest text-brand-${exp.color} bg-brand-${exp.color}/10 px-3 py-1 rounded-full border border-brand-${exp.color}/30 shadow-[0_0_10px_rgba(var(--brand-${exp.color}-rgb),0.2)]`}>
                        {exp.year}
                      </span>
                      <div className={`text-brand-${exp.color} opacity-40 group-hover:opacity-100 transition-opacity`}>
                        {exp.icon}
                      </div>
                    </div>
                    
                    <span className="text-white/40 text-xs font-mono uppercase tracking-widest block mb-2">{exp.org}</span>
                    <h3 className="text-2xl font-bold text-white mb-3 shadow-black drop-shadow-md">{exp.title}</h3>
                    <p className="text-foreground/60 leading-relaxed font-light">{exp.desc}</p>
                  </SpotlightCard>
                </div>

                {/* Spacer for the other side */}
                <div className="hidden md:block w-full md:w-[45%]" />

              </div>
            ))}
          </div>
        </div>
      </section>
      
      {/* CHANCELAS OFICIAIS */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-32 mt-16">
         <div className="mb-16">
          <h2 className="text-4xl md:text-5xl font-black mb-6 text-transparent bg-clip-text bg-gradient-to-br from-white to-white/40 tracking-tighter">
            Chancelas & Validations
          </h2>
          <p className="text-xl text-foreground/60 max-w-2xl font-light">
            CertificaÃ§Ãµes de Big Techs e plataformas core garantindo implementaÃ§Ã£o com excelÃªncia arquitetural oficial.
          </p>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
          {certs.map((cert) => (
             <div 
               key={cert.id} 
               className={`relative overflow-hidden p-8 md:p-10 rounded-[2rem] border border-white/10 group bg-gradient-to-br ${cert.bg} hover:-translate-y-2 transition-transform duration-500`}
             >
               <div className="absolute top-0 right-0 w-32 h-32 opacity-20 blur-3xl pointer-events-none group-hover:opacity-60 transition-opacity duration-1000" style={{ backgroundColor: cert.color }} />
               
               <div className="flex justify-between items-start mb-12 relative z-10">
                 <div className="w-12 h-12 bg-black/40 rounded-2xl border border-white/10 flex items-center justify-center p-2.5 backdrop-blur-md">
                   {/* Fallback to simple icon since we don't have SVG strings, use Award for generic */}
                   <Award className="w-full h-full text-white/80" style={{ color: cert.color }} />
                 </div>
                 <span className="text-[10px] font-mono text-white/50 bg-black/50 px-3 py-1 rounded border border-white/5">{cert.id}</span>
               </div>
               
               <div className="relative z-10">
                 <span className="text-xs uppercase tracking-widest font-bold block mb-2" style={{ color: cert.color }}>{cert.tag}</span>
                 <h3 className="text-2xl font-black text-white">{cert.title}</h3>
               </div>
             </div>
          ))}
        </div>
      </section>

    </div>
  )
}


ARQUIVO: src\app\interfaces\page.tsx

'use client'

import { motion } from 'framer-motion'
import Link from 'next/link'
import { ArrowRight, LayoutTemplate, Smartphone, Layers, PenTool, Sparkles, Building2, ShoppingBag, Briefcase, CalendarClock, ChevronRight } from 'lucide-react'
import { SpotlightCard } from '@/components/ui/SpotlightCard'

export default function InterfacesPage() {
  return (
    <div className="flex flex-col gap-32 pb-32">
      
      {/* BACKGROUND MESH EFFECT */}
      <div className="fixed inset-0 -z-50 pointer-events-none overflow-hidden flex justify-center items-center opacity-30">
        <div className="absolute w-[800px] h-[800px] bg-brand-indigo/20 blur-[120px] rounded-full mix-blend-screen animate-pulse-slow translate-x-1/3 -translate-y-1/3" />
        <div className="absolute w-[600px] h-[600px] bg-brand-primary/10 blur-[100px] rounded-full mix-blend-screen animate-pulse-slow translate-y-1/3 -translate-x-1/3" style={{ animationDelay: '2s' }} />
      </div>

      {/* HERO SECTION */}
      <section className="pt-32 container mx-auto px-6 relative z-10">
        <motion.div
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8, ease: "easeOut" }}
          className="max-w-4xl"
        >
          <span className="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-brand-indigo/30 bg-brand-indigo/5 text-brand-indigo font-bold tracking-wider uppercase mb-6 text-sm">
            <LayoutTemplate className="w-4 h-4" />
            Engenharia de Interfaces B2B
          </span>
          <h1 className="text-5xl md:text-7xl lg:text-8xl font-black tracking-tighter mb-8 text-white leading-[1.05]">
            Design que <span className="text-transparent bg-clip-text bg-gradient-to-r from-brand-indigo to-purple-300">Converte e RetÃ©m.</span>
          </h1>
          <p className="text-xl md:text-2xl text-foreground/70 max-w-3xl leading-relaxed font-light mb-12">
            A estÃ©tica Ã© a embalagem da confianÃ§a Institucional. Interface nÃ£o Ã© arte, Ã© <strong>Engenharia Comportamental</strong> projetada para reduÃ§Ã£o de carga cognitiva e aquisiÃ§Ã£o de LTV.
          </p>
        </motion.div>
      </section>

      {/* INTERACTIVE PROTOTYPE: STUDIO LILYAN */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-32">
        <div className="mb-16">
          <h2 className="text-4xl md:text-5xl font-black mb-6 text-white tracking-tighter">
            Studio Lilyan (SaaS Prototype)
          </h2>
          <p className="text-xl text-foreground/60 max-w-3xl font-light">
            ExperiÃªncia Dark UI Interativa para gestÃ£o de agendamentos e prestaÃ§Ã£o de serviÃ§os premium. Foco absoluto em microinteraÃ§Ãµes fluidas e reduÃ§Ã£o de fricÃ§Ã£o.
          </p>
        </div>

        <SpotlightCard className="p-8 md:p-12 border-brand-indigo/30 flex flex-col items-center group overflow-hidden" spotlightColor="rgba(79, 70, 229, 0.15)">
           
           {/* Interactive Calendar UI Mockup */}
           <div className="w-full max-w-4xl bg-[#080808] border border-white/10 rounded-3xl shadow-[0_0_50px_rgba(79,70,229,0.1)] overflow-hidden flex flex-col md:flex-row relative z-10">
              
              {/* Sidebar Menu Fake */}
              <div className="hidden md:flex flex-col w-64 bg-white/5 border-r border-white/5 p-6 gap-6">
                 <div className="h-8 w-32 bg-white/10 rounded" />
                 <div className="space-y-4 mt-8">
                    {[1, 2, 3, 4].map((i) => (
                      <div key={i} className={`flex items-center gap-3 p-3 rounded-xl cursor-pointer transition-colors ${i === 1 ? 'bg-brand-indigo/20 text-brand-indigo font-bold border border-brand-indigo/30' : 'hover:bg-white/5 text-foreground/50'}`}>
                        <div className="w-5 h-5 rounded bg-current opacity-50" />
                        <span className="text-sm">{i === 1 ? 'Agendamentos' : 'Clientes'}</span>
                      </div>
                    ))}
                 </div>
              </div>

              {/* Main Content Area Fake */}
              <div className="flex-1 p-6 md:p-10">
                <div className="flex justify-between items-center mb-10">
                   <div>
                     <h3 className="text-2xl font-bold text-white">Agenda do Dia</h3>
                     <p className="text-foreground/50 text-sm">TerÃ§a-feira, 24 de Outubro</p>
                   </div>
                   <div className="w-10 h-10 rounded-full bg-brand-indigo/20 border border-brand-indigo flex items-center justify-center">
                     <CalendarClock className="w-5 h-5 text-brand-indigo" />
                   </div>
                </div>

                <div className="space-y-4 relative">
                   {/* Timeline Line */}
                   <div className="absolute left-[31px] top-4 bottom-4 w-px bg-white/10" />

                   {/* Appointments */}
                   {[
                     { time: "09:00", duration: "1h 30m", title: "Design de Sobrancelha Premium", client: "Amanda S.", color: "brand-indigo" },
                     { time: "11:00", duration: "45m", title: "ManutenÃ§Ã£o Lash", client: "Carolina T.", color: "brand-blue" },
                     { time: "14:30", duration: "2h", title: "MicropigmentaÃ§Ã£o Labial", client: "Juliana R.", color: "brand-primary" }
                   ].map((apt, idx) => (
                     <motion.div 
                       key={idx}
                       whileHover={{ x: 10, backgroundColor: 'rgba(255,255,255,0.08)' }}
                       className="flex items-start gap-6 p-4 rounded-2xl border border-transparent hover:border-white/10 transition-all cursor-pointer relative z-10"
                     >
                        <div className="w-16 text-right pt-2 font-mono text-sm text-foreground/50">{apt.time}</div>
                        
                        {/* Dot */}
                        <div className="relative pt-3 z-10">
                          <div className={`w-3 h-3 rounded-full bg-${apt.color} shadow-[0_0_10px_rgba(var(--${apt.color}-rgb),0.8)] ring-4 ring-[#080808]`} />
                        </div>

                        <div className={`flex-1 bg-${apt.color}/5 border border-${apt.color}/20 p-4 rounded-xl`}>
                          <div className="flex justify-between items-start mb-2">
                            <h4 className="text-white font-bold">{apt.title}</h4>
                            <span className="text-[10px] uppercase font-mono tracking-wider text-white/50 bg-white/10 px-2 py-0.5 rounded">{apt.duration}</span>
                          </div>
                          <p className={`text-sm text-${apt.color} font-medium flex items-center gap-2`}>
                            {apt.client} <ChevronRight className="w-3 h-3" />
                          </p>
                        </div>
                     </motion.div>
                   ))}
                </div>
              </div>

           </div>
        </SpotlightCard>
      </section>

      {/* WEB SHOWCASE (BENTO GRID) */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-32">
        <div className="mb-16">
          <h2 className="text-4xl md:text-5xl font-black mb-6 text-white tracking-tighter">
            Web Showcases & Acervos Realizados
          </h2>
          <p className="text-xl text-foreground/60 max-w-2xl font-light">
            Engenharia Front-end aplicada a cenÃ¡rios industriais, varejo e consultoria jurÃ­dica. SoluÃ§Ãµes que entregam performance mensurÃ¡vel (Lighthouse &gt; 95%).
          </p>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-3 gap-6 auto-rows-[minmax(300px,_auto)]">
          
          {/* BENTO ITEM 1: JAT PLASTIC */}
          <SpotlightCard className="p-8 border-white/10 md:col-span-2 group overflow-hidden" spotlightColor="rgba(255, 255, 255, 0.05)">
            <div className="w-12 h-12 rounded-xl bg-white/5 flex items-center justify-center mb-6">
              <Building2 className="w-6 h-6 text-foreground/70" />
            </div>
            <h3 className="text-2xl font-bold text-white mb-2 group-hover:text-brand-indigo transition-colors">IndÃºstria Jat Plastic</h3>
            <span className="text-xs uppercase font-mono tracking-widest text-brand-indigo bg-brand-indigo/10 px-2 py-1 rounded inline-block mb-4">CatÃ¡logo B2B / Next.js</span>
            
            <p className="text-foreground/60 text-sm leading-relaxed max-w-md">
              ReformulaÃ§Ã£o completa do braÃ§o digital da indÃºstria. Vitrine tÃ©cnica B2B estruturada para aquisiÃ§Ã£o SEO de grandes distribuidores com foco em conversÃ£o de orÃ§amentos complexos.
            </p>

            {/* Faux Code Snippet / Perf Metrics */}
            <div className="mt-8 bg-black/50 p-4 rounded-xl border border-white/5 font-mono text-[10px] text-brand-success/70 flex gap-4">
              <div><span className="text-white/30">PERF:</span> 98/100</div>
              <div><span className="text-white/30">SEO:</span> 100/100</div>
              <div><span className="text-white/30">LCP:</span> 0.8s</div>
            </div>
          </SpotlightCard>

          {/* BENTO ITEM 2: MERICE */}
          <SpotlightCard className="p-8 border-white/10 group overflow-hidden relative" spotlightColor="rgba(255, 215, 0, 0.1)">
             <div className="absolute top-0 right-0 w-32 h-32 bg-brand-primary/10 blur-3xl rounded-full" />
             <div className="w-12 h-12 rounded-xl bg-brand-primary/10 flex items-center justify-center mb-6 relative z-10">
              <ShoppingBag className="w-6 h-6 text-brand-primary" />
            </div>
            <h3 className="text-xl font-bold text-white mb-2 group-hover:text-brand-primary transition-colors relative z-10">E-commerce Merice</h3>
            <span className="text-xs uppercase font-mono tracking-widest text-brand-primary bg-brand-primary/10 px-2 py-1 rounded inline-block mb-4 relative z-10">Shopify / Liquid</span>
            
            <p className="text-foreground/60 text-sm leading-relaxed relative z-10">
              Arquitetura de varejo digital D2C desenhada milimetricamente para o nicho de moda Ã­ntima. Taxa de conversÃ£o sustentÃ¡vel e fluxos de abandono de carrinho.
            </p>
          </SpotlightCard>

          {/* BENTO ITEM 3: JMARTINEZ */}
          <SpotlightCard className="p-8 border-white/10 md:col-span-3 lg:col-span-1 group overflow-hidden bg-gradient-to-br hover:from-brand-blue/5 hover:to-transparent transition-colors" spotlightColor="rgba(0, 112, 243, 0.1)">
            <div className="w-12 h-12 rounded-xl bg-brand-blue/10 flex items-center justify-center mb-6">
              <Briefcase className="w-6 h-6 text-brand-blue" />
            </div>
            <h3 className="text-xl font-bold text-white mb-2 group-hover:text-brand-blue transition-colors">JMartinez Advogados</h3>
            <span className="text-xs uppercase font-mono tracking-widest text-brand-blue bg-brand-blue/10 px-2 py-1 rounded inline-block mb-4">Authority Hub</span>
            
            <p className="text-foreground/60 text-sm leading-relaxed">
              Posicionamento Institucional focado em credibilidade e geraÃ§Ã£o de autoridade. Design monolÃ­tico, carregamento ultra-rÃ¡pido e funis limpos para captura de leads "High Ticket".
            </p>
          </SpotlightCard>

        </div>
      </section>

    </div>
  )
}


ARQUIVO: src\app\plataformas\page.tsx

'use client'

import { motion } from 'framer-motion'
import { Server, Grid, Database, Cloud, Zap } from 'lucide-react'
import { SpotlightCard } from '@/components/ui/SpotlightCard'

export default function PlataformasPage() {
  const stack = [
    { 
      category: "Frontend & UI Core", 
      icon: <Grid className="w-6 h-6 text-brand-indigo" />, 
      color: "brand-indigo",
      items: ["React", "Next.js", "TypeScript", "Tailwind CSS", "Framer Motion"] 
    },
    { 
      category: "Backend & Estrutura", 
      icon: <Server className="w-6 h-6 text-brand-primary" />,
      color: "brand-primary", 
      items: ["Node.js", "Python", "NestJS", "Express"] 
    },
    { 
      category: "Bancos & MemÃ³ria", 
      icon: <Database className="w-6 h-6 text-brand-blue" />, 
      color: "brand-blue",
      items: ["PostgreSQL", "Prisma ORM", "Redis", "Supabase"] 
    },
    { 
      category: "Cloud & Devops", 
      icon: <Cloud className="w-6 h-6 text-white" />, 
      color: "white",
      items: ["AWS", "Vercel", "Docker", "Git/GitHub Actions"] 
    },
    { 
      category: "ERPs & Hubs LogÃ­sticos", 
      icon: <Zap className="w-6 h-6 text-brand-success" />, 
      color: "brand-success",
      items: ["Microsoft Dynamics", "Bling ERP", "Kangu", "Melhor Envio", "Plugg.to"] 
    }
  ]

  return (
    <div className="flex flex-col gap-32 pb-32">
       {/* BACKGROUND MESH EFFECT */}
       <div className="fixed inset-0 -z-50 pointer-events-none overflow-hidden flex justify-center items-center opacity-30">
        <div className="absolute w-[800px] h-[800px] bg-brand-blue/20 blur-[120px] rounded-full mix-blend-screen animate-pulse-slow translate-x-1/2 -translate-y-1/2" />
      </div>

      {/* HERO SECTION */}
      <section className="pt-32 container mx-auto px-6 relative z-10">
        <motion.div
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8, ease: "easeOut" }}
          className="max-w-4xl"
        >
          <span className="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-brand-blue/30 bg-brand-blue/5 text-brand-blue font-bold tracking-wider uppercase mb-6 text-sm shadow-[0_0_20px_rgba(0,112,243,0.2)]">
            <Server className="w-4 h-4" />
            Ecosystem Stack
          </span>
          <h1 className="text-5xl md:text-7xl lg:text-8xl font-black tracking-tighter mb-8 text-white leading-[1.05]">
            Plataformas &<br />
            <span className="text-transparent bg-clip-text bg-gradient-to-r from-brand-blue to-cyan-300">Frameworks Corporativos.</span>
          </h1>
          <p className="text-xl md:text-2xl text-foreground/70 max-w-3xl leading-relaxed font-light">
            Arquitetura de sistemas agnÃ³stica. Cada ecossistema dita suas ferramentas, garantindo escalabilidade, seguranÃ§a e integridade transacional das pontas transacionais ao banco de dados.
          </p>
        </motion.div>
      </section>

      {/* STACK GRID */}
      <section className="container mx-auto px-6 mt-12">
        <div className="mb-16">
          <h2 className="text-4xl md:text-5xl font-black mb-6 text-transparent bg-clip-text bg-gradient-to-br from-white to-white/40 tracking-tighter">
            Toolkit de Engenharia
          </h2>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {stack.map((group, idx) => {
             const isWide = idx >= 3;
             return (
              <SpotlightCard 
                key={idx} 
                className={`p-8 border-current/10 ${isWide ? 'md:col-span-2 lg:col-span-3' : ''}`}
                spotlightColor={`rgba(var(--${group.color}-rgb), 0.15)`}
              >
                  <div className="flex items-center gap-4 border-b border-white/10 pb-6 mb-6">
                     <div className={`p-3 bg-${group.color}/10 border border-${group.color}/30 rounded-xl shadow-[0_0_15px_rgba(var(--${group.color}-rgb),0.2)]`}>
                        {group.icon}
                     </div>
                     <h3 className="text-2xl font-bold text-white">{group.category}</h3>
                  </div>

                  <div className="flex flex-wrap gap-3">
                     {group.items.map(item => (
                       <span 
                         key={item} 
                         className={`px-4 py-2 rounded-lg font-bold text-sm tracking-wide bg-[#0A0E27]/50 border border-white/10 hover:border-${group.color}/50 hover:bg-${group.color}/10 transition-colors cursor-default`}
                       >
                         {item}
                       </span>
                     ))}
                  </div>
              </SpotlightCard>
             )
          })}
        </div>
      </section>

    </div>
  )
}


ARQUIVO: src\app\processos\page.tsx

'use client'

import { motion } from 'framer-motion'
import {
  Bot, FileText, Zap, ArrowRight, Users, Brain,
  MessageSquare, CheckCircle2, Clock, AlertTriangle,
  GitBranch, RefreshCw, Target, Phone, Package,
  BarChart3, Workflow, BookOpen, Layers
} from 'lucide-react'
import { SpotlightCard } from '@/components/ui/SpotlightCard'
import { LiveSvgFlowchart } from '@/components/interactive/LiveSvgFlowchart'
import { ComplexSacFlow } from '@/components/interactive/ComplexSacFlow'

// â”€â”€â”€ BOM DE COMPRAS / EDUSEN LEAD JOURNEY â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const leadJourney = [
  {
    step: '01', label: 'Lead Captado', icon: Target, color: 'brand-primary',
    tool: 'Edusen CRM',
    desc: 'Lead entra via formulÃ¡rio de captaÃ§Ã£o (Google Ads / OrgÃ¢nico). O Edusen registra automaticamente nome, telefone, origem e histÃ³rico de navegaÃ§Ã£o.',
    automation: 'n8n webhook dispara em < 30s apÃ³s captaÃ§Ã£o'
  },
  {
    step: '02', label: 'QualificaÃ§Ã£o AutomÃ¡tica', icon: Brain, color: 'brand-blue',
    tool: 'IA + n8n',
    desc: 'Agente de IA (via OpenAI API) analisa o perfil do lead: histÃ³rico de compras, produto de interesse, localizaÃ§Ã£o e ticket potencial. Classifica como Hot/Warm/Cold.',
    automation: 'Score calculado em < 5s Â· Sem intervenÃ§Ã£o humana'
  },
  {
    step: '03', label: 'DistribuiÃ§Ã£o por Score', icon: GitBranch, color: 'brand-success',
    tool: 'n8n Router',
    desc: 'Hot leads sÃ£o encaminhados imediatamente para o SDR com alerta no WhatsApp. Warm leads entram em sequÃªncia de nutriÃ§Ã£o automatizada. Cold leads recebem conteÃºdo educacional.',
    automation: 'Regras de roteamento no n8n com 4 condicionais'
  },
  {
    step: '04', label: 'Proposta e NegociaÃ§Ã£o', icon: FileText, color: 'brand-indigo',
    tool: 'Bom de Compras',
    desc: 'SDR acessa o Bom de Compras com contexto completo do lead. Proposta gerada com template padronizado. HistÃ³rico de interaÃ§Ãµes consolidado em uma Ãºnica view.',
    automation: 'Template de proposta automÃ¡tico via Google Docs API'
  },
  {
    step: '05', label: 'Fechamento e CRM', icon: CheckCircle2, color: 'brand-success',
    tool: 'Edusen + Bling',
    desc: 'Contrato assinado via DocuSign. Bling cria o pedido automaticamente via webhook do Edusen. Status sincronizado em ambos os sistemas.',
    automation: 'Zero entrada manual de dados apÃ³s fechamento'
  },
]

// â”€â”€â”€ N8N AUTOMATION FLOWS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const automations = [
  {
    name: 'SAC Triagem AutomÃ¡tica',
    icon: MessageSquare,
    color: 'brand-primary',
    trigger: 'Webhook Mercado Livre',
    steps: 4,
    desc: 'Quando uma mensagem chega no ML, o n8n classifica automaticamente por tipo (dÃºvida, reclamaÃ§Ã£o, devoluÃ§Ã£o), aciona o template de resposta correto e registra na planilha de SAC. Somente reclamaÃ§Ãµes com score alto chegam ao atendente humano.',
    nodes: ['ML Webhook â†’ ClassificaÃ§Ã£o IA â†’ Template Selector â†’ Resposta + Log']
  },
  {
    name: 'SincronizaÃ§Ã£o de Estoque',
    icon: Package,
    color: 'brand-blue',
    trigger: 'Cron 30min',
    steps: 5,
    desc: 'A cada 30 minutos, o n8n puxa o estoque atual do Bling via API, compara com os volumes nos canais (ML, Amazon, Magalu) e dispara atualizaÃ§Ãµes somente para os SKUs com divergÃªncia. Evita o throttling da API por chamadas desnecessÃ¡rias.',
    nodes: ['Cron â†’ Bling API â†’ Diff Engine â†’ ML API + Amazon API + Magalu API â†’ Log']
  },
  {
    name: 'RelatÃ³rio Semanal KPIs',
    icon: BarChart3,
    color: 'brand-success',
    trigger: 'Toda segunda-feira 07:00',
    steps: 3,
    desc: 'O Make consolida dados de ACOS/ROAS do ML ADS, faturamento do Bling e mÃ©tricas de atendimento da planilha SAC. Gera um relatÃ³rio em Google Sheets formatado e envia por WhatsApp e e-mail para os responsÃ¡veis.',
    nodes: ['Make Schedule â†’ ML API + Bling API + Sheets â†’ FormataÃ§Ã£o â†’ WhatsApp + Email']
  },
  {
    name: 'Follow-up PÃ³s-Compra',
    icon: Phone,
    color: 'brand-indigo',
    trigger: 'Webhook Pedido Entregue',
    steps: 4,
    desc: 'Quando o carrier confirma entrega, o n8n aguarda 48h e dispara automaticamente uma mensagem de follow-up via WhatsApp solicitando avaliaÃ§Ã£o. Se o comprador nÃ£o avalia em 24h, envia um segundo toque mais direto.',
    nodes: ['Carrier Webhook â†’ 48h Wait â†’ WhatsApp Msg â†’ 24h Check â†’ WhatsApp Retry']
  },
]

// â”€â”€â”€ BPO FINANCEIRO PROCESS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const bpoFinanceiro = [
  {
    area: 'Contas a Pagar',
    icon: FileText,
    color: 'brand-primary',
    processo: [
      'Recebimento de NF via e-mail â†’ extraÃ§Ã£o automÃ¡tica de dados via OCR (Make)',
      'ValidaÃ§Ã£o contra pedido de compra no sistema interno',
      'LanÃ§amento no Bling (contas a pagar com vencimento e centro de custo)',
      'AprovaÃ§Ã£o do gestor via WhatsApp para valores > R$ 5.000',
      'ProgramaÃ§Ã£o de pagamento no banco com 2 dias de antecedÃªncia',
    ]
  },
  {
    area: 'Contas a Receber',
    icon: BarChart3,
    color: 'brand-blue',
    processo: [
      'Faturamento gerado automaticamente pelo Bling apÃ³s despacho confirmado',
      'NFe enviada por e-mail ao comprador + registro no XML da SEFAZ',
      'CobranÃ§a automÃ¡tica via WhatsApp para parcelas vencidas hÃ¡ > 3 dias',
      'ConciliaÃ§Ã£o bancÃ¡ria semanal via OFX importado no Bling',
      'RelatÃ³rio de inadimplÃªncia gerado automaticamente toda segunda-feira',
    ]
  },
  {
    area: 'ConciliaÃ§Ã£o & RelatÃ³rios',
    icon: RefreshCw,
    color: 'brand-success',
    processo: [
      'DRE mensal consolidado direto do Bling (categorias personalizadas por CC)',
      'Confronto entre NF emitida e recebimento bancÃ¡rio â€” divergÃªncias alertadas',
      'ExportaÃ§Ã£o para Excel formatado para o contador (layout especÃ­fico por escritÃ³rio)',
      'Demonstrativo de ADS por canal (ML, Amazon) consolidado no relatÃ³rio de CMV',
    ]
  },
]

// â”€â”€â”€ TEAM TRAINING METHODOLOGY â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const trainingMethod = [
  { step: '1', title: 'Mapeamento', desc: 'Entrevista com a equipe para mapear o processo atual (as-is). IdentificaÃ§Ã£o de gargalos e inconsistÃªncias com perguntas estruturadas.' },
  { step: '2', title: 'DocumentaÃ§Ã£o de SOPs', desc: 'RedaÃ§Ã£o dos Procedimentos Operacionais PadrÃ£o em formato visual (fluxograma) + texto explicativo. Cada SOP tem no mÃ¡ximo 5 passos por tela.' },
  { step: '3', title: 'Treinamento PrÃ¡tico', desc: 'Workshop hands-on de 2h usando casos reais. TÃ©cnica de "shadowing reverso": o treinador faz primeiro, o treinando replica com feedback imediato.' },
  { step: '4', title: 'ValidaÃ§Ã£o de AdesÃ£o', desc: 'Auditoria semanal durante 30 dias: amostragem de 10% das ocorrÃªncias para verificar conformidade com o SOP. Desvios geram micro-treinamento individual.' },
  { step: '5', title: 'Melhoria ContÃ­nua (PDCA)', desc: 'Ciclo mensal: coleta de feedbacks da equipe sobre os SOPs, atualizaÃ§Ã£o conforme cases novos, e revisÃ£o dos gatilhos de automaÃ§Ã£o no n8n.' },
]

export default function ProcessosPage() {
  return (
    <div className="flex flex-col gap-28 pb-32">

      {/* BACKGROUND */}
      <div className="fixed inset-0 -z-50 pointer-events-none overflow-hidden opacity-20">
        <div className="absolute w-[800px] h-[800px] bg-brand-blue/20 blur-[130px] rounded-full -top-1/4 right-0" />
        <div className="absolute w-[600px] h-[600px] bg-brand-primary/10 blur-[110px] rounded-full bottom-0 left-0" />
      </div>

      {/* â”€â”€ HERO â”€â”€ */}
      <section className="pt-32 container mx-auto px-6">
        <motion.div
          initial={{ opacity: 0, y: 28 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8 }}
          className="max-w-5xl"
        >
          <div className="flex flex-wrap gap-3 mb-8">
            <span className="inline-flex items-center gap-2 px-3 py-1.5 rounded-full border border-brand-blue/30 bg-brand-blue/5 text-brand-blue font-bold tracking-wider uppercase text-xs">
              <Bot className="w-3.5 h-3.5" />
              Case Study â€” Processos & AutomaÃ§Ã£o
            </span>
            <span className="text-white/20 text-xs font-mono self-center">n8n Â· Make Â· OpenAI Â· BPO Financeiro Â· SOPs</span>
          </div>

          <h1 className="text-5xl md:text-7xl font-black tracking-tighter mb-6 text-white leading-[1.02]">
            Processos que<br />
            <span className="text-transparent bg-clip-text bg-gradient-to-r from-brand-blue to-cyan-300">
              funcionam sem vocÃª.
            </span>
          </h1>

          <p className="text-xl text-white/55 max-w-3xl leading-relaxed font-light">
            AutomaÃ§Ã£o real nÃ£o Ã© substituir pessoas â€” Ã© eliminar o trabalho braÃ§al repetitivo para que as pessoas possam fazer o que sÃ³ elas sabem: julgar, persuadir e decidir. Cada fluxo aqui foi desenhado com essa filosofia.
          </p>
        </motion.div>
      </section>

      {/* â”€â”€ CASE: BOM DE COMPRAS / EDUSEN LEAD JOURNEY â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <p className="text-xs font-bold uppercase tracking-widest text-brand-primary mb-4">01 â€” Case Bom de Compras + Edusen</p>
        <h2 className="text-3xl md:text-4xl font-black mb-4 text-white tracking-tight">
          DigitalizaÃ§Ã£o da Jornada do Lead
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-8 mb-12">
          <div>
            <p className="text-white/60 leading-relaxed text-sm mb-4">
              <strong className="text-white">O contexto:</strong> A operaÃ§Ã£o comercial do Bom de Compras dependia de planilhas manuais para controlar leads, follow-ups por WhatsApp sem histÃ³rico centralizado, e propostas geradas em Word copiadas de modelo em modelo. O tempo mÃ©dio de primeiro contato apÃ³s captaÃ§Ã£o era de 4-6 horas.
            </p>
            <p className="text-white/60 leading-relaxed text-sm mb-4">
              <strong className="text-white">O que foi implantado:</strong> Integrei o Edusen CRM com o sistema de captaÃ§Ã£o via n8n. Leads classificados automaticamente por IA em atÃ© 30 segundos. Hot leads recebem prioridade imediata. Toda a jornada â€” desde a captaÃ§Ã£o atÃ© o fechamento â€” acontece dentro de um Ãºnico pipeline visual no CRM, sem planilhas auxiliares.
            </p>
            <p className="text-white/60 leading-relaxed text-sm">
              <strong className="text-white">Metodologia de implementaÃ§Ã£o:</strong> Mapeei o processo atual com os SDRs (3 entrevistas individuais de 1h), documentei o SOP de cadÃªncia de contatos, treinei a equipe em 2 workshops e implantei as automaÃ§Ãµes em fases de 2 semanas cada.
            </p>
          </div>
          <div className="space-y-3">
            {leadJourney.map((step, i) => {
              const Icon = step.icon
              return (
                <motion.div
                  key={step.step}
                  initial={{ opacity: 0, x: 20 }}
                  whileInView={{ opacity: 1, x: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: i * 0.07 }}
                  className="flex gap-4 p-4 rounded-xl border border-white/5 bg-white/[0.02]"
                >
                  <div className={`w-8 h-8 rounded-lg bg-${step.color}/10 border border-${step.color}/20 flex items-center justify-center shrink-0`}>
                    <Icon className={`w-4 h-4 text-${step.color}`} />
                  </div>
                  <div className="min-w-0">
                    <div className="flex items-center gap-2 mb-0.5">
                      <span className={`text-[10px] font-mono text-${step.color}`}>{step.step}</span>
                      <span className="text-white font-bold text-sm">{step.label}</span>
                      <span className="text-[10px] text-white/25 font-mono">{step.tool}</span>
                    </div>
                    <p className="text-white/50 text-xs leading-relaxed">{step.desc}</p>
                    <p className={`text-[10px] text-${step.color}/70 font-mono mt-1`}>âš¡ {step.automation}</p>
                  </div>
                </motion.div>
              )
            })}
          </div>
        </div>
        <LiveSvgFlowchart />
      </section>

      {/* â”€â”€ SAC DECISION TREE â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <p className="text-xs font-bold uppercase tracking-widest text-brand-primary mb-4">02 â€” SAC & OperaÃ§Ãµes DiÃ¡rias</p>
        <h2 className="text-3xl md:text-4xl font-black mb-4 text-white tracking-tight">
          Ãrvore de DecisÃ£o SAC â€” Rotina Real
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-8 mb-12">
          <div>
            <p className="text-white/60 leading-relaxed text-sm mb-4">
              <strong className="text-white">Como mapiei os SOPs:</strong> Durante 2 semanas, acompanhei as atendentes registrando cada tipo de ocorrÃªncia e o fluxo de decisÃ£o usado. Identifiquei 12 tipos recorrentes, dos quais 3 respondiam por 80% do volume: Chamados de etiqueta no ML, ocorrÃªncias de transportadora e reclamaÃ§Ãµes de prazo.
            </p>
            <p className="text-white/60 leading-relaxed text-sm mb-4">
              <strong className="text-white">DocumentaÃ§Ã£o e treinamento:</strong> Cada SOP foi desenhado em um fluxograma visual com no mÃ¡ximo 5 passos e disponibilizado na intranet da equipe. O treinamento foi hands-on: a atendente resolvia um caso real seguindo o fluxograma enquanto eu observava, corrigindo em tempo real.
            </p>
            <p className="text-white/60 leading-relaxed text-sm">
              <strong className="text-white">AutomaÃ§Ã£o n8n:</strong> Os passos mais repetitivos de cada SOP foram automatizados: geraÃ§Ã£o de declaraÃ§Ã£o de extravio, envio automÃ¡tico de mensagem padrÃ£o pÃ³s-mediaÃ§Ã£o, e registro de ocorrÃªncias na planilha de auditoria semanal. A triagem N1 (menos de 12h de SLA) Ã© inteiramente gerenciada por agente de IA antes de chegar ao humano.
            </p>
          </div>
          <SpotlightCard className="p-6" spotlightColor="rgba(0,112,243,0.08)">
            <p className="text-xs font-bold uppercase tracking-widest text-brand-blue mb-5">Fluxo de Triagem N1 â€” n8n</p>
            <div className="space-y-3 font-mono text-xs">
              {[
                { label: 'Trigger', value: 'Webhook ML Mensagem', color: 'brand-primary' },
                { label: 'ClassificaÃ§Ã£o', value: 'IA â†’ tipo + urgÃªncia + tom', color: 'brand-blue' },
                { label: 'Roteamento', value: 'Score < 60 â†’ Template auto', color: 'brand-success' },
                { label: 'ExceÃ§Ã£o', value: 'Score â‰¥ 60 â†’ Humano + alerta', color: 'red-400' },
                { label: 'Log', value: 'Sheets: ocorrÃªncia + resoluÃ§Ã£o + SLA', color: 'brand-indigo' },
              ].map((row) => (
                <div key={row.label} className="flex items-center gap-3">
                  <span className={`text-[10px] w-24 shrink-0 text-${row.color} uppercase tracking-widest`}>{row.label}</span>
                  <ArrowRight className="w-3 h-3 text-white/20 shrink-0" />
                  <span className="text-white/55">{row.value}</span>
                </div>
              ))}
            </div>
            <div className="mt-5 pt-4 border-t border-white/5 flex items-center gap-2">
              <Zap className="w-4 h-4 text-brand-primary" />
              <p className="text-xs text-white/40">Tempo mÃ©dio de triagem N1: &lt; 45 segundos</p>
            </div>
          </SpotlightCard>
        </div>
        <ComplexSacFlow />
      </section>

      {/* â”€â”€ N8N AUTOMATIONS â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <p className="text-xs font-bold uppercase tracking-widest text-brand-success mb-4">03 â€” AutomaÃ§Ãµes Ativas</p>
        <h2 className="text-3xl md:text-4xl font-black mb-4 text-white tracking-tight">
          Fluxos n8n / Make em ProduÃ§Ã£o
        </h2>
        <p className="text-white/50 max-w-2xl mb-10 text-sm leading-relaxed">
          Estes sÃ£o os 4 fluxos de automaÃ§Ã£o mais crÃ­ticos atualmente ativos. Cada um substituiu uma tarefa manual recorrente â€” e agora funciona 24/7 sem intervenÃ§Ã£o.
        </p>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-5">
          {automations.map((auto) => {
            const Icon = auto.icon
            return (
              <SpotlightCard key={auto.name} className="p-6" spotlightColor="rgba(255,215,0,0.05)">
                <div className="flex items-start justify-between mb-4">
                  <div className={`w-10 h-10 rounded-xl bg-${auto.color}/10 border border-${auto.color}/20 flex items-center justify-center`}>
                    <Icon className={`w-5 h-5 text-${auto.color}`} />
                  </div>
                  <div className="text-right">
                    <p className={`text-[10px] font-mono text-${auto.color}`}>{auto.trigger}</p>
                    <p className="text-white/25 text-[10px]">{auto.steps} nodes</p>
                  </div>
                </div>
                <h3 className="text-white font-bold mb-2">{auto.name}</h3>
                <p className="text-white/55 text-sm leading-relaxed mb-4">{auto.desc}</p>
                <div className="bg-black/50 rounded-xl p-3 font-mono text-[10px] text-white/35 leading-relaxed overflow-x-auto">
                  {auto.nodes[0]}
                </div>
              </SpotlightCard>
            )
          })}
        </div>
      </section>

      {/* â”€â”€ BPO FINANCEIRO â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <p className="text-xs font-bold uppercase tracking-widest text-brand-indigo mb-4">04 â€” BPO Financeiro</p>
        <h2 className="text-3xl md:text-4xl font-black mb-4 text-white tracking-tight">
          Backoffice Financeiro Integrado
        </h2>
        <p className="text-white/55 max-w-2xl mb-10 text-sm leading-relaxed">
          Estruturei o backoffice financeiro completo: contas a pagar, contas a receber e conciliaÃ§Ã£o bancÃ¡ria â€” todos integrados ao Bling e com automaÃ§Ãµes Make reduzindo o trabalho manual em mais de 60% do tempo.
        </p>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-5">
          {bpoFinanceiro.map((area) => {
            const Icon = area.icon
            return (
              <div key={area.area} className="p-6 rounded-2xl border border-white/5 bg-white/[0.02]">
                <div className={`w-9 h-9 rounded-xl bg-${area.color}/10 flex items-center justify-center mb-4`}>
                  <Icon className={`w-4 h-4 text-${area.color}`} />
                </div>
                <h3 className="text-white font-bold mb-4">{area.area}</h3>
                <ul className="space-y-2.5">
                  {area.processo.map((p, i) => (
                    <li key={i} className="flex items-start gap-2 text-xs text-white/50 leading-relaxed">
                      <span className={`text-${area.color} font-mono shrink-0 mt-0.5`}>â†’</span>
                      {p}
                    </li>
                  ))}
                </ul>
              </div>
            )
          })}
        </div>
      </section>

      {/* â”€â”€ TRAINING METHODOLOGY â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <p className="text-xs font-bold uppercase tracking-widest text-brand-primary mb-4">05 â€” Metodologia de Treinamento</p>
        <h2 className="text-3xl md:text-4xl font-black mb-4 text-white tracking-tight">
          Como Treino Equipes em SOPs
        </h2>
        <p className="text-white/50 max-w-2xl mb-10 text-sm leading-relaxed">
          Documentar processos Ã© metade do trabalho. A outra metade â€” garantir que a equipe adere â€” exige uma metodologia prÃ³pria de treinamento e verificaÃ§Ã£o contÃ­nua.
        </p>
        <div className="relative">
          <div className="absolute left-6 top-0 bottom-0 w-px bg-white/5 hidden md:block" />
          <div className="space-y-4">
            {trainingMethod.map((item, i) => (
              <motion.div
                key={item.step}
                initial={{ opacity: 0, x: -20 }}
                whileInView={{ opacity: 1, x: 0 }}
                viewport={{ once: true }}
                transition={{ delay: i * 0.08 }}
                className="relative flex gap-6 md:pl-4"
              >
                <div className="hidden md:flex w-12 h-12 rounded-full shrink-0 items-center justify-center border z-10 bg-brand-primary/10 border-brand-primary/30 font-black text-brand-primary text-sm">
                  {item.step}
                </div>
                <div className="flex-1 p-5 rounded-2xl border border-white/5 bg-white/[0.02]">
                  <h3 className="text-white font-bold mb-1">{item.title}</h3>
                  <p className="text-white/55 text-sm leading-relaxed">{item.desc}</p>
                </div>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

    </div>
  )
}


ARQUIVO: src\app\sobre\page.tsx

'use client'

import { motion } from 'framer-motion'
import Link from 'next/link'
import { ArrowRight, Terminal } from 'lucide-react'
import { SpotlightCard } from '@/components/ui/SpotlightCard'

export default function SobrePage() {
  return (
    <div className="flex flex-col gap-32 pb-32">
      
      {/* BACKGROUND MESH EFFECT */}
      <div className="fixed inset-0 -z-50 pointer-events-none overflow-hidden flex justify-center items-center opacity-30">
        <div className="absolute w-[800px] h-[800px] bg-brand-primary/10 blur-[120px] rounded-full mix-blend-screen animate-pulse-slow translate-x-1/3 -translate-y-1/3" />
      </div>

      {/* HERO SECTION */}
      <section className="pt-32 container mx-auto px-6 relative z-10">
        <motion.div
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8, ease: "easeOut" }}
          className="max-w-5xl"
        >
          <span className="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-brand-primary/30 bg-brand-primary/5 text-brand-primary font-bold tracking-wider uppercase mb-6 text-sm">
            <Terminal className="w-4 h-4" />
            Engineer & Head
          </span>
          <h1 className="text-5xl md:text-7xl lg:text-8xl font-black tracking-tighter mb-8 text-white leading-[1.05]">
             Prazer, Eduardo. <br />
            <span className="text-transparent bg-clip-text bg-gradient-to-r from-white to-white/40">Arquiteto de Escala.</span>
          </h1>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-12 mt-12">
            <p className="text-xl md:text-2xl text-foreground/80 leading-relaxed font-light">
               Eu nÃ£o escrevo cÃ³digo apenas para rodar aplicaÃ§Ãµes. Eu projeto maquinÃ¡rios digitais que substituem humanos em processos transacionais repetitivos.
            </p>
            <div className="bg-white/5 border border-white/10 p-8 rounded-3xl glass backdrop-blur-xl">
               <strong className="text-brand-primary font-bold tracking-widest uppercase text-xs block mb-4">Background</strong>
               <p className="text-foreground/60 leading-relaxed mb-4">
                 SÃ³lida experiÃªncia liderando o nÃºcleo tÃ©cnico de gigantes do E-commerce (operaÃ§Ãµes +7 dÃ­gitos mensais). 
               </p>
               <p className="text-foreground/60 leading-relaxed">
                 Minha visÃ£o combina a rigorosidade fiduciÃ¡ria de sistemas legado (ERP) Ã  velocidade moderna de Hubs SaaS e front-ends em Next.js.
               </p>
            </div>
          </div>
        </motion.div>
      </section>

      {/* METHODOLOGY TIMELINE */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-32">
        <div className="mb-16 text-center">
          <h2 className="text-4xl md:text-5xl font-black mb-6 text-transparent bg-clip-text bg-gradient-to-br from-white to-white/40 tracking-tighter">
            MÃ©todo de Acoplamento
          </h2>
          <p className="text-xl text-foreground/60 max-w-2xl font-light mx-auto">
            Como conectamos uma operaÃ§Ã£o em ruÃ­nas Ã  era dos sistemas sÃ­ncronos.
          </p>
        </div>

        <div className="max-w-4xl mx-auto space-y-8">
          {[
            { tag: "Fase 1", title: "DiagnÃ³stico e Mapeamento de Furos (Hunting)", desc: "Mapeia-se o fluxo do pedido fÃ­sico e a conciliaÃ§Ã£o do dado financeiro. Busca-se o causador do atrito.", color: "primary" },
            { tag: "Fase 2", title: "Arquitetura Visual e Interfaces (UX)", desc: "ProtÃ³tipos em Figma desenhados com extremo cuidado estÃ©tico. O app deve ser irresistÃ­vel para quem vai operÃ¡-lo 8 horas por dia.", color: "indigo" },
            { tag: "Fase 3", title: "Pipelines e API Bridges (Back-end)", desc: "O motor real. Ligamos o novo front-end direto no banco de dados do seu ERP (via APIs), suprimindo intervenÃ§Ã£o humana.", color: "blue" },
            { tag: "Fase 4", title: "Scale-up via Agentes IA", desc: "Com o trÃ¡fego limpo, plugam-se LLMs de contingÃªncia em todo canto: no suporte, na qualificaÃ§Ã£o comercial e no SLA logÃ­stico.", color: "success" }
          ].map((item, idx) => (
             <SpotlightCard key={idx} className="p-8 md:p-10 border-white/10 flex flex-col md:flex-row gap-8 items-center group cursor-pointer" spotlightColor={`rgba(var(--brand-${item.color}-rgb), 0.15)`}>
                <div className={`w-24 h-24 rounded-full bg-brand-${item.color}/10 border border-brand-${item.color}/30 flex items-center justify-center shrink-0 group-hover:scale-110 transition-transform shadow-[0_0_20px_rgba(var(--brand-${item.color}-rgb),_0.2)]`}>
                  <span className={`text-brand-${item.color} font-black text-2xl`}>0{idx+1}</span>
                </div>
                <div className="text-center md:text-left">
                  <span className={`text-xs uppercase font-bold tracking-widest text-brand-${item.color} mb-3 block`}>{item.tag}</span>
                  <h3 className="text-3xl font-bold text-white mb-4 tracking-tight">{item.title}</h3>
                  <p className="text-foreground/60 text-lg leading-relaxed font-light">{item.desc}</p>
                </div>
             </SpotlightCard>
          ))}
        </div>
      </section>

      {/* BOTTOM CTA */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-32">
        <div className="bg-gradient-to-br from-brand-primary/20 to-brand-primary/5 border border-brand-primary/30 p-12 md:p-24 rounded-[3rem] text-center shadow-[0_0_50px_rgba(255,215,0,0.1)] relative overflow-hidden">
           <div className="absolute inset-0 bg-[#0A0A0A]/40 mix-blend-multiply" />
           <div className="relative z-10">
              <h2 className="text-4xl md:text-6xl font-black mb-8 text-white tracking-tighter">
                Sua infraestrutura suporta <br className="hidden md:block" /> a sua escala de vendas?
              </h2>
              <p className="text-xl text-foreground/80 mb-12 max-w-2xl mx-auto font-light">
                Agende um diagnÃ³stico inicial de viabilidade tÃ©cnica. 
              </p>
              <Link href="/contato" className="inline-flex items-center gap-3 bg-brand-primary text-background px-10 py-5 rounded-full font-black text-lg hover:shadow-[0_0_30px_rgba(255,215,0,0.6)] hover:scale-105 transition-all">
                Iniciar Conversa <ArrowRight className="w-5 h-5" />
              </Link>
           </div>
        </div>
      </section>

    </div>
  )
}


ARQUIVO: src\app\globals.css

@import "tailwindcss";

@theme {
  --color-background: #0A0E27;
  --color-card: #1A1F3A;
  --color-secondary: #0A0E27;
  --color-foreground: #ffffff;
  
  --color-brand-primary: #FFD700;
  --color-brand-success: #00D98E;
  --color-brand-blue: #0070F3;
  --color-brand-indigo: #4F46E5;
  
  --animate-accordion-down: accordion-down 0.2s ease-out;
  --animate-accordion-up: accordion-up 0.2s ease-out;
  --animate-pulse-slow: pulse-slow 6s cubic-bezier(0.4, 0, 0.6, 1) infinite;
}

@keyframes pulse-slow {
  0%, 100% { opacity: 0.3; transform: scale(1); }
  50% { opacity: 0.6; transform: scale(1.05); }
}

body {
  background-color: var(--color-background);
  color: var(--color-foreground);
  font-family: __Bricolage_Grotesque_fallback_36a32a, system-ui, -apple-system, sans-serif;
  overflow-x: hidden;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

::selection {
  background: var(--color-brand-primary);
  color: #000;
}

/* Glassmorphism Utilities */
.glass {
  background: rgba(10, 10, 10, 0.4);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(255, 255, 255, 0.05);
}

.glass-card {
  background: linear-gradient(180deg, rgba(20, 20, 20, 0.9) 0%, rgba(10, 10, 10, 0.9) 100%);
  border: 1px solid rgba(255, 255, 255, 0.08);
  box-shadow: 0 4px 30px rgba(0, 0, 0, 0.5);
  transition: all 0.3s ease;
}

.glass-card:hover {
  border-color: rgba(255, 255, 255, 0.15);
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.7);
}

.hide-scrollbar::-webkit-scrollbar {
  display: none;
}
.hide-scrollbar {
  -ms-overflow-style: none;
  scrollbar-width: none;
}


ARQUIVO: src\app\layout.tsx

import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Header } from "@/components/layout/Header";
import { Footer } from "@/components/layout/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ecossistema Studio | Engenharia & GestÃ£o",
  description: "Transformamos o caos operacional em um ecossistema digital lucrativo.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="pt-BR"
      className={`${geistSans.variable} ${geistMono.variable} scroll-smooth antialiased dark`}
    >
      <body className="min-h-screen flex flex-col bg-background text-foreground font-sans">
        <Header />
        <main className="flex-1 pt-20">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}


ARQUIVO: src\app\page.tsx

'use client'

import { motion } from 'framer-motion'
import Link from 'next/link'
import {
  ArrowRight, Code2, LineChart, Cpu, LayoutTemplate,
  GraduationCap, Award, Database, GitBranch, Layers,
  Terminal, Brain, Package, Zap, Users, CheckCircle2,
  Building2, Calendar, ExternalLink, Scale, BookOpen
} from 'lucide-react'
import { SpotlightCard } from '@/components/ui/SpotlightCard'

// â”€â”€â”€ NAVIGATION MACRO-AREAS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const macros = [
  {
    title: 'Engenharia & Sistemas',
    desc: 'Sistema de OS (Memo), Trava Fiscal anti-evasÃ£o fiscal, integraÃ§Ã£o Blingâ†”Dynamics via Webhook, e Kanban Mother/Daughter Order.',
    icon: Code2, color: 'brand-primary', href: '/engenharia', tag: 'Infraestrutura & Dev',
    highlights: ['NestJS + Prisma', 'Webhooks bidirecionais', 'API Bling v3']
  },
  {
    title: 'E-commerce & Dados',
    desc: 'GestÃ£o multi-canal (ML / Amazon / Magalu) via Bling Hub. Controle semanal de ACOS, ROAS e ATG% com automaÃ§Ã£o de ADS.',
    icon: LineChart, color: 'brand-success', href: '/ecommerce', tag: 'Comercial / Escala',
    highlights: ['ACOS 4.4% real', 'ROAS 22.7x real', 'Multi-canal integrado']
  },
  {
    title: 'Processos & IA',
    desc: 'Fluxogramas SAC reais, BPO Financeiro, jornada do lead Bom de Compras, automaÃ§Ã£o n8n/Make e treinamento de equipes.',
    icon: Cpu, color: 'brand-blue', href: '/processos', tag: 'LideranÃ§a & BPO',
    highlights: ['n8n + Make', 'SOPs documentados', 'Jornada Bom de Compras']
  },
  {
    title: 'Interfaces & UX',
    desc: 'Projetos front-end: Studio Lilyan (sistema de agendamento), Jat Plastic, Merice Landing. Foco em conversÃ£o e usabilidade.',
    icon: LayoutTemplate, color: 'brand-indigo', href: '/interfaces', tag: 'Front-end / UX',
    highlights: ['Next.js + Supabase', 'Sistema de agendamento', 'Design System prÃ³prio']
  }
]

// â”€â”€â”€ ACADEMIC & PROFESSIONAL TIMELINE â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const timeline = [
  {
    year: '2025',
    type: 'current',
    role: 'Head de OperaÃ§Ãµes Digitais / Arquiteto de Sistemas',
    org: 'IndÃºstria e ComÃ©rcio de Chopeiras â€” RibeirÃ£o Preto, SP',
    desc: 'ResponsÃ¡vel pela arquitetura completa da operaÃ§Ã£o digital: desenvolvimento do sistema OS interno (NestJS/Prisma), integraÃ§Ã£o Blingâ†”Dynamics, gestÃ£o multi-canal de e-commerce (ML, Amazon, Magalu) e automaÃ§Ã£o de processos via n8n.',
    tags: ['NestJS', 'Bling API', 'Dynamics', 'Mercado Livre', 'n8n'],
    color: 'brand-primary'
  },
  {
    year: '2024',
    type: 'education',
    role: 'EspecializaÃ§Ã£o em Engenharia de Software',
    org: 'Facens (Faculdade de Engenharia de Sorocaba)',
    desc: 'Foco em arquitetura de sistemas distribuÃ­dos, design patterns aplicados e metodologias Ã¡geis (Scrum/Kanban) em contextos de produÃ§Ã£o.',
    tags: ['Sistemas DistribuÃ­dos', 'Design Patterns', 'Scrum'],
    color: 'brand-blue'
  },
  {
    year: '2023',
    type: 'project',
    role: 'ImplantaÃ§Ã£o Sistema BPO Financeiro',
    org: 'Projeto: Bom de Compras / Edusen',
    desc: 'DigitalizaÃ§Ã£o completa do backoffice financeiro: mapeamento de jornada do lead (Edusen â†’ Bom de Compras), implementaÃ§Ã£o de CRM com automaÃ§Ã£o de follow-up, e treinamento de equipe de atendimento nos SOPs documentados.',
    tags: ['CRM', 'AutomaÃ§Ã£o', 'Treinamento', 'SOPs'],
    color: 'brand-success'
  },
  {
    year: '2022',
    type: 'education',
    role: 'Tecnologo em AnÃ¡lise e Desenvolvimento de Sistemas',
    org: 'UNIRP â€” Centro UniversitÃ¡rio de Rio Preto',
    desc: 'FormaÃ§Ã£o sÃ³lida em algoritmos, estruturas de dados, banco de dados relacional, POO e desenvolvimento web full-stack.',
    tags: ['ADS', 'Full Stack', 'POO', 'SQL'],
    color: 'brand-indigo'
  },
  {
    year: '2020',
    type: 'education',
    role: 'TÃ©cnico em InformÃ¡tica',
    org: 'ETEC â€” Escola TÃ©cnica Estadual de SÃ£o Paulo',
    desc: 'FormaÃ§Ã£o tÃ©cnica em hardware, redes, lÃ³gica de programaÃ§Ã£o e desenvolvimento de sistemas. Base prÃ¡tica que consolidou o interesse em integraÃ§Ã£o de sistemas.',
    tags: ['Redes', 'Hardware', 'LÃ³gica', 'Dev Web'],
    color: 'brand-success'
  },
]

// â”€â”€â”€ SKILLS MATRIX â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const skills = [
  {
    category: 'Desenvolvimento',
    icon: Terminal,
    color: 'brand-primary',
    items: [
      { name: 'NestJS / Node.js', level: 85 },
      { name: 'TypeScript / React', level: 80 },
      { name: 'Prisma ORM / PostgreSQL', level: 75 },
      { name: 'Next.js / Tailwind', level: 88 },
      { name: 'REST API / Webhooks', level: 90 },
    ]
  },
  {
    category: 'OperaÃ§Ãµes & Dados',
    icon: Database,
    color: 'brand-blue',
    items: [
      { name: 'Bling ERP (API v3)', level: 95 },
      { name: 'Microsoft Dynamics', level: 70 },
      { name: 'Google Analytics 4', level: 82 },
      { name: 'Power BI / Looker Studio', level: 75 },
      { name: 'Excel AvanÃ§ado / VBA', level: 88 },
    ]
  },
  {
    category: 'AutomaÃ§Ã£o & IA',
    icon: Brain,
    color: 'brand-success',
    items: [
      { name: 'n8n (OrquestraÃ§Ã£o)', level: 85 },
      { name: 'Make (Integromat)', level: 80 },
      { name: 'Prompt Engineering', level: 85 },
      { name: 'OpenAI API', level: 72 },
      { name: 'Zapier (legado)', level: 90 },
    ]
  },
  {
    category: 'E-commerce',
    icon: Package,
    color: 'brand-indigo',
    items: [
      { name: 'Mercado Livre (Full + ADS)', level: 95 },
      { name: 'Amazon Seller Central', level: 78 },
      { name: 'Magazine Luiza', level: 75 },
      { name: 'Shopee', level: 70 },
      { name: 'ACOS / ROAS Management', level: 92 },
    ]
  },
]

// â”€â”€â”€ CERTIFICATIONS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const certs = [
  { name: 'Consultor Certificado ML Platinum', org: 'Mercado Livre', color: '#FFE600' },
  { name: 'Scrum Fundamentals Certified', org: 'ScrumStudy (SFC)', color: '#0070F3' },
  { name: 'Meta Blueprint Professional', org: 'Meta', color: '#0668E1' },
  { name: 'Google Analytics Certified', org: 'Google', color: '#00D98E' },
]

// â”€â”€â”€ KEY ACHIEVEMENTS (no fake numbers, qualitative) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const achievements = [
  {
    icon: Scale,
    color: 'brand-primary',
    title: 'Trava Fiscal â€” Sistema OS',
    desc: 'Desenvolvi e implantei mecanismo de bloqueio de fechamento de OS sem validaÃ§Ã£o de XML de nota fiscal. Resultado: eliminaÃ§Ã£o de evasÃ£o fiscal acidental na operaÃ§Ã£o industrial.'
  },
  {
    icon: GitBranch,
    color: 'brand-blue',
    title: 'IntegraÃ§Ã£o ERP Bidireccional',
    desc: 'Arquitetura Webhook Blingâ†”Dynamics para sincronizaÃ§Ã£o de pedidos logÃ­sticos pesados (>40kg). Eliminou o Double Data Entry entre dois sistemas corporativos distintos.'
  },
  {
    icon: Users,
    color: 'brand-success',
    title: 'EstruturaÃ§Ã£o de Equipe de SAC',
    desc: 'Documentei 15+ SOPs de atendimento em marketplace, treinei equipe de 4 pessoas e implementei protocolo de escalada N1/N2/N3 com SLAs definidos por tipo de ocorrÃªncia.'
  },
  {
    icon: Zap,
    color: 'brand-indigo',
    title: 'AutomaÃ§Ã£o de Backoffice com IA',
    desc: 'Implantei agentes n8n/Make para qualificaÃ§Ã£o automÃ¡tica de leads (Bom de Compras / Edusen), reduzindo o tempo de primeiro contato e eliminando etapas manuais repetitivas.'
  },
]

// â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
// COMPONENT
// â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
export default function HomePage() {
  return (
    <div className="flex flex-col gap-28 pb-32">

      {/* BACKGROUND */}
      <div className="fixed inset-0 -z-50 pointer-events-none overflow-hidden opacity-25">
        <div className="absolute w-[800px] h-[800px] bg-brand-primary/15 blur-[120px] rounded-full -top-1/4 right-0 animate-pulse-slow" />
        <div className="absolute w-[600px] h-[600px] bg-brand-indigo/10 blur-[100px] rounded-full bottom-0 left-0 animate-pulse-slow" style={{ animationDelay: '2s' }} />
      </div>

      {/* â”€â”€ HERO â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */}
      <section className="pt-32 container mx-auto px-6">
        <motion.div
          initial={{ opacity: 0, y: 28 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8 }}
          className="max-w-5xl"
        >
          <div className="flex flex-wrap gap-3 mb-8">
            <span className="inline-flex items-center gap-2 px-3 py-1.5 rounded-full border border-brand-primary/30 bg-brand-primary/5 text-brand-primary font-bold tracking-wider uppercase text-xs">
              PortfÃ³lio Executivo â€” Head of Digital Operations
            </span>
            <span className="inline-flex items-center gap-2 px-3 py-1.5 rounded-full border border-brand-blue/30 bg-brand-blue/5 text-brand-blue font-bold tracking-wider uppercase text-xs">
              Engenharia de Software + GestÃ£o
            </span>
          </div>

          <h1 className="text-5xl md:text-7xl lg:text-8xl font-black tracking-tighter mb-6 text-white leading-[1.02]">
            O Profissional<br />
            <span className="text-transparent bg-clip-text bg-gradient-to-r from-brand-primary via-yellow-300 to-amber-200">
              HÃ­brido.
            </span>
          </h1>

          <p className="text-xl md:text-2xl text-white/55 max-w-3xl leading-relaxed font-light mb-8">
            Engenheiro de Software que gerencia operaÃ§Ãµes. Gestor de E-commerce que escreve cÃ³digo. 
            A combinaÃ§Ã£o rara de <strong className="text-white">visÃ£o tÃ©cnica</strong> e <strong className="text-white">inteligÃªncia operacional</strong> â€” no mesmo profissional.
          </p>

          {/* Dual profile cards */}
          <div className="grid grid-cols-1 sm:grid-cols-2 gap-4 max-w-2xl">
            <div className="p-5 rounded-2xl border border-brand-primary/20 bg-brand-primary/5">
              <p className="text-[10px] uppercase tracking-widest text-brand-primary/70 mb-2 font-bold">VisÃ£o de Engenharia</p>
              <p className="text-white font-bold text-sm leading-snug">Desenvolvimento de sistemas, integraÃ§Ãµes de dados (ERP), arquitetura de APIs e infraestrutura de software.</p>
            </div>
            <div className="p-5 rounded-2xl border border-brand-blue/20 bg-brand-blue/5">
              <p className="text-[10px] uppercase tracking-widest text-brand-blue/70 mb-2 font-bold">VisÃ£o de GestÃ£o</p>
              <p className="text-white font-bold text-sm leading-snug">Engenharia de processos, BPO financeiro, estratÃ©gia de e-commerce multi-canal e lideranÃ§a baseada em KPIs.</p>
            </div>
          </div>
        </motion.div>
      </section>

      {/* â”€â”€ ACADEMIC BACKGROUND â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <div className="flex items-center gap-3 mb-12">
          <GraduationCap className="w-5 h-5 text-brand-blue" />
          <p className="text-xs font-bold uppercase tracking-widest text-brand-blue">FormaÃ§Ã£o AcadÃªmica & TrajetÃ³ria</p>
        </div>

        <div className="relative">
          {/* Vertical line */}
          <div className="absolute left-5 top-0 bottom-0 w-px bg-white/5 hidden md:block" />

          <div className="space-y-6">
            {timeline.map((item, i) => (
              <motion.div
                key={i}
                initial={{ opacity: 0, x: -20 }}
                whileInView={{ opacity: 1, x: 0 }}
                viewport={{ once: true }}
                transition={{ delay: i * 0.08, duration: 0.5 }}
                className="relative flex gap-6"
              >
                {/* Dot */}
                <div className={`hidden md:flex w-10 h-10 rounded-full shrink-0 items-center justify-center border z-10
                  bg-${item.color}/10 border-${item.color}/30`}>
                  {item.type === 'education' && <GraduationCap className={`w-4 h-4 text-${item.color}`} />}
                  {item.type === 'current' && <Building2 className={`w-4 h-4 text-${item.color}`} />}
                  {item.type === 'project' && <Zap className={`w-4 h-4 text-${item.color}`} />}
                </div>

                <div className="flex-1 p-6 rounded-2xl border border-white/5 bg-white/[0.02] hover:bg-white/[0.035] hover:border-white/10 transition-all">
                  <div className="flex flex-wrap items-start justify-between gap-3 mb-3">
                    <div>
                      <span className={`text-[10px] font-mono text-${item.color} uppercase tracking-widest`}>{item.year}</span>
                      <h3 className="text-white font-bold text-base mt-0.5">{item.role}</h3>
                      <p className="text-white/40 text-xs mt-0.5">{item.org}</p>
                    </div>
                    <span className={`text-[10px] font-bold uppercase px-2 py-0.5 rounded-full border text-${item.color} border-${item.color}/30 bg-${item.color}/10 shrink-0`}>
                      {item.type === 'education' ? 'FormaÃ§Ã£o' : item.type === 'current' ? 'Atual' : 'Projeto'}
                    </span>
                  </div>
                  <p className="text-white/55 text-sm leading-relaxed mb-3">{item.desc}</p>
                  <div className="flex flex-wrap gap-2">
                    {item.tags.map(t => (
                      <span key={t} className="text-[10px] font-mono text-white/30 border border-white/8 px-2 py-0.5 rounded-full bg-white/[0.02]">{t}</span>
                    ))}
                  </div>
                </div>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* â”€â”€ KEY ACHIEVEMENTS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <p className="text-xs font-bold uppercase tracking-widest text-brand-success mb-4">RealizaÃ§Ãµes TÃ©cnicas</p>
        <h2 className="text-3xl md:text-4xl font-black mb-12 text-white tracking-tight">
          Projetos com <span className="text-brand-success">Impacto Real</span>
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-5">
          {achievements.map((a) => {
            const Icon = a.icon
            return (
              <SpotlightCard key={a.title} className="p-6" spotlightColor="rgba(255,215,0,0.06)">
                <div className={`w-10 h-10 rounded-xl bg-${a.color}/10 border border-${a.color}/20 flex items-center justify-center mb-4`}>
                  <Icon className={`w-5 h-5 text-${a.color}`} />
                </div>
                <h3 className="text-white font-bold mb-2">{a.title}</h3>
                <p className="text-white/55 text-sm leading-relaxed">{a.desc}</p>
              </SpotlightCard>
            )
          })}
        </div>
      </section>

      {/* â”€â”€ SKILLS MATRIX â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <p className="text-xs font-bold uppercase tracking-widest text-brand-primary mb-4">Stack TÃ©cnico</p>
        <h2 className="text-3xl md:text-4xl font-black mb-12 text-white tracking-tight">
          Matriz de CompetÃªncias
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
          {skills.map((sk) => {
            const Icon = sk.icon
            return (
              <div key={sk.category} className="p-6 rounded-2xl border border-white/5 bg-white/[0.02]">
                <div className="flex items-center gap-3 mb-6">
                  <div className={`w-8 h-8 rounded-lg bg-${sk.color}/10 flex items-center justify-center`}>
                    <Icon className={`w-4 h-4 text-${sk.color}`} />
                  </div>
                  <p className="text-white font-bold text-sm">{sk.category}</p>
                </div>
                <div className="space-y-4">
                  {sk.items.map((item) => (
                    <div key={item.name}>
                      <div className="flex justify-between mb-1.5">
                        <span className="text-xs text-white/70">{item.name}</span>
                        <span className={`text-xs font-mono font-bold text-${sk.color}`}>{item.level}%</span>
                      </div>
                      <div className="h-1.5 bg-white/5 rounded-full overflow-hidden">
                        <motion.div
                          className={`h-full bg-${sk.color} rounded-full`}
                          initial={{ width: 0 }}
                          whileInView={{ width: `${item.level}%` }}
                          viewport={{ once: true }}
                          transition={{ duration: 1, ease: 'easeOut', delay: 0.1 }}
                        />
                      </div>
                    </div>
                  ))}
                </div>
              </div>
            )
          })}
        </div>
      </section>

      {/* â”€â”€ CERTIFICATIONS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <p className="text-xs font-bold uppercase tracking-widest text-brand-indigo mb-4">CertificaÃ§Ãµes</p>
        <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
          {certs.map((c) => (
            <div key={c.name} className="flex items-center gap-4 p-5 rounded-2xl border border-white/5 bg-white/[0.02]">
              <div className="w-10 h-10 rounded-xl flex items-center justify-center shrink-0" style={{ background: `${c.color}15`, border: `1px solid ${c.color}30` }}>
                <Award className="w-5 h-5" style={{ color: c.color }} />
              </div>
              <div>
                <p className="text-white font-bold text-sm">{c.name}</p>
                <p className="text-white/35 text-xs mt-0.5">{c.org}</p>
              </div>
            </div>
          ))}
        </div>
      </section>

      {/* â”€â”€ MACRO NAVIGATION â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */}
      <section className="container mx-auto px-6 border-t border-white/5 pt-24">
        <p className="text-xs font-bold uppercase tracking-widest text-white/30 mb-4">Explorar o PortfÃ³lio</p>
        <h2 className="text-3xl md:text-4xl font-black mb-12 text-white tracking-tight">
          Quatro Ãreas de AtuaÃ§Ã£o
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-5">
          {macros.map((m) => {
            const Icon = m.icon
            return (
              <Link key={m.href} href={m.href}>
                <SpotlightCard className="p-6 h-full group cursor-pointer" spotlightColor={`rgba(255,215,0,0.07)`}>
                  <div className="flex items-start justify-between mb-4">
                    <div className={`w-11 h-11 rounded-xl bg-${m.color}/10 border border-${m.color}/20 flex items-center justify-center`}>
                      <Icon className={`w-5 h-5 text-${m.color}`} />
                    </div>
                    <span className={`text-[10px] uppercase tracking-widest font-bold text-${m.color} border border-${m.color}/20 px-2 py-0.5 rounded-full`}>{m.tag}</span>
                  </div>
                  <h3 className="text-white font-bold text-lg mb-2">{m.title}</h3>
                  <p className="text-white/50 text-sm leading-relaxed mb-4">{m.desc}</p>
                  <div className="flex flex-wrap gap-2 mb-4">
                    {m.highlights.map(h => (
                      <span key={h} className="text-[10px] font-mono text-white/30 border border-white/8 px-2 py-0.5 rounded-full">{h}</span>
                    ))}
                  </div>
                  <div className={`flex items-center gap-2 text-${m.color} font-bold text-sm mt-auto`}>
                    Ver Case Study <ArrowRight className="w-4 h-4 group-hover:translate-x-1 transition-transform" />
                  </div>
                </SpotlightCard>
              </Link>
            )
          })}
        </div>
      </section>

    </div>
  )
}


ARQUIVO: src\components\interactive\BuyBoxSimulator.tsx

'use client'

import { motion } from 'framer-motion'
import { useState } from 'react'
import { Trophy, Truck, Star, AlertCircle } from 'lucide-react'

const sellers = [
  {
    id: 1,
    name: 'Sua OperaÃ§Ã£o Estruturada',
    reputation: 'green',
    fulfillment: true,
    price: 149.90,
    isWinner: true
  },
  {
    id: 2,
    name: 'Concorrente A (Amador)',
    reputation: 'yellow',
    fulfillment: false,
    price: 145.00,
    isWinner: false
  },
  {
    id: 3,
    name: 'Concorrente B (Saturado)',
    reputation: 'red',
    fulfillment: false,
    price: 139.90,
    isWinner: false
  }
]

export function BuyBoxSimulator() {
  const [activeStrategy, setActiveStrategy] = useState<'price' | 'logistics' | 'reputation'>('logistics')

  return (
    <div className="w-full bg-[#0A0E27] rounded-3xl p-8 border border-brand-blue/30 shadow-[0_0_40px_rgba(33,150,243,0.1)] relative">
      <div className="absolute top-0 right-0 p-4">
        <span className="bg-brand-blue/20 text-brand-blue text-xs font-bold px-3 py-1 rounded-full border border-brand-blue/30 uppercase">
          Simulador em Tempo Real
        </span>
      </div>

      <h3 className="text-2xl font-bold text-white mb-2">DomÃ­nio de Buy Box</h3>
      <p className="text-foreground/60 mb-8 max-w-lg">
        Veja como o algoritmo prioriza vendedores. Estruture a logÃ­stica e a reputaÃ§Ã£o para ganhar a Buy Box mesmo cobrando mais caro.
      </p>

      {/* Strategy Controls */}
      <div className="flex flex-wrap gap-4 mb-8">
        <button 
          onClick={() => setActiveStrategy('price')}
          className={`px-4 py-2 rounded-lg text-sm font-bold border transition-colors ${activeStrategy === 'price' ? 'bg-white text-black border-white' : 'bg-transparent text-white/60 border-white/20 hover:border-white/50'}`}
        >
          EstratÃ©gia: Guerra de PreÃ§o
        </button>
        <button 
          onClick={() => setActiveStrategy('logistics')}
          className={`px-4 py-2 rounded-lg text-sm font-bold border transition-colors ${activeStrategy === 'logistics' ? 'bg-brand-blue text-white border-brand-blue' : 'bg-transparent text-white/60 border-white/20 hover:border-white/50'}`}
        >
          EstratÃ©gia: Full/LogÃ­stica
        </button>
      </div>

      <div className="grid gap-4">
        {sellers.map((seller) => {
          // Calculate if they win the buy box based on the selected strategy
          let isWinning = false;
          if (activeStrategy === 'price' && seller.price === 139.90) isWinning = true;
          if (activeStrategy === 'logistics' && seller.fulfillment === true) isWinning = true;

          return (
            <motion.div
              key={seller.id}
              layout
              initial={{ opacity: 0, y: 10 }}
              animate={{ opacity: 1, y: 0 }}
              className={`relative flex flex-col md:flex-row md:items-center justify-between p-6 rounded-2xl border transition-all duration-500 ${isWinning ? 'bg-gradient-to-r from-accent-green/10 to-transparent border-accent-green/50 scale-[1.02] shadow-[0_0_20px_rgba(0,217,142,0.1)]' : 'bg-secondary/50 border-white/5'}`}
            >
              <div className="flex items-center gap-4 mb-4 md:mb-0">
                <div className={`w-3 h-12 rounded-full ${seller.reputation === 'green' ? 'bg-accent-green' : seller.reputation === 'yellow' ? 'bg-accent-gold' : 'bg-accent-red'}`} />
                <div>
                  <h4 className="font-bold text-white text-lg flex items-center gap-2">
                    {seller.name}
                    {isWinning && <Trophy className="w-5 h-5 text-accent-gold" />}
                  </h4>
                  <div className="flex items-center gap-3 mt-1">
                    <span className="flex items-center gap-1 text-xs text-foreground/60">
                      <Star className="w-3 h-3 text-accent-gold fill-accent-gold" />
                      ReputaÃ§Ã£o {seller.reputation === 'green' ? 'Verde' : seller.reputation === 'yellow' ? 'Amarela' : 'Laranja'}
                    </span>
                    {seller.fulfillment && (
                      <span className="flex items-center gap-1 text-xs text-brand-blue font-bold tracking-wide">
                        <Truck className="w-4 h-4" /> FULL
                      </span>
                    )}
                  </div>
                </div>
              </div>

              <div className="flex items-center gap-6">
                <div className="text-right">
                  <p className="text-xs text-foreground/50 line-through">R$ {(seller.price * 1.2).toFixed(2)}</p>
                  <p className={`text-2xl font-bold ${isWinning ? 'text-white' : 'text-foreground/80'}`}>
                    R$ {seller.price.toFixed(2)}
                  </p>
                </div>
                
                {isWinning && (
                  <motion.div 
                    initial={{ scale: 0 }}
                    animate={{ scale: 1 }}
                    className="hidden md:flex bg-accent-green text-black px-4 py-2 rounded-lg font-bold text-sm items-center gap-2"
                  >
                    Buy Box Winner
                  </motion.div>
                )}
              </div>

              {/* Warnings based on strategy */}
              {isWinning && activeStrategy === 'price' && seller.price === 139.90 && (
                 <div className="absolute -top-3 left-1/2 -translate-x-1/2 bg-accent-red/90 text-white text-[10px] px-3 py-1 rounded-full font-bold flex items-center gap-1">
                   <AlertCircle className="w-3 h-3" /> MARGEM DESTRUÃDA
                 </div>
              )}
              {isWinning && activeStrategy === 'logistics' && seller.fulfillment && (
                 <div className="absolute -top-3 left-1/2 -translate-x-1/2 bg-accent-green text-black text-[10px] px-3 py-1 rounded-full font-bold flex items-center gap-1">
                   <Trophy className="w-3 h-3" /> LÃDER COM MARGEM ALTA
                 </div>
              )}

            </motion.div>
          )
        })}
      </div>
    </div>
  )
}


ARQUIVO: src\components\interactive\ComplexSacFlow.tsx

'use client'

import { useState } from 'react'
import { motion, AnimatePresence } from 'framer-motion'
import {
  CheckCircle2, AlertTriangle, Clock, Package, ShoppingCart,
  FileText, Truck, MessageSquare, ChevronDown, ChevronRight,
  RefreshCw, ArrowRight, Shield, Search, Phone
} from 'lucide-react'

// â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
// DATA: Real SAC SOPs extracted from PDFs
// â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const SAC_BRANCHES = [
  {
    id: 'etiqueta',
    label: 'Chamado Meli â€” Sem Etiqueta',
    icon: FileText,
    color: '#FFD700',
    glow: 'rgba(255,215,0,0.3)',
    borderClass: 'border-brand-primary',
    tagClass: 'text-brand-primary bg-brand-primary/10 border-brand-primary/30',
    priority: 'Alta',
    sla: '24h',
    steps: [
      { step: '01', action: 'Localizar pedido no Hub Bling', detail: 'Filtrar por status "Aguardando Envio" e nÃºmero do pedido MP' },
      { step: '02', action: 'Verificar bloqueio de etiqueta', detail: 'Checar se hÃ¡ inconsistÃªncia de endereÃ§o ou limite de peso no carrier' },
      { step: '03', action: 'Acionar geraÃ§Ã£o manual', detail: 'Emitir nova etiqueta via painel Mercado Envios â€” seÃ§Ã£o "EmissÃ£o Avulsa"' },
      { step: '04', action: 'Notificar timeline do pedido', detail: 'Atualizar prazo no sistema e disparar mensagem proativa ao comprador via MP Mensagens' },
      { step: '05', action: 'Registrar ocorrÃªncia', detail: 'Log interno na planilha SAC com causa-raiz para anÃ¡lise de recorrÃªncia (PDCA)' },
    ]
  },
  {
    id: 'transportadora',
    label: 'OcorrÃªncia Transportadora',
    icon: Truck,
    color: '#0070F3',
    glow: 'rgba(0,112,243,0.3)',
    borderClass: 'border-brand-blue',
    tagClass: 'text-brand-blue bg-brand-blue/10 border-brand-blue/30',
    priority: 'CrÃ­tica',
    sla: '12h',
    steps: [
      { step: '01', action: 'Abrir rastreamento detalhado', detail: 'Cruzar tracking MP / Correios / Jadlog na timeline do pedido para identificar o evento de falha' },
      { step: '02', action: 'Classificar tipo de ocorrÃªncia', detail: 'Objeto retido â†’ EndereÃ§o incorreto â†’ Tentativa sem sucesso (TNT) â†’ Extravio total' },
      { step: '03', action: 'Acionar carrier via B2B', detail: 'Protocolo de contestaÃ§Ã£o no portal da transportadora (ex: Jadlog Empresas) com evidÃªncias do rastreio' },
      { step: '04', action: 'Abrir solicitaÃ§Ã£o de reenvio ML', detail: 'Se prazo excedido: acionar "Problemas com entrega" no Gerenciador ML para proteÃ§Ã£o do vendedor' },
      { step: '05', action: 'Comunicar comprador com SLA', detail: 'Mensagem padronizada: informar previsÃ£o, alternativas (reenvio ou reembolso) e abrir mediaÃ§Ã£o preventiva se necessÃ¡rio' },
      { step: '06', action: 'Aplicar PDCA', detail: 'Registrar carrier-problema. Volume â†’ 5 ocorrÃªncias/mÃªs ativa gatilho de revisÃ£o de contrato TMS' },
    ]
  },
  {
    id: 'prazo',
    label: 'ReclamaÃ§Ã£o de Prazo (SLA Breach)',
    icon: Clock,
    color: '#00D98E',
    glow: 'rgba(0,217,142,0.3)',
    borderClass: 'border-brand-success',
    tagClass: 'text-brand-success bg-brand-success/10 border-brand-success/30',
    priority: 'Moderada',
    sla: '6h',
    steps: [
      { step: '01', action: 'Identificar gatilho da reclamaÃ§Ã£o', detail: 'Verificar se a violaÃ§Ã£o Ã© interna (despacho tardio) ou de carrier (logÃ­stica). Cada uma tem tratativa SLA diferente no ML' },
      { step: '02', action: 'Verificar ReputaÃ§Ã£o (TermÃ´metro)', detail: 'Checar se o pedido impactou o indicador de "Prazo de Envio" no Painel de ReputaÃ§Ã£o ML (meta: acima de 90%)' },
      { step: '03', action: 'Oferta de compensaÃ§Ã£o proativa', detail: 'Enviar cupom de desconto 5-10% na prÃ³xima compra via mensagem antes que o comprador abra reclamaÃ§Ã£o formal' },
      { step: '04', action: 'MediaÃ§Ã£o se necessÃ¡rio', detail: 'Se reclamaÃ§Ã£o jÃ¡ aberta: responder em atÃ© 6h com evidÃªncias de despacho (NF + rastreio). Evitar reembolso automÃ¡tico' },
      { step: '05', action: 'RevisÃ£o de buffer de expediÃ§Ã£o', detail: 'Se padrÃ£o: estender prazo de processamento em D+1 nas listagens para criar margem de seguranÃ§a operacional' },
    ]
  }
]

const MAIN_FLOW = [
  { id: 'start', label: 'InÃ­cio do Dia', icon: RefreshCw, color: 'brand-primary', desc: 'Check-in operacional 08:00' },
  { id: 'orders', label: 'RevisÃ£o de Pedidos', icon: ShoppingCart, color: 'brand-blue', desc: 'Pedidos D-1 e novos do dia' },
  { id: 'stock', label: 'AtualizaÃ§Ã£o de Estoque', icon: Package, color: 'brand-success', desc: 'Sync Bling â†” Marketplaces' },
  { id: 'sac', label: 'SAC / PÃ³s-Venda', icon: MessageSquare, color: 'brand-primary', desc: 'Triagem por prioridade' },
]

// â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
// COMPONENT
// â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
export function ComplexSacFlow() {
  const [activeStep, setActiveStep] = useState<string | null>(null)
  const [expandedBranch, setExpandedBranch] = useState<string | null>(null)
  const [completedSteps, setCompletedSteps] = useState<Set<string>>(new Set())

  const toggleStep = (id: string) => {
    setActiveStep(prev => prev === id ? null : id)
    if (id === 'sac') setExpandedBranch(null)
  }

  const toggleBranch = (id: string) => {
    setExpandedBranch(prev => prev === id ? null : id)
  }

  const markComplete = (stepId: string) => {
    setCompletedSteps(prev => {
      const next = new Set(prev)
      next.has(stepId) ? next.delete(stepId) : next.add(stepId)
      return next
    })
  }

  const isSacActive = activeStep === 'sac'

  return (
    <div className="w-full rounded-3xl border border-white/5 bg-[#080808] overflow-hidden">
      {/* HEADER */}
      <div className="flex items-center justify-between px-6 py-4 border-b border-white/5 bg-white/[0.02]">
        <div className="flex items-center gap-3">
          <div className="w-3 h-3 rounded-full bg-red-500 opacity-70" />
          <div className="w-3 h-3 rounded-full bg-yellow-500 opacity-70" />
          <div className="w-3 h-3 rounded-full bg-green-500 opacity-70" />
          <span className="ml-2 text-white/30 text-xs font-mono">sac_decision_tree.workflow</span>
        </div>
        <span className="text-xs font-mono text-brand-success flex items-center gap-1">
          <span className="w-1.5 h-1.5 rounded-full bg-brand-success animate-pulse inline-block" />
          LIVE
        </span>
      </div>

      <div className="p-6 md:p-8">

        {/* MAIN FLOW PIPELINE */}
        <div className="mb-8">
          <p className="text-xs font-bold uppercase tracking-widest text-white/30 mb-5">Rotina DiÃ¡ria de OperaÃ§Ãµes</p>
          <div className="flex flex-col sm:flex-row items-stretch sm:items-center gap-2">
            {MAIN_FLOW.map((node, i) => {
              const Icon = node.icon
              const isActive = activeStep === node.id
              const isDone = completedSteps.has(node.id)
              return (
                <div key={node.id} className="flex sm:flex-1 items-center gap-2">
                  <button
                    onClick={() => toggleStep(node.id)}
                    className={`flex-1 flex flex-col items-center gap-2 p-4 rounded-2xl border transition-all duration-300 text-left sm:text-center group
                      ${isDone
                        ? 'border-brand-success/50 bg-brand-success/5'
                        : isActive
                          ? `border-${node.color}/60 bg-${node.color}/10`
                          : 'border-white/5 bg-white/[0.02] hover:border-white/10'
                      }`}
                  >
                    <div className={`w-10 h-10 rounded-xl flex items-center justify-center transition-all
                      ${isDone ? 'bg-brand-success text-black' : `bg-${node.color}/10 text-${node.color}`}`}>
                      {isDone ? <CheckCircle2 className="w-5 h-5" /> : <Icon className="w-5 h-5" />}
                    </div>
                    <p className="text-white font-bold text-xs">{node.label}</p>
                    <p className="text-white/40 text-[10px] hidden sm:block">{node.desc}</p>
                  </button>
                  {i < MAIN_FLOW.length - 1 && (
                    <ArrowRight className="w-4 h-4 text-white/20 shrink-0 hidden sm:block" />
                  )}
                </div>
              )
            })}
          </div>
        </div>

        {/* DETAIL PANEL for non-SAC steps */}
        <AnimatePresence>
          {activeStep && activeStep !== 'sac' && (
            <motion.div
              key={activeStep}
              initial={{ opacity: 0, height: 0 }}
              animate={{ opacity: 1, height: 'auto' }}
              exit={{ opacity: 0, height: 0 }}
              className="overflow-hidden mb-8"
            >
              <div className="bg-white/[0.03] border border-white/8 rounded-2xl p-5 flex items-start gap-4">
                <Search className="w-5 h-5 text-brand-primary shrink-0 mt-0.5" />
                <div>
                  <p className="text-white font-bold mb-1">
                    {activeStep === 'start' && 'Protocolo de Abertura â€” D+0'}
                    {activeStep === 'orders' && 'RevisÃ£o de Pedidos â€” AÃ§Ãµes PrioritÃ¡rias'}
                    {activeStep === 'stock' && 'Sync de Estoque â€” PrevenÃ§Ã£o de Ruptura'}
                  </p>
                  <p className="text-white/60 text-sm leading-relaxed">
                    {activeStep === 'start' && 'Abertura do painel Bling, Mercado Livre, Amazon e Shopee. VerificaÃ§Ã£o de alertas crÃ­ticos no status de reputaÃ§Ã£o dos canais antes de qualquer aÃ§Ã£o operacional.'}
                    {activeStep === 'orders' && 'SeparaÃ§Ã£o de pedidos por modalidade logÃ­stica: Full (CD ML), Flex (despacho D+0) e Coletas (D+1). PriorizaÃ§Ã£o pelo SLA de entrega mais curto.'}
                    {activeStep === 'stock' && 'Leitura do relatÃ³rio de posiÃ§Ã£o de estoque no Bling. SincronizaÃ§Ã£o automÃ¡tica com os portais via API. Gatilho manual de reposiÃ§Ã£o quando SKU cai abaixo do ponto de pedido definido.'}
                  </p>
                  <button
                    onClick={() => markComplete(activeStep)}
                    className="mt-3 text-xs font-bold border rounded-lg px-3 py-1.5 transition-all
                      border-brand-success/40 text-brand-success hover:bg-brand-success/10"
                  >
                    {completedSteps.has(activeStep) ? 'âœ“ ConcluÃ­do' : 'Marcar como Executado'}
                  </button>
                </div>
              </div>
            </motion.div>
          )}
        </AnimatePresence>

        {/* SAC DECISION TREE â€” expanded when "SAC" is clicked */}
        <AnimatePresence>
          {isSacActive && (
            <motion.div
              key="sac-tree"
              initial={{ opacity: 0, height: 0 }}
              animate={{ opacity: 1, height: 'auto' }}
              exit={{ opacity: 0, height: 0 }}
              className="overflow-hidden"
            >
              <div className="mb-4 flex items-center gap-3">
                <div className="h-px flex-1 bg-white/5" />
                <span className="text-xs font-bold uppercase tracking-widest text-brand-primary">
                  Ãrvore de DecisÃ£o SAC
                </span>
                <div className="h-px flex-1 bg-white/5" />
              </div>

              <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
                {SAC_BRANCHES.map((branch) => {
                  const BIcon = branch.icon
                  const isOpen = expandedBranch === branch.id
                  return (
                    <div key={branch.id} className="flex flex-col">
                      {/* Branch Header Card */}
                      <button
                        onClick={() => toggleBranch(branch.id)}
                        className={`w-full p-5 rounded-2xl border text-left transition-all duration-300
                          ${isOpen
                            ? `bg-[${branch.color}]/10 border-[${branch.color}]/40 shadow-[0_0_25px_${branch.glow}]`
                            : 'border-white/8 bg-white/[0.02] hover:border-white/15'
                          }`}
                        style={isOpen ? {
                          background: `linear-gradient(135deg, ${branch.color}12, transparent)`,
                          borderColor: `${branch.color}50`,
                          boxShadow: `0 0 25px ${branch.glow}`
                        } : {}}
                      >
                        <div className="flex items-center justify-between mb-3">
                          <div className="w-10 h-10 rounded-xl flex items-center justify-center"
                            style={{ background: `${branch.color}20`, border: `1px solid ${branch.color}40` }}>
                            <BIcon className="w-5 h-5" style={{ color: branch.color }} />
                          </div>
                          <div className="flex items-center gap-2">
                            <span className={`text-[10px] font-bold uppercase px-2 py-0.5 rounded-full border ${branch.tagClass}`}>
                              SLA {branch.sla}
                            </span>
                            {isOpen
                              ? <ChevronDown className="w-4 h-4 text-white/40" />
                              : <ChevronRight className="w-4 h-4 text-white/40" />
                            }
                          </div>
                        </div>
                        <p className="text-white font-bold text-sm leading-tight">{branch.label}</p>
                        <div className="flex items-center gap-2 mt-2">
                          <AlertTriangle className="w-3 h-3" style={{ color: branch.color }} />
                          <span className="text-[10px] text-white/40">Prioridade: {branch.priority}</span>
                        </div>
                      </button>

                      {/* SOP Steps (expanded) */}
                      <AnimatePresence>
                        {isOpen && (
                          <motion.div
                            initial={{ opacity: 0, height: 0, y: -8 }}
                            animate={{ opacity: 1, height: 'auto', y: 0 }}
                            exit={{ opacity: 0, height: 0, y: -8 }}
                            transition={{ type: 'spring', damping: 25, stiffness: 300 }}
                            className="overflow-hidden mt-2"
                          >
                            <div className="bg-black/60 border border-white/5 rounded-2xl p-4 space-y-3">
                              <p className="text-[10px] font-mono uppercase tracking-widest text-white/30 mb-3">
                                Procedimento Operacional PadrÃ£o
                              </p>
                              {branch.steps.map((step, idx) => (
                                <motion.div
                                  key={step.step}
                                  initial={{ opacity: 0, x: -10 }}
                                  animate={{ opacity: 1, x: 0 }}
                                  transition={{ delay: idx * 0.06 }}
                                  className="flex gap-3 group"
                                >
                                  <span className="text-[10px] font-mono font-bold shrink-0 mt-0.5 pt-0.5 w-6"
                                    style={{ color: branch.color }}>
                                    {step.step}
                                  </span>
                                  <div>
                                    <p className="text-white/90 text-xs font-bold leading-tight">{step.action}</p>
                                    <p className="text-white/40 text-[10px] leading-relaxed mt-0.5">{step.detail}</p>
                                  </div>
                                </motion.div>
                              ))}
                              <div className="pt-3 border-t border-white/5 flex justify-end">
                                <button
                                  onClick={() => markComplete(branch.id)}
                                  className="text-[10px] font-bold border rounded-lg px-3 py-1.5 transition-all"
                                  style={{
                                    borderColor: `${branch.color}50`,
                                    color: branch.color,
                                  }}
                                >
                                  {completedSteps.has(branch.id) ? 'âœ“ SOP Executado' : 'Confirmar ExecuÃ§Ã£o'}
                                </button>
                              </div>
                            </div>
                          </motion.div>
                        )}
                      </AnimatePresence>
                    </div>
                  )
                })}
              </div>

              {/* Escalation Matrix */}
              <div className="p-5 rounded-2xl border border-white/5 bg-white/[0.02]">
                <div className="flex items-center gap-2 mb-4">
                  <Shield className="w-4 h-4 text-brand-primary" />
                  <p className="text-white font-bold text-sm">Matriz de Escalada â€” Marketplace</p>
                </div>
                <div className="grid grid-cols-3 gap-3 text-center">
                  {[
                    { level: 'N1', label: 'Atendimento PadrÃ£o', time: '&lt;12h', color: 'brand-success' },
                    { level: 'N2', label: 'MediaÃ§Ã£o Formal', time: '&lt;24h', color: 'brand-primary' },
                    { level: 'N3', label: 'Acionamento JurÃ­dico', time: '&gt;72h', color: 'red-400' },
                  ].map(m => (
                    <div key={m.level} className="bg-black/40 rounded-xl p-3 border border-white/5">
                      <span className={`text-xs font-mono font-bold text-${m.color}`}>{m.level}</span>
                      <p className="text-white/70 text-[10px] mt-1">{m.label}</p>
                      <p className="text-white/30 text-[10px] font-mono mt-1"
                        dangerouslySetInnerHTML={{ __html: m.time }} />
                    </div>
                  ))}
                </div>
              </div>
            </motion.div>
          )}
        </AnimatePresence>

        {/* Prompt if nothing selected */}
        {!activeStep && (
          <p className="text-center text-white/20 text-xs font-mono py-4 animate-pulse">
            â†’ Clique em um nÃ³ do fluxo para expandir o procedimento
          </p>
        )}
      </div>
    </div>
  )
}


ARQUIVO: src\components\interactive\ErpBridge.tsx

'use client'

import { motion } from 'framer-motion'
import { Server, Database, ArrowRightLeft, Cpu } from 'lucide-react'

export function ErpBridge() {
  return (
    <div className="w-full bg-black/40 rounded-3xl p-6 md:p-8 border border-white/10 backdrop-blur-md relative overflow-hidden flex flex-col min-h-[400px]">
      
      <div className="mb-12 flex justify-between items-center z-10">
        <div>
          <h3 className="text-lg font-bold text-white mb-1">Topologia de Rede (Live)</h3>
          <p className="text-foreground/50 text-xs font-mono uppercase tracking-widest">SincronizaÃ§Ã£o Bidirecional (TMS Carga &gt;40kg)</p>
        </div>
      </div>

      <div className="flex flex-col md:flex-row items-center justify-between gap-6 flex-1 relative z-10">
        
        {/* Node 1: Dynamics */}
        <motion.div 
          animate={{ y: [0, -5, 0] }}
          transition={{ duration: 4, repeat: Infinity, ease: "easeInOut" }}
          className="relative flex flex-col items-center group w-full md:w-auto"
        >
          <div className="w-24 h-24 rounded-2xl bg-[#0A0E27]/80 border border-brand-primary/30 flex items-center justify-center shadow-[0_0_30px_rgba(255,215,0,0.1)] relative overflow-hidden group-hover:border-brand-primary group-hover:shadow-[0_0_40px_rgba(255,215,0,0.3)] transition-all">
            <div className="absolute inset-0 bg-gradient-to-br from-brand-primary/20 to-transparent opacity-50" />
            <Server className="w-10 h-10 text-brand-primary relative z-10 drop-shadow-[0_0_10px_rgba(255,215,0,0.8)]" />
          </div>
          <p className="mt-4 font-bold text-white tracking-wide text-center">Micros. Dynamics</p>
          <span className="text-[10px] text-brand-primary uppercase tracking-widest font-mono font-bold mt-1 bg-brand-primary/10 px-2 py-0.5 rounded border border-brand-primary/20">ERP Industrial</span>
        </motion.div>

        {/* Animated Bridge */}
        <div className="hidden md:flex flex-col items-center justify-center flex-1 min-w-[120px] relative px-4">
          
          <div className="w-12 h-12 bg-white/5 rounded-full border border-white/10 flex items-center justify-center relative z-20 mb-4 shadow-lg backdrop-blur-md">
            <ArrowRightLeft className="w-5 h-5 text-white/50" />
          </div>
          
          <span className="text-[10px] text-white/50 uppercase tracking-widest font-mono">Webhooks</span>
          
          {/* Beams */}
          <div className="absolute top-1/2 left-0 right-0 -translate-y-[20px] h-0.5 bg-white/10 rounded-full" />
          <motion.div
            className="absolute top-1/2 left-0 h-1 w-16 bg-brand-primary rounded-full shadow-[0_0_15px_rgba(255,215,0,0.9)] -translate-y-[21px] z-10"
            animate={{ x: [0, 200] }}
            transition={{ duration: 1.5, repeat: Infinity, ease: "linear" }}
          />

          <div className="absolute top-1/2 left-0 right-0 -translate-y-[4px] h-0.5 bg-white/10 rounded-full" />
          <motion.div
            className="absolute top-1/2 right-0 h-1 w-16 bg-brand-blue rounded-full shadow-[0_0_15px_rgba(0,112,243,0.9)] -translate-y-[5px] z-10"
            animate={{ x: [0, -200] }}
            transition={{ duration: 1.8, repeat: Infinity, ease: "linear" }}
          />

        </div>

        {/* Mobile Beams (Vertical) */}
        <div className="md:hidden flex flex-row items-center justify-center h-24 relative w-full">
           <div className="absolute left-1/2 top-0 bottom-0 -translate-x-[20px] w-0.5 bg-white/10 rounded-full" />
           <motion.div
            className="absolute top-0 left-1/2 w-1 h-12 bg-brand-primary rounded-full shadow-[0_0_15px_rgba(255,215,0,0.9)] -translate-x-[21px] z-10"
            animate={{ y: [0, 100] }}
            transition={{ duration: 1.5, repeat: Infinity, ease: "linear" }}
          />

          <div className="absolute left-1/2 top-0 bottom-0 translate-x-[20px] w-0.5 bg-white/10 rounded-full" />
          <motion.div
            className="absolute bottom-0 left-1/2 w-1 h-12 bg-brand-blue rounded-full shadow-[0_0_15px_rgba(0,112,243,0.9)] translate-x-[19px] z-10"
            animate={{ y: [0, -100] }}
            transition={{ duration: 1.8, repeat: Infinity, ease: "linear" }}
          />
        </div>

        {/* Node 2: Bling */}
        <motion.div 
          animate={{ y: [0, 5, 0] }}
          transition={{ duration: 3.5, repeat: Infinity, ease: "easeInOut" }}
          className="relative flex flex-col items-center group w-full md:w-auto"
        >
          <div className="w-24 h-24 rounded-2xl bg-[#0A0E27]/80 border border-brand-blue/30 flex items-center justify-center shadow-[0_0_30px_rgba(0,112,243,0.1)] relative overflow-hidden group-hover:border-brand-blue group-hover:shadow-[0_0_40px_rgba(0,112,243,0.3)] transition-all">
             <div className="absolute inset-0 bg-gradient-to-tl from-brand-blue/20 to-transparent opacity-50" />
            <Database className="w-10 h-10 text-brand-blue relative z-10 drop-shadow-[0_0_10px_rgba(0,112,243,0.8)]" />
          </div>
          <p className="mt-4 font-bold text-white tracking-wide text-center">Bling Core</p>
          <span className="text-[10px] text-brand-blue uppercase tracking-widest font-mono font-bold mt-1 bg-brand-blue/10 px-2 py-0.5 rounded border border-brand-blue/20">Hub Omnichannel</span>
        </motion.div>

      </div>
    </div>
  )
}


ARQUIVO: src\components\interactive\ExecutiveDashboard.tsx

'use client'

import { useState, useEffect } from 'react'
import { motion } from 'framer-motion'
import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer, CartesianGrid, Legend } from 'recharts'
import { Calendar, TrendingUp, AlertTriangle } from 'lucide-react'

// Data models for different seasons
const dataDefault = [
  { name: 'Mercado Livre', ROAS: 3.2, ACOS: 14, Conv: 2.1 },
  { name: 'Amazon', ROAS: 2.8, ACOS: 18, Conv: 1.8 },
  { name: 'Magalu', ROAS: 4.1, ACOS: 11, Conv: 2.5 },
  { name: 'Shopee', ROAS: 5.5, ACOS: 8, Conv: 3.1 },
]

const dataBlackFriday = [
  { name: 'Mercado Livre', ROAS: 6.5, ACOS: 10, Conv: 4.8 },
  { name: 'Amazon', ROAS: 5.2, ACOS: 12, Conv: 3.9 },
  { name: 'Magalu', ROAS: 7.1, ACOS: 9, Conv: 5.1 },
  { name: 'Shopee', ROAS: 8.5, ACOS: 6, Conv: 6.2 },
]

const dataDiaDasMaes = [
  { name: 'Mercado Livre', ROAS: 4.5, ACOS: 12, Conv: 3.5 },
  { name: 'Amazon', ROAS: 3.8, ACOS: 14, Conv: 2.9 },
  { name: 'Magalu', ROAS: 5.1, ACOS: 10, Conv: 3.8 },
  { name: 'Shopee', ROAS: 6.0, ACOS: 7, Conv: 4.5 },
]

export function ExecutiveDashboard() {
  const [season, setSeason] = useState<'default' | 'black_friday' | 'dia_das_maes'>('default')
  const [isMounted, setIsMounted] = useState(false)

  useEffect(() => {
    setIsMounted(true)
  }, [])

  const getData = () => {
    switch (season) {
      case 'black_friday': return dataBlackFriday;
      case 'dia_das_maes': return dataDiaDasMaes;
      default: return dataDefault;
    }
  }

  const activeData = getData();

  const getAvg = (key: 'ROAS' | 'ACOS' | 'Conv') => {
    return (activeData.reduce((acc, val) => acc + val[key], 0) / activeData.length).toFixed(1);
  }

  return (
    <div className="w-full bg-[#0A0E27] rounded-3xl p-8 border border-white/10 relative mt-8">
      
      {/* Header and Controls */}
      <div className="flex flex-col md:flex-row md:items-end justify-between gap-6 mb-12">
        <div>
          <h3 className="text-2xl font-bold text-white flex items-center gap-2 mb-2">
            <TrendingUp className="text-brand-blue" /> VisÃ£o Geral de Performance
          </h3>
          <p className="text-foreground/60 text-sm">Dashboard Gerencial: Margem vs Investimento por Canal</p>
        </div>

        <div className="flex bg-secondary p-1 rounded-xl border border-white/5">
          <button
            onClick={() => setSeason('default')}
            className={`px-4 py-2 rounded-lg text-sm font-bold transition-all ${season === 'default' ? 'bg-white text-black shadow-sm' : 'text-white/60 hover:text-white'}`}
          >
            MÃ©dia PadrÃ£o
          </button>
          <button
            onClick={() => setSeason('dia_das_maes')}
            className={`px-4 py-2 rounded-lg text-sm font-bold transition-all ${season === 'dia_das_maes' ? 'bg-accent-gold text-black shadow-sm' : 'text-white/60 hover:text-white'}`}
          >
            Dia das MÃ£es
          </button>
          <button
            onClick={() => setSeason('black_friday')}
            className={`px-4 py-2 rounded-lg text-sm font-bold transition-all ${season === 'black_friday' ? 'bg-accent-red text-white shadow-[0_0_15px_rgba(255,71,87,0.3)]' : 'text-white/60 hover:text-white'}`}
          >
            Black Friday
          </button>
        </div>
      </div>

      {/* KPI Cards */}
      <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
        <motion.div layout className="bg-secondary/50 border border-white/5 rounded-2xl p-6">
          <p className="text-foreground/60 text-sm mb-2 font-bold uppercase tracking-wider">ROAS MÃ©dio</p>
          <p className="text-4xl font-bold text-white">{getAvg('ROAS')}x</p>
          <p className="text-xs text-accent-green mt-2 flex items-center gap-1">+ Retorno sobre ad spend</p>
        </motion.div>
        <motion.div layout className="bg-secondary/50 border border-white/5 rounded-2xl p-6">
          <p className="text-foreground/60 text-sm mb-2 font-bold uppercase tracking-wider">ACOS MÃ©dio</p>
          <p className="text-4xl font-bold text-white">{getAvg('ACOS')}%</p>
          <p className="text-xs text-accent-gold mt-2 flex items-center gap-1">Custo percentual ok</p>
        </motion.div>
        <motion.div layout className="bg-secondary/50 border border-white/5 rounded-2xl p-6">
          <p className="text-foreground/60 text-sm mb-2 font-bold uppercase tracking-wider">Taxa de ConversÃ£o</p>
          <p className="text-4xl font-bold text-white">{getAvg('Conv')}%</p>
          <p className="text-xs text-brand-blue mt-2 flex items-center gap-1">Acima do mercado</p>
        </motion.div>
      </div>

      {/* Recharts Chart */}
      <div className="h-[400px] w-full">
        {isMounted && (
          <ResponsiveContainer width="100%" height="100%">
            <BarChart
              data={activeData}
              margin={{ top: 20, right: 30, left: 0, bottom: 5 }}
            >
              <CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,0.05)" vertical={false} />
              <XAxis dataKey="name" stroke="rgba(255,255,255,0.5)" tick={{fill: 'rgba(255,255,255,0.5)'}} />
              <YAxis stroke="rgba(255,255,255,0.2)" tick={{fill: 'rgba(255,255,255,0.5)'}} />
              <Tooltip 
                contentStyle={{ backgroundColor: '#1A1F3A', borderColor: 'rgba(255,255,255,0.1)', borderRadius: '12px' }}
                itemStyle={{ fontWeight: 'bold' }}
              />
              <Legend wrapperStyle={{ paddingTop: '20px' }} />
              <Bar dataKey="ROAS" name="ROAS (Multiplier)" fill="#00D98E" radius={[4, 4, 0, 0]} animationDuration={1000} />
              <Bar dataKey="ACOS" name="ACOS (%)" fill="#FF4757" radius={[4, 4, 0, 0]} animationDuration={1000} />
              <Bar dataKey="Conv" name="ConversÃ£o (%)" fill="#2196F3" radius={[4, 4, 0, 0]} animationDuration={1000} />
            </BarChart>
          </ResponsiveContainer>
        )}
      </div>

      {season === 'black_friday' && (
        <motion.div 
          initial={{ opacity: 0, y: 10 }} 
          animate={{ opacity: 1, y: 0 }}
          className="mt-8 bg-accent-red/10 border border-accent-red/20 rounded-xl p-4 flex items-start gap-4"
        >
          <AlertTriangle className="text-accent-red mt-1 shrink-0" />
          <div>
            <h4 className="text-accent-red font-bold text-sm mb-1">Alerta EstratÃ©gico de Black Friday</h4>
            <p className="text-xs text-white/80">O ACOS cai substancialmente devido Ã  alta conversÃ£o (efeito awareness da data). EstratÃ©gia de margem agressiva recomendada: aumentar o spend mantendo a margem de produto um pouco menor = maior lucro lÃ­quido total.</p>
          </div>
        </motion.div>
      )}

    </div>
  )
}


ARQUIVO: src\components\interactive\KanbanSimulator.tsx

'use client'

import { motion } from 'framer-motion'
import { useState } from 'react'
import { FileWarning, CheckCircle, ShieldAlert } from 'lucide-react'

export function KanbanSimulator() {
  const [isFaturado, setIsFaturado] = useState(false)
  const [isDragging, setIsDragging] = useState(false)

  return (
    <div className="w-full bg-black/40 rounded-3xl p-6 md:p-8 border border-white/10 backdrop-blur-md relative overflow-hidden flex flex-col min-h-[450px]">
      
      {/* Background UI Grid */}
      <div className="absolute inset-0 pointer-events-none opacity-20" style={{ 
        backgroundImage: 'linear-gradient(rgba(255, 255, 255, 0.1) 1px, transparent 1px), linear-gradient(90deg, rgba(255, 255, 255, 0.1) 1px, transparent 1px)', 
        backgroundSize: '20px 20px' 
      }}></div>

      <div className="mb-8 flex justify-between items-center z-10">
        <div>
          <h3 className="text-lg font-bold text-white mb-1 flex items-center gap-2">
            Terminal OS <span className="bg-brand-primary/20 text-brand-primary text-[10px] px-2 py-0.5 rounded-full border border-brand-primary/30 uppercase tracking-widest">Live</span>
          </h3>
          <p className="text-foreground/40 text-xs font-mono uppercase tracking-widest">Mova o card para liberar NFS-e</p>
        </div>
      </div>

      <div className="flex flex-col md:flex-row gap-6 h-full flex-1 z-10 relative">
        
        {/* COLUNA PRODUÃ‡ÃƒO (PENDENTE) */}
        <div className="flex-1 flex flex-col bg-[#0A0E27]/50 rounded-2xl border border-white/5 p-4 relative">
          <div className="flex items-center justify-between mb-4">
            <h4 className="text-xs font-bold text-white/60 uppercase tracking-widest">Pendentes (ProduÃ§Ã£o)</h4>
            <span className="bg-white/10 text-white/50 text-[10px] px-2 py-0.5 rounded-md font-mono">{!isFaturado ? '1' : '0'}</span>
          </div>

          <div className="flex-1 relative">
            {!isFaturado && (
              <motion.div
                drag="x"
                dragConstraints={{ left: 0, right: typeof window !== 'undefined' && window.innerWidth > 768 ? 200 : 0 }}
                dragSnapToOrigin
                onDragStart={() => setIsDragging(true)}
                onDragEnd={(e, info) => {
                  setIsDragging(false)
                  if (info.offset.x > 100) setIsFaturado(true)
                }}
                whileHover={{ scale: 1.02 }}
                whileTap={{ scale: 0.95, cursor: "grabbing" }}
                className="absolute w-full z-20 cursor-grab bg-gradient-to-br from-white/10 to-white/5 border border-white/20 p-5 rounded-xl shadow-[0_8px_30px_rgba(0,0,0,0.5)] backdrop-blur-md"
              >
                <div className="flex justify-between items-center mb-4">
                  <span className="bg-brand-primary/20 text-brand-primary text-[10px] px-2 py-1 rounded-md font-bold uppercase tracking-widest border border-brand-primary/20">
                    Urgente
                  </span>
                  <span className="text-foreground/40 text-xs font-mono">#OS-2026</span>
                </div>
                
                <h5 className="font-bold text-white text-lg leading-tight mb-2">Ordem MÃ£e: Setup Infra</h5>
                <p className="text-foreground/50 text-xs mb-4">TÃ©cnico designado: Eduardo L.</p>
                
                <div className="flex items-center gap-2 text-[10px] text-foreground/40 uppercase tracking-wider font-bold">
                  <ShieldAlert className="w-3 h-3 text-amber-500" />
                  Trava Fiscal (Sem XML)
                </div>
              </motion.div>
            )}

            {/* Ghost outline while dragging */}
            {!isFaturado && isDragging && (
              <div className="absolute inset-0 border-2 border-dashed border-white/20 rounded-xl flex items-center justify-center bg-white/5"></div>
            )}
          </div>
        </div>

        {/* COLUNA FATURADO (DROPZONE) */}
        <div className={`flex-1 flex flex-col bg-[#0A0A0A]/80 rounded-2xl border border-white/5 p-4 transition-all duration-500 relative ${isDragging ? 'ring-2 ring-brand-success/50 bg-brand-success/5' : ''}`}>
          
          <div className="flex items-center justify-between mb-4 relative z-10">
            <h4 className={`text-xs font-bold uppercase tracking-widest transition-colors ${isDragging ? 'text-brand-success' : 'text-white/60'}`}>
              Faturamento (Fiscal)
            </h4>
            <span className={`text-[10px] px-2 py-0.5 rounded-md font-mono transition-colors ${isDragging ? 'bg-brand-success/20 text-brand-success' : 'bg-white/10 text-white/50'}`}>
              {isFaturado ? '1' : '0'}
            </span>
          </div>

          <div className="flex-1 flex flex-col relative justify-center items-center rounded-xl">
            {/* Glow Dropzone when dragging */}
            {isDragging && !isFaturado && (
              <motion.div 
                initial={{ opacity: 0 }}
                animate={{ opacity: 1 }}
                className="absolute inset-0 bg-brand-success/10 rounded-xl border border-brand-success/30 flex items-center justify-center flex-col gap-3"
              >
                <div className="w-12 h-12 rounded-full bg-brand-success/20 flex items-center justify-center animate-pulse">
                  <CheckCircle className="w-6 h-6 text-brand-success drop-shadow-[0_0_8px_rgba(0,217,142,0.8)]" />
                </div>
                <span className="text-brand-success font-bold uppercase tracking-widest text-xs drop-shadow-md">Solte aqui para Faturar</span>
              </motion.div>
            )}

            {!isFaturado && !isDragging && (
               <div className="text-center opacity-30">
                 <FileWarning className="w-8 h-8 text-white mx-auto mb-3 opacity-50" />
                 <p className="text-xs font-bold uppercase tracking-widest text-white">Aguardando OS</p>
               </div>
            )}

            {isFaturado && (
              <motion.div
                initial={{ opacity: 0, scale: 0.8, rotateX: 20 }}
                animate={{ opacity: 1, scale: 1, rotateX: 0 }}
                transition={{ type: "spring", stiffness: 300, damping: 20 }}
                className="w-full bg-gradient-to-br from-brand-success/20 to-brand-success/5 p-6 rounded-xl border border-brand-success/30 shadow-[0_0_30px_rgba(0,217,142,0.15)] relative overflow-hidden"
              >
                <div className="absolute top-0 right-0 w-32 h-32 bg-brand-success/20 blur-2xl rounded-full translate-x-1/2 -translate-y-1/2 pointer-events-none" />
                
                <div className="flex items-center gap-3 mb-4 relative z-10">
                  <div className="bg-brand-success rounded-full p-1 drop-shadow-[0_0_8px_rgba(0,217,142,0.8)]">
                    <CheckCircle className="w-5 h-5 text-black" />
                  </div>
                  <h4 className="font-bold text-white text-lg">Ordens Filhas Geradas</h4>
                </div>

                <div className="space-y-3 relative z-10">
                  <div className="flex justify-between items-center text-xs">
                    <span className="text-foreground/50 uppercase tracking-widest font-bold">Status Webhook</span>
                    <span className="text-brand-success font-mono font-bold">200 OK</span>
                  </div>
                  <div className="flex justify-between items-center text-xs pt-3 border-t border-brand-success/20">
                    <span className="text-foreground/50 uppercase tracking-widest font-bold">ValidaÃ§Ã£o Sefaz</span>
                    <span className="text-white font-mono font-bold">#0094821</span>
                  </div>
                </div>

                <div className="mt-6 pt-4 border-t border-brand-success/20 relative z-10">
                  <button 
                    onClick={() => setIsFaturado(false)}
                    className="w-full py-2 bg-black/40 hover:bg-black/60 transition-colors rounded-lg text-xs font-bold tracking-widest text-brand-success uppercase border border-brand-success/30"
                  >
                    Resetar Ambiente
                  </button>
                </div>
              </motion.div>
            )}
          </div>
          
        </div>

      </div>
    </div>
  )
}


ARQUIVO: src\components\interactive\LiveSvgFlowchart.tsx

'use client'

import { motion } from 'framer-motion'
import { Users, Database, ShoppingCart, RefreshCcw } from 'lucide-react'

export function LiveSvgFlowchart() {
  return (
    <div className="w-full bg-[#0A0E27] rounded-3xl p-8 lg:p-12 border border-white/5 shadow-2xl relative overflow-hidden flex flex-col items-center">
      <div className="absolute inset-0 opacity-20 bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-brand-blue via-transparent to-transparent" />
      
      <div className="w-full mb-12 relative z-10 text-center">
        <h3 className="text-2xl font-bold text-white mb-2">Projeto Edusen & Bom de Compras</h3>
        <p className="text-foreground/60 text-sm max-w-lg mx-auto">
          TransiÃ§Ã£o B2B/B2C: Do fluxo fÃ­sico para E-commerce e Plataforma LMS.
        </p>
      </div>

      <div className="relative w-full max-w-4xl h-[400px] flex items-center justify-between z-10 px-4">
        
        {/* Animated Background Path connecting all nodes */}
        <svg className="absolute inset-0 w-full h-full pointer-events-none" preserveAspectRatio="none">
          {/* Path 1: Lead -> CRM */}
          <path 
            d="M 12%,50% Q 25%,50% 36%,50%" 
            stroke="rgba(255,255,255,0.1)" strokeWidth="2" strokeDasharray="5 5" fill="none" 
          />
          {/* Mapped Flow Lead -> CRM */}
          <motion.circle
            cx="0" cy="0" r="4" fill="#2196F3"
            animate={{
              offsetDistance: ["0%", "100%"]
            }}
            transition={{ duration: 2, repeat: Infinity, ease: "linear" }}
            style={{ offsetPath: "path('M 12%,50% Q 25%,50% 36%,50%')" }}
          />

          {/* Path 2: CRM -> Venda */}
          <path 
            d="M 64%,50% Q 75%,50% 88%,50%" 
            stroke="rgba(255,255,255,0.1)" strokeWidth="2" strokeDasharray="5 5" fill="none" 
          />
          <motion.circle
            cx="0" cy="0" r="4" fill="#00D98E"
            animate={{
              offsetDistance: ["0%", "100%"]
            }}
            transition={{ duration: 2, repeat: Infinity, ease: "linear", delay: 1 }}
            style={{ offsetPath: "path('M 64%,50% Q 75%,50% 88%,50%')" }}
          />
          
          {/* Path 3: RetenÃ§Ã£o Return */}
          <path 
            d="M 88%,65% C 70%,90% 30%,90% 12%,65%" 
            stroke="rgba(255,255,255,0.05)" strokeWidth="2" strokeDasharray="5 5" fill="none" 
            id="retention-path"
          />
           <motion.circle
            cx="0" cy="0" r="3" fill="#FFD700"
            animate={{
              offsetDistance: ["100%", "0%"]
            }}
            transition={{ duration: 4, repeat: Infinity, ease: "linear", delay: 2 }}
            style={{ offsetPath: "path('M 88%,65% C 70%,90% 30%,90% 12%,65%')" }}
          />
        </svg>

        {/* Node 1: Capture / Lead */}
        <motion.div 
          initial={{ opacity: 0, scale: 0.8 }}
          whileInView={{ opacity: 1, scale: 1 }}
          viewport={{ once: true }}
          className="relative flex flex-col items-center w-32 left-[4%]"
        >
          <div className="w-20 h-20 rounded-2xl bg-brand-blue/20 border-2 border-brand-blue flex items-center justify-center shadow-[0_0_20px_rgba(33,150,243,0.3)] z-10 mb-4">
            <Users className="w-8 h-8 text-brand-blue" />
          </div>
          <h4 className="font-bold text-white text-center">Captura B2C/B2B</h4>
          <span className="text-[10px] text-brand-blue font-bold px-2 py-1 bg-brand-blue/10 rounded-full mt-2 uppercase tracking-wide">FÃ­sico & Digital</span>
        </motion.div>

        {/* Node 2: CRM / GestÃ£o */}
        <motion.div 
          initial={{ opacity: 0, scale: 0.8 }}
          whileInView={{ opacity: 1, scale: 1 }}
          viewport={{ once: true }}
          transition={{ delay: 0.3 }}
          className="relative flex flex-col items-center w-40"
        >
          <div className="w-24 h-24 rounded-full bg-secondary border-2 border-white/20 flex items-center justify-center z-10 mb-4 shadow-xl">
            <Database className="w-10 h-10 text-white" />
          </div>
          <h4 className="font-bold text-white text-center">Hub CRM Corp</h4>
          <span className="text-xs text-foreground/60 text-center mt-1">IntegraÃ§Ã£o Dynamics<br/>& AutomaÃ§Ã£o n8n</span>
        </motion.div>

        {/* Node 3: Venda & RetenÃ§Ã£o */}
        <motion.div 
          initial={{ opacity: 0, scale: 0.8 }}
          whileInView={{ opacity: 1, scale: 1 }}
          viewport={{ once: true }}
          transition={{ delay: 0.6 }}
          className="relative flex flex-col items-center w-32 right-[4%]"
        >
          <div className="w-20 h-20 rounded-2xl bg-accent-green/20 border-2 border-accent-green flex items-center justify-center shadow-[0_0_20px_rgba(0,217,142,0.3)] z-10 mb-4">
            <ShoppingCart className="w-8 h-8 text-accent-green" />
          </div>
          <h4 className="font-bold text-white text-center">LMS / E-com</h4>
          <div className="flex items-center gap-1 text-[10px] text-accent-gold font-bold px-2 py-1 bg-accent-gold/10 rounded-full mt-2 uppercase tracking-wide">
            <RefreshCcw className="w-3 h-3" />
            LTV Loop
          </div>
        </motion.div>

      </div>
    </div>
  )
}


ARQUIVO: src\components\interactive\MarketplaceDataGrid.tsx

'use client'

import { useState } from 'react'
import { motion, AnimatePresence } from 'framer-motion'
import { Play, Check, TrendingUp, TrendingDown, Loader2 } from 'lucide-react'

// â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
// Benchmarks confirmados (Marketplace_Ecosystem_meu.xlsx):
//   ML ACOS real: 4.4% | ML ROAS real: 22.7x
// Dados semanais abaixo sÃ£o representativos / simulados para fins de
// demonstraÃ§Ã£o de metodologia. VariaÃ§Ã£o intencional para mostrar anÃ¡lise
// de desvio, correÃ§Ã£o de curso e impacto de decisÃµes de ADS.
// â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€

type CellState = 'done' | 'running' | 'pending'

interface WeekRow {
  week: string
  meta: number
  real: number | null
  invAds: number | null
  acos: number | null
  roas: number | null
  ticket: number | null
  state: CellState
}

const ACOS_BENCHMARK = 5.0  // â‰¤5% = on target
const ROAS_BENCHMARK = 20.0 // â‰¥20x = healthy

const BASE_ROWS: WeekRow[] = [
  // â”€â”€ S01â€“S08: semanas consolidadas â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  { week: 'S01', meta: 82000,  real: 79400,  invAds: 3200, acos: 4.8, roas: 24.8, ticket: 231, state: 'done' },
  { week: 'S02', meta: 82000,  real: 87600,  invAds: 3450, acos: 3.9, roas: 25.4, ticket: 258, state: 'done' },
  { week: 'S03', meta: 86000,  real: 83100,  invAds: 3900, acos: 4.7, roas: 21.3, ticket: 244, state: 'done' },
  { week: 'S04', meta: 86000,  real: 91800,  invAds: 3600, acos: 3.9, roas: 25.5, ticket: 267, state: 'done' },
  { week: 'S05', meta: 90000,  real: 94200,  invAds: 4100, acos: 4.4, roas: 23.0, ticket: 272, state: 'done' },
  { week: 'S06', meta: 90000,  real: 86500,  invAds: 4800, acos: 5.5, roas: 18.0, ticket: 241, state: 'done' }, // â† ACOS estoura: campus de anÃ¡lise
  { week: 'S07', meta: 94000,  real: 98100,  invAds: 4200, acos: 4.3, roas: 23.4, ticket: 283, state: 'done' },
  { week: 'S08', meta: 94000,  real: 96300,  invAds: 4050, acos: 4.2, roas: 23.8, ticket: 278, state: 'done' },
  // â”€â”€ S09â€“S12: semanas projetadas (macro preenche) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  { week: 'S09', meta: 98000,  real: null, invAds: null, acos: null, roas: null, ticket: null, state: 'pending' },
  { week: 'S10', meta: 98000,  real: null, invAds: null, acos: null, roas: null, ticket: null, state: 'pending' },
  { week: 'S11', meta: 102000, real: null, invAds: null, acos: null, roas: null, ticket: null, state: 'pending' },
  { week: 'S12', meta: 102000, real: null, invAds: null, acos: null, roas: null, ticket: null, state: 'pending' },
]

// ProjeÃ§Ãµes injetadas pela macro (baseadas em tendÃªncia das 8 semanas)
const MACRO_FILL: Partial<WeekRow>[] = [
  { real: 103200, invAds: 4400, acos: 4.3, roas: 23.5, ticket: 289 },
  { real: 97800,  invAds: 4600, acos: 4.8, roas: 21.3, ticket: 274 },
  { real: 108500, invAds: 4800, acos: 4.1, roas: 22.6, ticket: 295 },
  { real: 111200, invAds: 5000, acos: 4.5, roas: 22.2, ticket: 301 },
]

const fmtCurr = (n: number) =>
  `R$\u00a0${n.toLocaleString('pt-BR', { maximumFractionDigits: 0 })}`

function atg(meta: number, real: number | null): number | null {
  if (real === null) return null
  return ((real - meta) / meta) * 100
}

export function MarketplaceDataGrid() {
  const [rows, setRows] = useState<WeekRow[]>(BASE_ROWS)
  const [scanLine, setScanLine] = useState<number | null>(null)
  const [isRunning, setIsRunning] = useState(false)
  const [done, setDone] = useState(false)
  const [selected, setSelected] = useState<string | null>(null)

  const runMacro = async () => {
    if (isRunning || done) return
    setIsRunning(true)

    const pending = rows.map((r, i) => ({ ...r, i })).filter(r => r.state === 'pending')
    for (let p = 0; p < pending.length; p++) {
      const idx = pending[p].i
      setScanLine(idx)
      setRows(prev => prev.map((r, i) => i === idx ? { ...r, state: 'running' } : r))
      await new Promise(res => setTimeout(res, 700))
      const fill = MACRO_FILL[p] ?? {}
      setRows(prev => prev.map((r, i) => i === idx ? { ...r, ...fill, state: 'done' } : r))
      await new Promise(res => setTimeout(res, 200))
    }

    setScanLine(null)
    setIsRunning(false)
    setDone(true)
  }

  const reset = () => { setRows(BASE_ROWS); setDone(false); setScanLine(null) }

  const hasPending = rows.some(r => r.state === 'pending')
  const doneRows = rows.filter(r => r.real !== null)
  const totalMeta = rows.reduce((s, r) => s + r.meta, 0)
  const totalReal = doneRows.reduce((s, r) => s + (r.real ?? 0), 0)
  const overallAtg = doneRows.length > 0
    ? ((totalReal - doneRows.reduce((s, r) => s + r.meta, 0)) / doneRows.reduce((s, r) => s + r.meta, 0)) * 100
    : null

  return (
    <div className="w-full rounded-3xl border border-white/5 bg-[#080808] overflow-hidden">

      {/* TOOLBAR */}
      <div className="flex flex-col sm:flex-row items-start sm:items-center justify-between px-6 py-4 border-b border-white/5 bg-white/[0.02] gap-3">
        <div className="flex items-center gap-3">
          <div className="w-3 h-3 rounded-full bg-red-500/70" />
          <div className="w-3 h-3 rounded-full bg-yellow-500/70" />
          <div className="w-3 h-3 rounded-full bg-green-500/70" />
          <span className="ml-2 text-white/30 text-xs font-mono">Marketplace_Ecosystem_meu.xlsx â€” SEMANAL</span>
        </div>
        <div className="flex items-center gap-3">
          {done && <button onClick={reset} className="text-xs text-white/30 hover:text-white font-mono transition-colors">â†º Reset</button>}
          <button
            onClick={runMacro}
            disabled={isRunning || done || !hasPending}
            className={`flex items-center gap-2 px-4 py-2 rounded-xl text-xs font-bold transition-all duration-300
              ${done
                ? 'bg-brand-success/10 text-brand-success border border-brand-success/30'
                : isRunning || !hasPending
                  ? 'bg-white/5 text-white/20 border border-white/5 cursor-not-allowed'
                  : 'bg-brand-primary text-black border border-brand-primary hover:shadow-[0_0_20px_rgba(255,215,0,0.35)] hover:scale-105'
              }`}
          >
            {done
              ? <><Check className="w-3.5 h-3.5" /> ConsolidaÃ§Ã£o ConcluÃ­da</>
              : isRunning
                ? <><Loader2 className="w-3.5 h-3.5 animate-spin" /> Processando...</>
                : <><Play className="w-3.5 h-3.5" /> Run Consolidation Macro</>
            }
          </button>
        </div>
      </div>

      {/* SUMMARY */}
      <div className="grid grid-cols-3 divide-x divide-white/5 border-b border-white/5">
        {[
          { label: 'Meta Acum.', value: fmtCurr(totalMeta), color: 'text-white/60' },
          { label: 'Real Acum.', value: totalReal > 0 ? fmtCurr(totalReal) : 'â€”â€”', color: totalReal >= doneRows.reduce((s,r)=>s+r.meta,0) ? 'text-brand-success' : 'text-red-400' },
          { label: 'ATG Geral', value: overallAtg !== null ? `${overallAtg >= 0 ? '+' : ''}${overallAtg.toFixed(1)}%` : 'â€”â€”', color: overallAtg !== null && overallAtg >= 0 ? 'text-brand-success' : 'text-red-400' },
        ].map(s => (
          <div key={s.label} className="px-6 py-3 text-center">
            <p className="text-[10px] uppercase tracking-widest text-white/30 mb-0.5">{s.label}</p>
            <p className={`text-sm font-black font-mono ${s.color}`}>{s.value}</p>
          </div>
        ))}
      </div>

      {/* TABLE */}
      <div className="overflow-x-auto">
        <table className="w-full min-w-[720px] text-xs font-mono border-collapse">
          <thead>
            <tr className="border-b border-white/5">
              {['Sem.','Meta (R$)','Real (R$)','ATG%','Inv. ADS','ACOS%','ROAS','Ticket MÃ©d.'].map((h, i) => (
                <th key={h} className={`px-3 py-3 text-[10px] uppercase tracking-widest text-white/30 font-bold ${i === 0 ? 'text-center' : 'text-right'}`}>{h}</th>
              ))}
            </tr>
          </thead>
          <tbody>
            {rows.map((row, idx) => {
              const pct = atg(row.meta, row.real)
              const isScan = scanLine === idx
              const isRunRow = row.state === 'running'
              const isPending = row.state === 'pending'

              return (
                <motion.tr
                  key={row.week}
                  onClick={() => setSelected(selected === row.week ? null : row.week)}
                  className={`border-b border-white/[0.03] cursor-pointer relative transition-colors
                    ${selected === row.week ? 'bg-brand-primary/5' : 'hover:bg-white/[0.02]'}`}
                >
                  {/* Scan overlay */}
                  {isScan && (
                    <motion.td colSpan={8} className="absolute inset-0 pointer-events-none p-0 m-0">
                      <motion.div
                        className="w-full h-full"
                        style={{ background: 'linear-gradient(90deg,transparent,rgba(255,215,0,0.07),rgba(255,215,0,0.12),rgba(255,215,0,0.07),transparent)' }}
                        animate={{ opacity: [0.5, 1, 0.5] }}
                        transition={{ duration: 0.45, repeat: Infinity }}
                      />
                    </motion.td>
                  )}

                  <td className="px-3 py-3 text-center">
                    <span className={`font-bold ${isPending ? 'text-white/20' : 'text-white'}`}>{row.week}</span>
                  </td>
                  <td className="px-3 py-3 text-right text-white/50">{fmtCurr(row.meta)}</td>

                  {/* Real */}
                  <td className="px-3 py-3 text-right">
                    {isRunRow
                      ? <motion.span className="text-brand-primary" animate={{ opacity: [0.3,1,0.3] }} transition={{ duration:0.4,repeat:Infinity }}>Â·Â·Â·</motion.span>
                      : row.real !== null
                        ? <motion.span initial={{ color: '#00D98E' }} animate={{ color: '#ffffff99' }} transition={{ duration: 2 }}>{fmtCurr(row.real)}</motion.span>
                        : <span className="text-white/15">â€”â€”</span>
                    }
                  </td>

                  {/* ATG */}
                  <td className="px-3 py-3 text-right">
                    {pct !== null
                      ? <span className={`font-bold flex items-center justify-end gap-1 ${pct >= 0 ? 'text-brand-success' : 'text-red-400'}`}>
                          {pct >= 0 ? <TrendingUp className="w-3 h-3" /> : <TrendingDown className="w-3 h-3" />}
                          {pct >= 0 ? '+' : ''}{pct.toFixed(1)}%
                        </span>
                      : <span className="text-white/15">â€”â€”</span>
                    }
                  </td>

                  {/* Inv ADS */}
                  <td className="px-3 py-3 text-right">
                    {row.invAds !== null ? fmtCurr(row.invAds) : <span className="text-white/15">â€”â€”</span>}
                  </td>

                  {/* ACOS */}
                  <td className="px-3 py-3 text-right">
                    {row.acos !== null
                      ? <span className={row.acos <= ACOS_BENCHMARK ? 'text-brand-success font-bold' : row.acos <= 6 ? 'text-brand-primary font-bold' : 'text-red-400 font-bold'}>
                          {row.acos.toFixed(1)}%
                        </span>
                      : <span className="text-white/15">â€”â€”</span>
                    }
                  </td>

                  {/* ROAS */}
                  <td className="px-3 py-3 text-right">
                    {row.roas !== null
                      ? <span className={row.roas >= ROAS_BENCHMARK ? 'text-white/80' : 'text-red-400'}>{row.roas.toFixed(1)}x</span>
                      : <span className="text-white/15">â€”â€”</span>
                    }
                  </td>

                  {/* Ticket */}
                  <td className="px-3 py-3 text-right">
                    {row.ticket !== null ? `R$\u00a0${row.ticket}` : <span className="text-white/15">â€”â€”</span>}
                  </td>
                </motion.tr>
              )
            })}
          </tbody>
        </table>
      </div>

      {/* LEGEND */}
      <div className="flex items-center justify-between flex-wrap gap-3 px-6 py-3 border-t border-white/5 bg-white/[0.02]">
        <div className="flex items-center gap-4 flex-wrap text-[10px] text-white/30">
          <div className="flex items-center gap-1.5"><span className="w-2 h-2 rounded-sm bg-brand-success/60" />ACOS â‰¤{ACOS_BENCHMARK}%</div>
          <div className="flex items-center gap-1.5"><span className="w-2 h-2 rounded-sm bg-brand-primary/60" />ACOS Alerta</div>
          <div className="flex items-center gap-1.5"><span className="w-2 h-2 rounded-sm bg-red-400/60" />Desvio CrÃ­tico</div>
        </div>
        <span className="text-[10px] text-white/20 font-mono">
          {doneRows.length}/{rows.length} semanas Â· Benchmark ML: ACOS 4.4% | ROAS 22.7x
        </span>
      </div>
    </div>
  )
}


ARQUIVO: src\components\layout\Footer.tsx

import { ArrowRight } from 'lucide-react'

export function Footer() {
  return (
    <footer className="border-t border-white/5 bg-[#080808] relative overflow-hidden mt-32">
      <div className="absolute top-0 left-1/2 -translate-x-1/2 w-[600px] h-[300px] bg-brand-blue/20 blur-[120px] rounded-full pointer-events-none" />
      
      <div className="container mx-auto px-6 py-24 text-center max-w-3xl relative z-10">
        <h2 className="text-4xl md:text-5xl font-bold tracking-tight mb-6">
          Pronto para escalar sua operaÃ§Ã£o de verdade?
        </h2>
        <p className="text-lg text-foreground/70 mb-10">
          Fale diretamente com os fundadores e descubra o impacto de uma engenharia de ponta aplicada ao seu negÃ³cio.
        </p>
        <a 
          href="https://wa.me/5511999999999" 
          target="_blank" 
          rel="noopener noreferrer"
          className="inline-flex items-center justify-center rounded-md bg-brand-blue text-white px-8 py-4 text-base font-semibold transition-all hover:bg-brand-blue/90 glow-blue"
        >
          Falar com a Diretoria
          <ArrowRight className="ml-2 w-5 h-5" />
        </a>
      </div>
      
      <div className="border-t border-white/5 py-8 text-center text-sm text-foreground/50 relative z-10">
        &copy; {new Date().getFullYear()} Ecossistema Studio. Todos os direitos reservados.
      </div>
    </footer>
  )
}


ARQUIVO: src\components\layout\Header.tsx

'use client';

import Link from 'next/link'
import { Menu, ArrowRight } from 'lucide-react'
import { motion } from 'framer-motion'
import { usePathname } from 'next/navigation'

const navLinks = [
  { path: '/', label: 'Home' },
  { path: '/engenharia', label: 'Engenharia' },
  { path: '/ecommerce', label: 'E-commerce' },
  { path: '/processos', label: 'Processos & IA' },
  { path: '/interfaces', label: 'Interfaces' }
];

export function Header() {
  const pathname = usePathname();

  return (
    <header className="fixed top-0 w-full z-50 glass border-b border-white/5 transition-all duration-300">
      <div className="container mx-auto px-6 h-20 flex items-center justify-between">
        <Link href="/" className="text-xl font-bold tracking-tighter hover:opacity-80 transition-opacity">
          EDUARDO<span className="text-brand-primary">.</span>LOURENÃ‡O
        </Link>

        {/* Desktop Navigation */}
        <nav className="hidden lg:flex items-center gap-1">
          {navLinks.map((link) => {
            const isActive = pathname === link.path;
            return (
              <Link 
                key={link.path} 
                href={link.path} 
                className={`relative px-4 py-2 text-xs font-bold uppercase tracking-wider rounded-full transition-colors ${
                  isActive ? 'text-white' : 'text-foreground/60 hover:text-white hover:bg-white/5'
                }`}
              >
                {isActive && (
                  <motion.div
                    layoutId="active-nav"
                    className="absolute inset-0 bg-white/10 rounded-full"
                    transition={{ type: 'spring', stiffness: 380, damping: 30 }}
                  />
                )}
                <span className="relative z-10">{link.label}</span>
              </Link>
            )
          })}
        </nav>

        <div className="flex items-center gap-4">
          <Link href="/contato" className="group hidden md:inline-flex items-center justify-center rounded-full bg-brand-primary/10 border border-brand-primary/30 text-brand-primary px-6 py-2.5 text-sm font-bold transition-all hover:bg-brand-primary hover:text-background hover:shadow-[0_0_20px_rgba(255,215,0,0.4)]">
            Start a Project
            <ArrowRight className="w-4 h-4 ml-2 group-hover:translate-x-1 transition-transform" />
          </Link>
          <button className="lg:hidden p-2 text-foreground/80 hover:text-white rounded-md bg-white/5 border border-white/10">
            <Menu className="w-5 h-5" />
          </button>
        </div>
      </div>
    </header>
  )
}


ARQUIVO: src\components\sections\EngineeringSection.tsx

'use client'

import { motion } from 'framer-motion'
import { Server, Database, Code, CheckCircle, MousePointer2 } from 'lucide-react'
import { useEffect, useState } from 'react'

export function EngineeringSection() {
  const [key, setKey] = useState(0)

  // Loop the animation every 5 seconds
  useEffect(() => {
    const interval = setInterval(() => {
      setKey(prev => prev + 1)
    }, 5000)
    return () => clearInterval(interval)
  }, [])

  return (
    <section id="engenharia" className="py-24 relative overflow-hidden bg-[#080808]">
      <div className="container mx-auto px-6">
        <div className="flex flex-col lg:flex-row gap-16 items-center">
          
          {/* Content side */}
          <div className="lg:w-1/2">
            <motion.h2 
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="text-3xl md:text-5xl font-bold mb-6"
            >
              Engenharia de Software para <span className="text-brand-blue">OperaÃ§Ãµes Complexas</span>
            </motion.h2>
            <motion.p 
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="text-lg text-foreground/70 mb-10"
            >
              Sistemas de Ordem de ServiÃ§o, Dashboards e Plataformas Web que nÃ£o quebram quando a sua empresa cresce. Para indÃºstrias e e-commerces que o software de prateleira nÃ£o atende mais.
            </motion.p>
            
            <div className="space-y-8">
              <motion.div 
                initial={{ opacity: 0, x: -20 }}
                whileInView={{ opacity: 1, x: 0 }}
                viewport={{ once: true }}
                className="flex gap-4"
              >
                <div className="shrink-0 w-12 h-12 rounded-full bg-brand-blue/10 flex items-center justify-center text-brand-blue border border-brand-blue/20">
                  <Server className="w-6 h-6" />
                </div>
                <div>
                  <h4 className="text-xl font-bold mb-2">Desenvolvimento Customizado (OS)</h4>
                  <p className="text-foreground/70">Portais Web, BDs e integraÃ§Ãµes API REST (SAP, TOTVS, Bling). Sistemas de Ordem de ServiÃ§o dinÃ¢micos sem mensalidades abusivas.</p>
                </div>
              </motion.div>
              
              <motion.div 
                initial={{ opacity: 0, x: -20 }}
                whileInView={{ opacity: 1, x: 0 }}
                viewport={{ once: true }}
                transition={{ delay: 0.1 }}
                className="flex gap-4"
              >
                <div className="shrink-0 w-12 h-12 rounded-full bg-brand-blue/10 flex items-center justify-center text-brand-blue border border-brand-blue/20">
                  <Code className="w-6 h-6" />
                </div>
                <div>
                  <h4 className="text-xl font-bold mb-2">Performance E-commerce (CRO)</h4>
                  <p className="text-foreground/70">RefatoraÃ§Ã£o Next.js, Headless Checkout e tagueamento avanÃ§ado focados em Core Web Vitals e aumento de margem.</p>
                </div>
              </motion.div>

              <motion.div 
                initial={{ opacity: 0, x: -20 }}
                whileInView={{ opacity: 1, x: 0 }}
                viewport={{ once: true }}
                transition={{ delay: 0.2 }}
                className="flex gap-4"
              >
                <div className="shrink-0 w-12 h-12 rounded-full bg-brand-blue/10 flex items-center justify-center text-brand-blue border border-brand-blue/20">
                  <Database className="w-6 h-6" />
                </div>
                <div>
                  <h4 className="text-xl font-bold mb-2">Canais Digitais & Marketplaces</h4>
                  <p className="text-foreground/70">Ecossistema de integraÃ§Ãµes para Mercado Livre/Amazon com controle simultÃ¢neo de 200+ SKUs.</p>
                </div>
              </motion.div>
            </div>
          </div>

          {/* Interactive Kanban Simulator */}
          <div className="lg:w-1/2 w-full perspective-1000">
            <motion.div 
              initial={{ opacity: 0, rotateY: 20 }}
              whileInView={{ opacity: 1, rotateY: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.8 }}
              className="glass rounded-2xl p-8 border border-brand-blue/20 relative overflow-hidden glow-blue" 
              key={key}
            >
              <div className="mb-8">
                <h3 className="text-2xl font-bold text-white mb-2">Case: Chopeiras Memo</h3>
                <p className="text-sm text-brand-blue/80">O Motor da Maior IndÃºstria da AmÃ©rica Latina</p>
              </div>

              <div className="grid grid-cols-2 gap-6 h-[280px]">
                {/* Column 1 */}
                <div className="bg-black/50 rounded-xl p-4 border border-white/5 relative">
                  <h4 className="text-sm font-semibold text-foreground/50 mb-4 tracking-wider">AGUARDANDO LAUDO</h4>
                  
                  {/* The moving card */}
                  <motion.div 
                    initial={{ x: 0, y: 0, scale: 1 }}
                    animate={{ 
                      x: [0, 0, "115%", "115%"], // Wait, grab, move right to next column, drop
                      y: [0, -10, -10, 0],
                      scale: [1, 1.05, 1.05, 1],
                      boxShadow: ["0 0 0 rgba(0,0,0,0)", "0 20px 25px -5px rgba(0, 112, 243, 0.4)", "0 20px 25px -5px rgba(0, 112, 243, 0.4)", "0 0 0 rgba(0,0,0,0)"]
                    }}
                    transition={{ duration: 2, times: [0, 0.2, 0.7, 0.9], delay: 1 }}
                    className="bg-[#141414] border border-white/10 rounded-lg p-4 cursor-grab relative z-10 w-full"
                  >
                    <div className="text-xs text-brand-blue font-mono mb-3">OT #1042</div>
                    <div className="h-2 bg-white/10 rounded w-3/4 mb-3"></div>
                    <div className="h-2 bg-white/10 rounded w-1/2"></div>
                  </motion.div>

                  {/* Placeholder under the moving card */}
                  <div className="absolute top-[48px] left-4 right-4 bg-white/5 border border-white/5 border-dashed rounded-lg h-[88px] -z-10"></div>
                </div>

                {/* Column 2 */}
                <div className="bg-black/50 rounded-xl p-4 border border-brand-blue/30 relative flex flex-col items-center">
                  <h4 className="text-sm font-semibold text-brand-blue mb-4 tracking-wider w-full text-left">FATURADO</h4>
                  
                  {/* NF Vinculada Badge (Animated popup) */}
                  <motion.div
                    initial={{ scale: 0, opacity: 0 }}
                    animate={{ scale: [0, 1.2, 1], opacity: 1 }}
                    transition={{ duration: 0.5, delay: 3 }}
                    className="absolute top-[80px] z-20 flex items-center justify-center gap-2 bg-brand-emerald text-black px-4 py-2 rounded-full font-bold text-sm shadow-[0_0_20px_rgba(16,185,129,0.4)]"
                  >
                    <CheckCircle className="w-5 h-5" />
                    NF Vinculada
                  </motion.div>
                </div>
              </div>

              {/* Fake Cursor */}
              <motion.div
                initial={{ x: 60, y: 180, opacity: 0 }}
                animate={{ 
                  x: [60, 60, "160%", "160%", 60], 
                  y: [180, 130, 130, 180, 180],
                  opacity: [0, 1, 1, 0, 0]
                }}
                transition={{ duration: 3.5, times: [0, 0.2, 0.6, 0.8, 1], delay: 0.5 }}
                className="absolute z-30 pointer-events-none text-white drop-shadow-2xl"
              >
                <MousePointer2 fill="white" className="w-8 h-8 rotate-[-15deg]" />
              </motion.div>
            </motion.div>
          </div>
        </div>
      </div>
    </section>
  )
}


ARQUIVO: src\components\sections\Founders.tsx

'use client'

import { motion } from 'framer-motion'
import { Code2, Settings, Terminal, Database, FileSpreadsheet, Key, Network, Users } from 'lucide-react'

export function Founders() {
  return (
    <section id="sobre" className="py-24 relative overflow-hidden bg-background">
      <div className="container mx-auto px-6 max-w-6xl relative z-10">
        <div className="text-center mb-16">
          <motion.h2 
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            className="text-3xl md:text-5xl font-bold mb-4"
          >
            Mentes de grande agÃªncia.<br/><span className="text-foreground/50">Velocidade de uma consultoria boutique.</span>
          </motion.h2>
        </div>

        <div className="grid md:grid-cols-2 gap-8">
          
          {/* Eduardo Card */}
          <motion.div 
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            className="group relative glass rounded-3xl p-8 md:p-12 border border-white/5 hover:border-brand-blue/30 transition-all duration-500 overflow-hidden"
          >
            <div className="absolute inset-0 bg-gradient-to-br from-brand-blue/10 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
            
            <div className="relative z-10">
              <div className="w-20 h-20 rounded-full bg-brand-blue/10 flex items-center justify-center mb-8 text-brand-blue border border-brand-blue/20 group-hover:glow-blue transition-all">
                <Code2 className="w-10 h-10" />
              </div>
              <h3 className="text-3xl font-bold mb-2 text-white">Eduardo LourenÃ§o</h3>
              <p className="text-brand-blue font-medium mb-6">Arquiteto de Software & EstratÃ©gia Digital</p>
              
              <p className="text-foreground/70 mb-10 min-h-[100px]">
                Focado em resolver gargalos tecnolÃ³gicos e criar caminhos prÃ¡ticos para o crescimento atravÃ©s de cÃ³digo escalÃ¡vel. Acredita que software bom Ã© aquele que reduz custo operacional.
              </p>

              {/* Tech Stack Reveal */}
              <div className="space-y-4">
                <p className="text-sm font-semibold text-foreground/50 uppercase tracking-widest">Stack TÃ©cnico</p>
                <div className="flex flex-wrap gap-3">
                  {[
                    { name: 'React / Next.js', icon: <Terminal className="w-4 h-4"/> },
                    { name: 'NestJS / Node', icon: <Settings className="w-4 h-4"/> },
                    { name: 'Prisma DB', icon: <Database className="w-4 h-4"/> },
                    { name: 'AWS Cloud', icon: <Network className="w-4 h-4"/> },
                  ].map((tech, i) => (
                    <motion.div 
                      key={i}
                      initial={{ opacity: 0.6, y: 0 }}
                      whileHover={{ scale: 1.05, opacity: 1 }}
                      className="flex items-center gap-2 px-4 py-2 rounded-full bg-[#141414] border border-white/10 text-sm font-medium hover:bg-brand-blue/20 hover:border-brand-blue/50 hover:text-brand-blue transition-all cursor-default"
                    >
                      {tech.icon}
                      {tech.name}
                    </motion.div>
                  ))}
                </div>
              </div>
            </div>
          </motion.div>

          {/* Paloma Card */}
          <motion.div 
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ delay: 0.2 }}
            className="group relative glass rounded-3xl p-8 md:p-12 border border-white/5 hover:border-brand-emerald/30 transition-all duration-500 overflow-hidden"
          >
            <div className="absolute inset-0 bg-gradient-to-bl from-brand-emerald/10 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
            
            <div className="relative z-10">
              <div className="w-20 h-20 rounded-full bg-brand-emerald/10 flex items-center justify-center mb-8 text-brand-emerald border border-brand-emerald/20 group-hover:glow-emerald transition-all">
                <Users className="w-10 h-10" />
              </div>
              <h3 className="text-3xl font-bold mb-2 text-white">Paloma Amaral</h3>
              <p className="text-brand-emerald font-medium mb-6">Engenharia de Processos & BPO Financeiro</p>
              
              <p className="text-foreground/70 mb-10 min-h-[100px]">
                Une o rigor tÃ©cnico da tecnologia Ã  visÃ£o administrativa. Transforma processos manuais em inteligÃªncia operacional e caixa previsÃ­vel, acabando com a dependÃªncia de pessoas.
              </p>

              {/* Stack Reveal */}
              <div className="space-y-4">
                <p className="text-sm font-semibold text-foreground/50 uppercase tracking-widest">Arsenal</p>
                <div className="flex flex-wrap gap-3">
                  {[
                    { name: 'Looker Studio', icon: <FileSpreadsheet className="w-4 h-4"/> },
                    { name: 'Multicompany BPO', icon: <Key className="w-4 h-4"/> },
                    { name: 'Apps Script', icon: <Terminal className="w-4 h-4"/> },
                    { name: 'SOPs & Manuais', icon: <Settings className="w-4 h-4"/> },
                  ].map((tech, i) => (
                    <motion.div 
                      key={i}
                      initial={{ opacity: 0.6, y: 0 }}
                      whileHover={{ scale: 1.05, opacity: 1 }}
                      className="flex items-center gap-2 px-4 py-2 rounded-full bg-[#141414] border border-white/10 text-sm font-medium hover:bg-brand-emerald/20 hover:border-brand-emerald/50 hover:text-brand-emerald transition-all cursor-default"
                    >
                      {tech.icon}
                      {tech.name}
                    </motion.div>
                  ))}
                </div>
              </div>
            </div>
          </motion.div>

        </div>
      </div>
    </section>
  )
}


ARQUIVO: src\components\sections\Hero.tsx

'use client'

import { motion } from 'framer-motion'
import { ArrowDown } from 'lucide-react'

export function Hero() {
  return (
    <section className="relative min-h-[90vh] flex flex-col items-center justify-center pt-20 pb-12 overflow-hidden">
      {/* Background elements */}
      <div className="absolute top-1/2 left-0 -translate-y-1/2 w-[40vw] h-[40vw] max-w-[600px] max-h-[600px] bg-brand-blue/10 blur-[120px] rounded-full pointer-events-none" />
      <div className="absolute top-1/2 right-0 -translate-y-1/2 w-[40vw] h-[40vw] max-w-[600px] max-h-[600px] bg-brand-emerald/10 blur-[120px] rounded-full pointer-events-none" />

      <div className="container mx-auto px-6 relative z-10">
        <div className="text-center max-w-4xl mx-auto mb-16">
          <motion.h1 
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.5 }}
            className="text-5xl md:text-6xl lg:text-7xl font-bold tracking-tight mb-6"
          >
            Transformamos o caos operacional em um ecossistema digital <span className="text-transparent bg-clip-text bg-gradient-to-r from-brand-blue to-brand-emerald">lucrativo.</span>
          </motion.h1>
          <motion.p 
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.5, delay: 0.1 }}
            className="text-lg md:text-xl text-foreground/70 mb-10 max-w-3xl mx-auto"
          >
            A uniÃ£o definitiva entre Engenharia de Software de alta performance e a GestÃ£o Financeira. OperaÃ§Ãµes complexas exigem cÃ³digo escalÃ¡vel e caixa previsÃ­vel.
          </motion.p>
          <motion.a 
            href="#engenharia"
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.5, delay: 0.2 }}
            className="inline-flex items-center justify-center rounded-full bg-white/5 border border-white/10 text-white px-8 py-4 text-sm font-semibold transition-all hover:bg-white/10 hover:scale-105 backdrop-blur-md"
          >
            Conhecer o Ecossistema
            <ArrowDown className="ml-2 w-4 h-4 animate-bounce" />
          </motion.a>
        </div>

        {/* Bifurcated UI */}
        <div className="grid md:grid-cols-2 gap-6 max-w-5xl mx-auto">
          <motion.a 
            href="#engenharia"
            initial={{ opacity: 0, x: -30 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ duration: 0.6, delay: 0.3 }}
            className="group relative p-10 rounded-3xl border border-white/5 glass hover:border-brand-blue/30 transition-all duration-300 overflow-hidden"
          >
            <div className="absolute inset-0 bg-gradient-to-br from-brand-blue/10 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
            <h3 className="text-3xl font-bold mb-4 relative z-10 text-brand-blue group-hover:glow-blue duration-300">
              Engenharia & Sistemas
            </h3>
            <p className="text-foreground/70 relative z-10 text-lg">
              Arquitetura de software, sistemas web responsivos e performance extrema para operaÃ§Ãµes que nÃ£o podem parar.
            </p>
          </motion.a>

          <motion.a 
            href="#gestao"
            initial={{ opacity: 0, x: 30 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ duration: 0.6, delay: 0.4 }}
            className="group relative p-10 rounded-3xl border border-white/5 glass hover:border-brand-emerald/30 transition-all duration-300 overflow-hidden"
          >
            <div className="absolute inset-0 bg-gradient-to-bl from-brand-emerald/10 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
            <h3 className="text-3xl font-bold mb-4 relative z-10 text-brand-emerald group-hover:glow-emerald duration-300">
              GestÃ£o & Processos
            </h3>
            <p className="text-foreground/70 relative z-10 text-lg">
              BPO Financeiro, automaÃ§Ãµes de backoffice e mapeamento de rotinas operacionais para controle absoluto.
            </p>
          </motion.a>
        </div>
      </div>
    </section>
  )
}


ARQUIVO: src\components\sections\ManagementSection.tsx

'use client'

import { motion } from 'framer-motion'
import { Calculator, FileSpreadsheet, GraduationCap, MousePointer2, Play } from 'lucide-react'
import { useEffect, useState } from 'react'
import { cn } from '@/lib/utils'

export function ManagementSection() {
  const [macroExecuted, setMacroExecuted] = useState(false)
  const [key, setKey] = useState(0)

  // Loop the spreadsheet animation
  useEffect(() => {
    let timeout1: NodeJS.Timeout;
    let timeout2: NodeJS.Timeout;

    const runCycle = () => {
      setMacroExecuted(false)
      
      // Cursor clicks after 1.5s
      timeout1 = setTimeout(() => {
        setMacroExecuted(true)
      }, 1500)

      // Reset after 6s to loop
      timeout2 = setTimeout(() => {
        setKey(prev => prev + 1)
        runCycle()
      }, 6000)
    }

    runCycle()

    return () => {
      clearTimeout(timeout1)
      clearTimeout(timeout2)
    }
  }, [key])

  // Mock data for grid
  const initialChaos = [
    "-R$ 8.432,10", "PENDENTE", "ERRO", "N/A",
    "-R$ 1.200,50", "DESCONHECIDO", "N/A", "PENDENTE",
    "-R$ 45.900,00", "ATRASO", "N/A", "ERRO",
    "-R$ 3.320,00", "PENDENTE", "N/A", "ERRO"
  ]

  const organizedData = [
    "R$ +12.500,00", "CONCILIADO", "OK", "BancÃ¡rio",
    "R$ +4.200,50", "CONCILIADO", "OK", "Estoque",
    "R$ +55.900,00", "CONCILIADO", "OK", "Checkout",
    "R$ +8.320,00", "CONCILIADO", "OK", "Pix"
  ]

  return (
    <section id="gestao" className="py-24 relative overflow-hidden bg-[#0A0A0A]">
      {/* Background glow */}
      <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[60vw] h-[60vw] max-w-[800px] max-h-[800px] bg-brand-emerald/5 blur-[120px] rounded-full pointer-events-none" />

      <div className="container mx-auto px-6 relative z-10">
        <div className="flex flex-col lg:flex-row-reverse gap-16 items-center">
          
          {/* Content side */}
          <div className="lg:w-1/2">
            <motion.h2 
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="text-3xl md:text-5xl font-bold mb-6"
            >
              O Fim do Caos <span className="text-brand-emerald">Operacional e Financeiro</span>
            </motion.h2>
            <motion.p 
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="text-lg text-foreground/70 mb-10"
            >
              Estruturamos processos internos, assumimos a gestÃ£o do seu fluxo de caixa e automatizamos o seu dia a dia. Para gestores que perdem dinheiro pela falta de visÃ£o clara dos nÃºmeros.
            </motion.p>
            
            <div className="space-y-8">
              <motion.div 
                initial={{ opacity: 0, x: 20 }}
                whileInView={{ opacity: 1, x: 0 }}
                viewport={{ once: true }}
                className="flex gap-4"
              >
                <div className="shrink-0 w-12 h-12 rounded-full bg-brand-emerald/10 flex items-center justify-center text-brand-emerald border border-brand-emerald/20">
                  <Calculator className="w-6 h-6" />
                </div>
                <div>
                  <h4 className="text-xl font-bold mb-2">GestÃ£o Financeira Multicompany (BPO)</h4>
                  <p className="text-foreground/70">ConciliaÃ§Ã£o bancÃ¡ria diÃ¡ria, auditoria de contas e clareza para mÃºltiplos CNPJs simultaneamente.</p>
                </div>
              </motion.div>
              
              <motion.div 
                initial={{ opacity: 0, x: 20 }}
                whileInView={{ opacity: 1, x: 0 }}
                viewport={{ once: true }}
                transition={{ delay: 0.1 }}
                className="flex gap-4"
              >
                <div className="shrink-0 w-12 h-12 rounded-full bg-brand-emerald/10 flex items-center justify-center text-brand-emerald border border-brand-emerald/20">
                  <FileSpreadsheet className="w-6 h-6" />
                </div>
                <div>
                  <h4 className="text-xl font-bold mb-2">AutomaÃ§Ã£o de Backoffice</h4>
                  <p className="text-foreground/70">InteligÃªncia low-code (Google Workspace) para criar sistemas de controle invisÃ­veis e sem custo de licenÃ§as.</p>
                </div>
              </motion.div>

              <motion.div 
                initial={{ opacity: 0, x: 20 }}
                whileInView={{ opacity: 1, x: 0 }}
                viewport={{ once: true }}
                transition={{ delay: 0.2 }}
                className="flex gap-4"
              >
                <div className="shrink-0 w-12 h-12 rounded-full bg-brand-emerald/10 flex items-center justify-center text-brand-emerald border border-brand-emerald/20">
                  <GraduationCap className="w-6 h-6" />
                </div>
                <div>
                  <h4 className="text-xl font-bold mb-2">Arquitetura de Treinamento (SOPs)</h4>
                  <p className="text-foreground/70">Mapeamento de rotinas e criaÃ§Ã£o de Universidades Corporativas para zerar o impacto do turnover.</p>
                </div>
              </motion.div>
            </div>
          </div>

          {/* Interactive Spreadsheet Automator */}
          <div className="lg:w-1/2 w-full perspective-1000">
            <motion.div 
              initial={{ opacity: 0, rotateY: -20 }}
              whileInView={{ opacity: 1, rotateY: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.8 }}
              className="glass rounded-2xl p-6 md:p-8 border border-brand-emerald/20 relative overflow-hidden glow-emerald"
            >
              <div className="mb-6 flex justify-between items-end">
                <div>
                  <h3 className="text-2xl font-bold text-white mb-2">Base Desestruturada / ERP</h3>
                  <p className="text-sm text-brand-emerald/80">AutomaÃ§Ã£o Low-Code Ativa</p>
                </div>
                
                {/* Execute Macro Button */}
                <div className={cn(
                  "flex items-center gap-2 px-4 py-2 rounded-md font-bold text-sm transition-all duration-300 z-10",
                  macroExecuted ? "bg-brand-emerald text-black shadow-[0_0_20px_rgba(16,185,129,0.5)] scale-95" : "bg-white/10 text-white/50 border border-white/10"
                )}>
                  <Play className="w-4 h-4" />
                  <span className="hidden md:inline">Executar</span> Macro
                </div>
              </div>

              {/* Spreadsheet Grid */}
              <div className="relative rounded-lg border border-white/10 overflow-hidden bg-[#141414]">
                
                {/* Scanning Light sweep effect */}
                {macroExecuted && (
                  <motion.div 
                    initial={{ top: "-10%" }}
                    animate={{ top: "110%" }}
                    transition={{ duration: 1.5, ease: "linear" }}
                    className="absolute left-0 right-0 h-1 bg-brand-emerald shadow-[0_0_30px_10px_rgba(16,185,129,0.3)] z-20 pointer-events-none"
                  />
                )}

                <div className="grid grid-cols-4 gap-[1px] bg-white/10">
                  <div className="bg-black/80 p-2 text-[10px] md:text-xs font-semibold text-foreground/50 text-center tracking-wider">VALOR</div>
                  <div className="bg-black/80 p-2 text-[10px] md:text-xs font-semibold text-foreground/50 text-center tracking-wider">STATUS</div>
                  <div className="bg-black/80 p-2 text-[10px] md:text-xs font-semibold text-foreground/50 text-center tracking-wider">AUDITORIA</div>
                  <div className="bg-black/80 p-2 text-[10px] md:text-xs font-semibold text-foreground/50 text-center tracking-wider">ORIGEM</div>

                  {Array.from({ length: 16 }).map((_, i) => (
                    <div 
                      key={i} 
                      className="bg-[#141414] p-3 text-[10px] md:text-xs text-center font-mono flex items-center justify-center relative overflow-hidden"
                    >
                      <motion.span
                        initial={false}
                        animate={{ 
                          color: macroExecuted ? "#10B981" : "#EF4444",
                          opacity: macroExecuted ? 1 : 0.7
                        }}
                        transition={{ duration: 0.3, delay: macroExecuted ? (Math.floor(i/4) * 0.1) : 0 }}
                        className="relative z-10"
                      >
                        {macroExecuted ? organizedData[i] : initialChaos[i]}
                      </motion.span>
                      
                      {/* Cell flash effect during scan */}
                      {macroExecuted && (
                        <motion.div
                          initial={{ opacity: 0 }}
                          animate={{ opacity: [0, 0.3, 0] }}
                          transition={{ duration: 0.5, delay: Math.floor(i/4) * 0.1 }}
                          className="absolute inset-0 bg-brand-emerald/20 pointer-events-none"
                        />
                      )}
                    </div>
                  ))}
                </div>
              </div>

              {/* Fake Cursor */}
              <motion.div
                key={`cursor-${key}`}
                initial={{ left: "10%", top: "80%", opacity: 0 }}
                animate={{ 
                  left: ["10%", "10%", "85%", "85%", "10%"], 
                  top: ["80%", "80%", "8%", "8%", "80%"],
                  opacity: [0, 1, 1, 0, 0],
                  scale: [1, 1, 0.8, 1, 1]
                }}
                transition={{ duration: 4, times: [0, 0.1, 0.35, 0.5, 1] }}
                className="absolute z-30 pointer-events-none drop-shadow-2xl"
              >
                <MousePointer2 fill="white" className="text-white w-8 h-8 rotate-[-15deg]" />
              </motion.div>
            </motion.div>
          </div>

        </div>
      </div>
    </section>
  )
}


ARQUIVO: src\components\sections\MethodologyFlow.tsx

'use client'

import { motion } from 'framer-motion'
import { ShoppingCart, Package, DollarSign, ArrowRight } from 'lucide-react'

export function MethodologyFlow() {
  return (
    <section className="py-24 relative overflow-hidden bg-background">
      <div className="container mx-auto px-6 max-w-5xl">
        <div className="text-center mb-16">
          <motion.h2 
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            className="text-3xl md:text-5xl font-bold mb-4"
          >
            A Nossa Metodologia
          </motion.h2>
          <motion.p 
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            className="text-lg text-foreground/70"
          >
            Passo a passo: Deixando o caos operacional para trÃ¡s.
          </motion.p>
        </div>

        {/* Live Flowchart */}
        <div className="relative glass border border-white/10 rounded-3xl p-12 md:p-24 overflow-hidden flex flex-col md:flex-row items-center justify-between gap-12 md:gap-0">
          
          {/* SVG Connecting Path Background */}
          <svg className="absolute inset-0 w-full h-full pointer-events-none hidden md:block" preserveAspectRatio="none">
            <path 
              d="M 20% 50% L 50% 50% L 80% 50%" 
              stroke="rgba(255,255,255,0.1)" 
              strokeWidth="3" 
              fill="none" 
              strokeDasharray="6 6" 
            />
            {/* The traveling glowing dot */}
            <motion.circle 
              r="8" 
              fill="#10B981" 
              style={{ filter: "drop-shadow(0px 0px 8px #10B981)" }}
              animate={{ 
                cx: ["20%", "50%", "80%"], 
                cy: ["50%", "50%", "50%"],
                opacity: [0, 1, 1, 1, 0]
              }}
              transition={{ 
                duration: 3, 
                repeat: Infinity,
                times: [0, 0.1, 0.5, 0.9, 1],
                ease: "linear"
              }}
            />
          </svg>

          {/* Node 1: Vendas */}
          <div className="relative z-10 flex flex-col items-center">
            <motion.div 
              initial={{ scale: 0.8, opacity: 0 }}
              whileInView={{ scale: 1, opacity: 1 }}
              viewport={{ once: true }}
              className="w-24 h-24 rounded-full bg-[#080808] border-2 border-brand-blue flex items-center justify-center glow-blue mb-4 relative shadow-[0_0_20px_rgba(0,112,243,0.3)]"
            >
              <ShoppingCart className="w-8 h-8 text-brand-blue" />
            </motion.div>
            <h4 className="text-xl font-bold text-white">Vendas</h4>
            <p className="text-sm text-foreground/60 text-center max-w-[120px]">E-commerce / Marketplaces</p>
          </div>

          <ArrowRight className="md:hidden w-8 h-8 text-white/20 animate-pulse" />

          {/* Node 2: Estoque */}
          <div className="relative z-10 flex flex-col items-center">
            <motion.div 
              initial={{ scale: 0.8, opacity: 0 }}
              whileInView={{ scale: 1, opacity: 1 }}
              viewport={{ once: true }}
              transition={{ delay: 0.2 }}
              className="w-24 h-24 rounded-full bg-[#080808] border-2 border-white/30 flex items-center justify-center mb-4 relative"
            >
              <Package className="w-8 h-8 text-white" />
            </motion.div>
            <h4 className="text-xl font-bold text-white">OperaÃ§Ã£o</h4>
            <p className="text-sm text-foreground/60 text-center max-w-[120px]">Estoque & ERP</p>
          </div>

          <ArrowRight className="md:hidden w-8 h-8 text-white/20 animate-pulse" />

          {/* Node 3: Financeiro */}
          <div className="relative z-10 flex flex-col items-center">
            <motion.div 
              initial={{ scale: 0.8, opacity: 0 }}
              whileInView={{ scale: 1, opacity: 1 }}
              viewport={{ once: true }}
              transition={{ delay: 0.4 }}
              className="w-24 h-24 rounded-full bg-[#080808] border-2 border-brand-emerald flex items-center justify-center glow-emerald mb-4 relative shadow-[0_0_20px_rgba(16,185,129,0.3)]"
            >
              <DollarSign className="w-8 h-8 text-brand-emerald" />
            </motion.div>
            <h4 className="text-xl font-bold text-white">Financeiro</h4>
            <p className="text-sm text-foreground/60 text-center max-w-[120px]">ConciliaÃ§Ã£o & Caixa</p>
          </div>

        </div>

        {/* Steps */}
        <div className="grid md:grid-cols-4 gap-6 mt-16">
          {[
            { step: '01', title: 'Raio-X', desc: 'Mapeamento de gargalos e vazamentos financeiros.' },
            { step: '02', title: 'Desenho do Fluxo', desc: 'CriaÃ§Ã£o das regras e padronizaÃ§Ã£o das rotinas.' },
            { step: '03', title: 'ImplantaÃ§Ã£o', desc: 'InjeÃ§Ã£o de automaÃ§Ãµes low-code e software escalÃ¡vel.' },
            { step: '04', title: 'Monitoramento', desc: 'Sua operaÃ§Ã£o rodando de forma previsÃ­vel e segura.' }
          ].map((item, idx) => (
            <motion.div 
              key={idx}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: 0.1 * idx }}
              className="glass p-6 rounded-2xl border border-white/5 hover:border-white/10 transition-colors"
            >
              <span className="text-white/30 text-xl font-bold font-mono mb-2 block">{item.step}</span>
              <h4 className="text-lg font-bold mb-2">{item.title}</h4>
              <p className="text-sm text-foreground/70">{item.desc}</p>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  )
}


ARQUIVO: src\components\ui\SpotlightCard.tsx

'use client';

import React, { useRef, useState } from "react";
import { motion } from "framer-motion";

interface SpotlightCardProps {
  children: React.ReactNode;
  className?: string;
  spotlightColor?: string;
}

export function SpotlightCard({
  children,
  className = "",
  spotlightColor = "rgba(255, 255, 255, 0.05)"
}: SpotlightCardProps) {
  const divRef = useRef<HTMLDivElement>(null);
  const [isFocused, setIsFocused] = useState(false);
  const [position, setPosition] = useState({ x: 0, y: 0 });
  const [opacity, setOpacity] = useState(0);

  const handleMouseMove = (e: React.MouseEvent<HTMLDivElement>) => {
    if (!divRef.current || isFocused) return;

    const div = divRef.current;
    const rect = div.getBoundingClientRect();

    setPosition({ x: e.clientX - rect.left, y: e.clientY - rect.top });
  };

  const handleFocus = () => {
    setIsFocused(true);
    setOpacity(1);
  };

  const handleBlur = () => {
    setIsFocused(false);
    setOpacity(0);
  };

  const handleMouseEnter = () => {
    setOpacity(1);
  };

  const handleMouseLeave = () => {
    setOpacity(0);
  };

  return (
    <motion.div
      ref={divRef}
      onMouseMove={handleMouseMove}
      onFocus={handleFocus}
      onBlur={handleBlur}
      onMouseEnter={handleMouseEnter}
      onMouseLeave={handleMouseLeave}
      className={`relative overflow-hidden rounded-3xl border border-white/5 bg-[#0a0a0a] transition-colors focus-within:border-brand-primary/50 ${className}`}
    >
      <div
        className="pointer-events-none absolute -inset-px opacity-0 transition duration-300"
        style={{
          opacity,
          background: `radial-gradient(600px circle at ${position.x}px ${position.y}px, ${spotlightColor}, transparent 40%)`,
        }}
      />
      {children}
    </motion.div>
  );
}


ARQUIVO: src\lib\utils.ts

import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
